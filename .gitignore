# Dependencies
node_modules/

# Build output
dist/
packages/server/dist/
packages/dashboard/dist/
packages/shared/dist/
watchdog/dist/
menubar/build/

# TypeScript incremental build cache
*.tsbuildinfo

# Deploy artifacts
deploy/dist/

# macOS
.DS_Store
*.swp
*.swo

# IDE
.vscode/
.idea/
*.code-workspace

# Environment / Secrets (never commit)
.env
.env.local
*.yaml.bak

# User data (lives in ~/.dojo/, not in the repo)
*.db
*.db-wal
*.db-shm

# Logs
*.log

# Temporary files
/tmp/
*.tmp

# Voice mode runtime assets (copied from node_modules at dev/build time)
packages/dashboard/public/vad/
packages/dashboard/public/ort/

# Internal planning docs — kept local, never in the public repo (see a041b8d).
# Ignored so they can't be accidentally re-staged.
/DOJO-ORB-REDESIGN-PLAN.md
/docs/error-handling-spec.md
/packages/server/src/agent/v2/PRESERVATION_CHECKLIST.md
/SEMANTIC-ROUTER-PLAN.md

# Local router test harness (dev-only; run via: npx tsx packages/server/scripts/router-harness.mts)
/packages/server/scripts/router-harness.mts
/SCREEN-SHARE-PLAN.md
