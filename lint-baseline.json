{
  "$comment": "Lint baseline (PHASE-0 T3). Every count below may only FALL: check-lint-baseline.mjs fails the build when any rule or diagnostic code exceeds its pin, when an unpinned rule reports findings, or when either total rises. Counts are PER RULE, not just totals, so a rise in one rule cannot hide behind a fall in another. Seeded 2026-07-27 at commit fc662b6 from the commands recorded in each section, run on a clean, instrument-free tree. The eslint rules are all severity `warn` in eslint.config.js on purpose — the tree has hundreds of pre-existing findings and error-mode would be unshippable; this file is the enforcement, not the severity. To LOWER entries after real fixes: node deploy/checks/check-lint-baseline.mjs --tighten. To RAISE one, or to admit a newly enabled rule, edit this file BY HAND in a gate-side-only commit — that edit is meant to be seen and reviewed. No tool raises a number.",
  "eslint": {
    "$comment": "Findings by rule id. `--no-inline-config` is part of the command and is not optional: without it a one-line eslint-disable comment lowers the count without fixing anything. no-constant-binary-expression is pinned at 0 — the rule IS enabled and the tree is genuinely clean of it, so any future finding is a rise from zero.",
    "$classification-no-restricted-imports": "PHASE-5 T7 (2026-08-03), RULING P5-R12 DISCHARGED — the exit gate said \"no-restricted-imports flips ENFORCED\" and it CANNOT be executed as written, so this records the measurement instead of gaming the number. ALL 95 SITES WERE CLASSIFIED BY READING each one and its callers (two independent read-throughs, 12 of the 95 re-verified by the task's own commands), never by filename and never by directory. Command: `npx eslint packages/server/src --no-inline-config -f json`, then group the no-restricted-imports messages by file:line. THE ANSWER IS THREE CLASSES, NOT TWO, and the third is the reason the flip is impossible honestly. (1) AGENT-NAMED — 37 sites. The fs/proc call acts on a resource the AGENT CHOOSES: a path, a command, a URL out of tool arguments. THIS SET IS THE REQUIREMENT (*nothing an agent can reach touches fs/proc/net except through a broker*). Sites: agent/canvas-view.ts:19 · agent/image-prep.ts:31,32 · agent/path-resolve.ts:16 · agent/pdf-tools.ts:23 · agent/runtime.ts:5 · agent/system-control.ts:19,20 · agent/tools/cat/canvas.ts:23 · agent/tools/cat/comms.ts:47 · agent/tools/cat/fs.ts:35 · agent/tools/cat/media.ts:41 · agent/tools/process-run.ts:19 · agent/tools/util.ts:19 · google/tools-slides.ts:11 · memory/large-files.ts:1 · microsoft/tools-office.ts:6 · plaud/client.ts:14 · services/audio-generation.ts:27 · services/canvas-html.ts:18 · services/email-attachments.ts:10 · services/generation-jobs.ts:28 · services/image-generation.ts:25 · services/imessage-bridge.ts:5,6 · services/office-render.ts:16 · services/public-share.ts:16 · services/transcription.ts:18,21 · services/video-generation.ts:21 · services/video-job-poller.ts:24 · techniques/dependencies.ts:36 · techniques/share-import.ts:10 · techniques/store.ts:5 · techniques/tools.ts:5 · techniques/versioning.ts:5 · tools/index-generator.ts:7. (2) AGENT-TRIGGERED, PLATFORM-NAMED — 17 sites. A tool call can make the code run, but every path and command is a platform literal the agent cannot influence (self-update, the watchdog bundle, voice model files, the tunnel pidfile, the PM/Dreamer soul templates). Routing these through agent-facing brokers is exactly what P5-R12 forbids: it would record an agent-facing fs site that does not exist and make the count LIE. Sites: gateway/routes/update.ts:6,8 · services/resource-monitor.ts:6 · services/tunnel.ts:6,7 · services/watchdog-refresh.ts:25,27 · tracker/pm-agent.ts:1 · update-state.ts:18 · update/artifact-integrity.ts:48 · vault/maintenance.ts:8 · voice/custom-voices.ts:25 · voice/model-manager.ts:1,2 · voice/stt-service.ts:1,2 · voice/tts-service.ts:4. (3) PLATFORM-INTERNAL — 41 sites. No tool path at all: boot, the database, migration import/export, logging, the dashboard's own HTTP routes, dependency installers. Sites: agent/v2/receipt.ts:23 · config/loader.ts:1 · db/connection.ts:2 · db/migrations.ts:1 · gateway/routes/agents.ts:3 · gateway/routes/config.ts:4 · gateway/routes/migration.ts:6,11 · gateway/routes/services.ts:8 · gateway/routes/setup-deps.ts:7,8 · gateway/routes/techniques.ts:6 · gateway/routes/upload.ts:3 · gateway/server.ts:6 · healer/healer-agent.ts:13 · imaginer/imaginer-agent.ts:24 · index.ts:2 · logger.ts:1 · memory/assembly-validation-sink.ts:54 · microsoft/office-packages.ts:6 · migration/checks.ts:5,8 · migration/export.ts:5 · migration/fs-copy.ts:5 · migration/import.ts:5 · migration/manifest.ts:6 · migration/path-migration.ts:5 · migration/step-classify.ts:16,19 · plaud/auth.ts:10 · prompt/assembler.ts:1 · providers/anthropic-sdk-auth.ts:5 · screen-share/manager.ts:17 · services/ensure-system-deps.ts:20,22 · techniques/share-export.ts:12 · techniques/trainer-agent.ts:1 · twilio/sms-inbound.ts:8 · voice/smart-turn.ts:29,30 · voice/voice-assets.ts:8. WHY THE FLIP DOES NOT LAND HERE, said plainly: the brokers AUTHORIZE a declared effect at the executor's gate loop, before dispatch — they do not perform the I/O. So a handler that has been authorized still has to call `fs`, and the rule counts that call's import. Satisfying the rule over class (1) needs an fs/proc FACADE that performs the I/O behind the brokers, and no plan in this folder builds one. Inventing it inside a phase-exit task would be a large refactor of 37 sites with no capability gained and the exit battery still to run. TRANSFERRED, per P5-R12 step 3, to SWEEP-G (the last sweep, and the only point at which class (1) stops moving — every earlier sweep relocates some of these files), with the precondition stated: the flip requires the facade decision first, and if that decision is NOT taken the honest outcome is to retire the \"flips ENFORCED\" wording rather than leave a gate nobody can pass. THE DIRECTION IS STILL ENFORCED MEANWHILE, which is what actually protects the surface: this baseline may only FALL, and every raise is a reviewed by-hand gate-side edit that has to name the alternatives it measured and refused — the three $raise entries below are that record.",
    "command": "node_modules/.bin/eslint packages/server/src --no-inline-config -f json",
    "filesLintedWhenSeeded": 305,
    "counts": {
      "@typescript-eslint/no-floating-promises": 13,
      "no-constant-binary-expression": 0,
      "no-empty": 5,
      "no-restricted-imports": 87,
      "prefer-const": 7
    },
    "$raise-no-restricted-imports-t6b": "94 -> 95, PHASE-5 T6B (2026-08-03), by hand, gate-side. ONE new `node:fs` site: packages/server/src/update/artifact-integrity.ts, which streams the downloaded update artifact through sha256 before anything is rsynced over the running install. THE ALTERNATIVES WERE MEASURED, NOT WAVED AWAY. (a) `fs/promises.readFile` is the same restricted family AND would hold a tens-of-megabyte platform zip in memory to hash it — a worse answer that does not even move the count. (b) Routing it through a broker is the wrong shape and would make the count lie: the brokers exist to put an AGENT'S declared effects behind a gate, and this path is platform-internal update machinery no agent can reach, so a broker call here would record an agent-facing fs site that does not exist. (c) Hashing inside gateway/routes/update.ts, which already imports node:fs, would hold the count flat at the cost of putting the whole verify/refuse/transition-case decision back in the route this task took it out of. The rule is advisory by its own message and exists to MEASURE the surface Phase 5's brokers must route; this is that surface growing by one, named, with its file and its job, so T7 re-derives a count it can trust rather than one that was gamed. Verifying downloaded bytes before overwriting a working install is the task.",
    "$raise-no-restricted-imports-2": "90 -> 92, PHASE-5 T4 sitting 2 (2026-08-02), by hand, gate-side. THE RULE COUNTS IMPORT STATEMENTS, AND A SPLIT MULTIPLIES THEM WITHOUT ADDING ONE BYTE OF NEW fs ACCESS. `agent/tools.ts` held ONE `node:fs` import serving every fs-touching handler in a 268-case switch. T4 moves those handlers into per-category modules, and a module that calls `fs.statSync` must import `node:fs` itself — so the SAME calls, byte for byte, now need one import statement per module instead of one for the file. AMENDED 2026-08-02, same task, same mechanism, +1 more (92 -> 93) for `agent/tools/cat/media.ts`, whose four generators write the produced asset into the calling agent's uploads dir, and +1 more again (93 -> 94) for `agent/tools/cat/canvas.ts`, whose `canvas_render` stats the file it is asked to put on the canvas. This raise is +2 for the comms category: `agent/tools/cat/comms.ts` (share_file / show_to_user / share_publicly read and stat the paths they publish) and `agent/tools/util.ts` (registerSharedFile, which every category that mints a download URL calls, and which therefore may not live in any one category module). Both were merged to a SINGLE statement each rather than left as the two the conversion produced (`import fs, { existsSync, statSync } from 'node:fs'`). The alternative that would hold the count flat is a shared fs facade invented during a relocation, which is exactly the improvement-during-a-move this task forbids; the honest answer is that the count is a proxy for direct fs access and the access did not change. T7 re-derives this number when the split is finished and `agent/tools.ts` is gone. NOT a licence for new fs sites: any raise that is not a relocation still owes its own reason.",
    "$raise-no-restricted-imports": "89 -> 90, PHASE-3 T5 opening rider (2026-08-01), by hand. ONE new `node:fs` site: packages/server/src/memory/assembly-validation-sink.ts, the durable divergence sink T4's detect-only window needs — its evidence had been going to dojo.log, which logger.ts rotates at 10MB keeping one backup, measured at ababf11 as 16m14s of total retained history for a SEVEN-DAY window, with T4's own 17 day-0 lines already unrecoverable. The one alternative that would have kept this count flat was routing the append through logger.ts, which already imports fs — and it was REFUSED on measurement, not taste: logger.ts resolves LOG_DIR from os.homedir() at MODULE LOAD, so a sink built on it could not be tested against a redirected HOME without making a foundational module's path resolution lazy, i.e. changing the logging hot path to hold a lint count flat. This rule is advisory by its own message and exists to measure the surface Phase 5's brokers must route; this entry is that surface growing by one, named, with its file, so Phase 5 inherits a count it can trust rather than one that was gamed.",
    "$raise-no-restricted-imports-t7": "90 -> 91, PHASE-3 T7 Step 2 (2026-08-01), by hand. ONE new `node:fs` site: packages/server/src/agent/v2/re-answer-sink.ts, the durable sink the 7-day RE-ANSWER QUIET WINDOW needs. Same measurement as T5's entry above, RE-DERIVED at T7's own HEAD rather than inherited: dojo.log.1 (19,522,717 bytes) spans 06:51:12.869Z -> 06:52:14.145Z and dojo.log (6,635,551 bytes) spans 06:52:14.124Z -> 06:55:27.542Z, i.e. 4 MINUTES 15 SECONDS of total retained history for a SEVEN-DAY window. The same alternative was available and refused for the same measured reason (logger.ts resolves LOG_DIR from os.homedir() at MODULE LOAD, so a sink built on it cannot be tested against a redirected HOME without making the logging hot path lazy to hold a lint count flat). THE SURFACE IS TEMPORARY BY DESIGN AND ITS RETIREMENT IS DATED: PHASE-3.md T7 Step 2 deletes the detector after the quiet window and this sink goes with it, and T9's exit gate greps for the detector. Phase 5 inherits a count that is due to fall by one, not a permanent addition. PAID 2026-08-01, PHASE-3 T7 Step 2 (RULING P3-R3): the detector and this sink are DELETED and the count is back to 90 by `--tighten`, not by hand. The entry is KEPT rather than removed because a raise that named its own retirement and then met it is the record worth having.",
    "total": 112,
    "$raise-no-restricted-imports-t8": "95 -> 96, PHASE-5 T8 Steps 2-3 (2026-08-03), by hand, gate-side. THE FACADE IS THE SANCTIONED HOLDER OF THESE TWO IMPORTS, AND THIS IS THE ONE RAISE THAT ENDS WITH THE RULE FLIPPING. Net movement, measured (`npx eslint packages/server/src --no-inline-config -f json`, unit = import statements): MINUS agent/tools/process-run.ts:19 (`node:child_process`, GONE — the process door now spawns through the facade), PLUS agent/effects/proc.ts:20 and agent/effects/fs.ts:25 — the two entries that PERFORM the fs/proc work behind the per-call capability the executor's gate loop mints (RULING P5-R14). THE ALTERNATIVES WERE MEASURED, NOT WAVED AWAY. (a) Hold the count flat by leaving `child_process` in `process-run.ts` and having the facade only CHECK: refused, and it is the whole point of the task — a layer that authorizes but does not carry is the residual T8 exists to close, and a handler holding the import can still aim it anywhere. (b) Put both entries in ONE module so the rule counts one import: refused, because it would merge two effect families into one file to move a number; the plan's own wording is one entry per family (fs / proc / net), and the families have different resolvers, different request shapes and different tests. (c) Route the facade's own I/O through an existing importer (`logger.ts`, `path-resolve.ts`) to avoid a new site: refused for the reason RULING P5-R12 names — it would make the count LIE about the surface it measures, recording fs access in a module that does not do it and hiding it in one that does. THE RETIREMENT IS DATED AND IS THE NEXT STEP OF THIS TASK: PHASE-5.md T8 Step 4 scopes `no-restricted-imports` to the AGENT-REACHABLE set and FLIPS IT TO ERROR, with the facade named on the excluded list as the layer the rule exists to funnel everything into, and the count over the agent-reachable set falls to zero as each category converts. This raise is the transitional state between 'the brokers decide and nothing carries' and 'nothing an agent can reach touches fs/proc except through the facade'. THE DIRECTION IS STILL ENFORCED: every other rule is unchanged and this one may not rise again without another hand edit.",
    "$classification-amendment-t8": "AMENDMENT to $classification-no-restricted-imports, PHASE-5 T8 Steps 2-3 (2026-08-03). The 95-site enumeration above was measured at 4f3e21e and REPRODUCED EXACTLY at the start of this task (95 = 37 agent-named / 17 agent-triggered-platform-named / 41 platform-internal, 0 drift either direction, same command, unit = import statements). T8's first converted category moves ONE site and adds a new CLASS the original enumeration could not have: (1) AGENT-NAMED loses `agent/tools/process-run.ts:19` — the process door's `node:child_process` import is deleted and both exec doors now spawn through the facade. Class (1) is therefore 36 sites at this HEAD. (2) NEW CLASS - THE FACADE ITSELF: `agent/effects/proc.ts:20` and `agent/effects/fs.ts:25`. These are neither agent-named nor platform-internal: they are the SANCTIONED holders, the two modules the other 36 are being funnelled into, each performing I/O only on a resource the per-call capability names. They are the excluded set Step 4's flip is scoped AROUND, by name and not by directory. Totals at this HEAD: 96 = 36 agent-named + 2 facade + 17 agent-triggered-platform-named + 41 platform-internal. Re-derive at your own HEAD; do not spend this.",
    "$fall-no-restricted-imports-t8c": "96 -> 95, PHASE-5 T8 Step 3 category 2 (2026-08-03), by hand, gate-side. THE PIN FALLS, WHICH IS THE DIRECTION THIS RULE HAS ALWAYS ENFORCED, and it is the first fall since the facade landed. Net movement, measured (`node_modules/.bin/eslint packages/server/src --no-inline-config -f json`, unit = import statements): MINUS agent/tools/cat/canvas.ts:23 (`node:fs`, GONE — the canvas door's screenshot write now goes through the facade, which performs it only on the resource `open_browser`'s corrected declaration names). Nothing was added. Class (1) AGENT-NAMED is therefore 35 import statements in 31 files at this HEAD, down from 36 in 32; the facade's own 2 sites, the 17 agent-triggered-platform-named and the 41 platform-internal are unchanged. Re-derive at your own HEAD; do not spend this. The retirement of `$raise-no-restricted-imports-t8` is still dated to Step 4 and Step 4 has NOT run — see task-T8C-report.md §3 for the measured reason (a majority of the remaining agent-named set reaches its fs/proc calls from OUTSIDE any tool call, where a converted site holds no capability, and that set is enumerated and handed up rather than forced).",
    "$fall-no-restricted-imports-t8d-c3": "95 -> 94, PHASE-5 T8 Step 3 category 3 (2026-08-03), by hand, gate-side. THE PIN FALLS AGAIN — the second fall since the facade landed, and the direction this rule has always enforced. Net movement, measured (`node_modules/.bin/eslint packages/server/src --no-inline-config -f json`, unit = import statements): MINUS memory/large-files.ts:1 (`node:fs`, GONE — the recall door reads the stored body of a `file_*` id through the facade, which performs it only on the resource `history_get`'s corrected declaration names). Nothing was added. Class (1) AGENT-NAMED is therefore 34 import statements in 30 files at this HEAD, down from 35 in 31; the facade's own 2 sites, the 17 agent-triggered-platform-named and the 41 platform-internal are unchanged. Re-derive at your own HEAD; do not spend this. The retirement of `$raise-no-restricted-imports-t8` is still dated to Step 4, which RULING P5-R15 part 4 holds until the owner's answer on the seven platform-timed files arrives — its excluded list must describe a decided tree.",
    "$fall-no-restricted-imports-t8d-c4": "94 -> 93, PHASE-5 T8 Step 3 category 4 (2026-08-03), by hand, gate-side. Net movement, measured (`node_modules/.bin/eslint packages/server/src --no-inline-config -f json`, unit = import statements): MINUS services/email-attachments.ts:10 (`node:fs`, GONE — `readLocalAttachments` asks the facade for exactly the paths the call named). Nothing was added. THE MECHANIC THAT UNBLOCKED IT is RULING P5-R15 part 1's array-argument resolver correction: `args.<name>[]` now mints ONE GRANT PER ELEMENT. Read as a literal key it had resolved to no grant at all, which is fail-closed in the wrong place — it reads as `this tool touches no file` when the tool touches every file in the list. Re-derived at this HEAD (`git grep -n \"from: 'args\\.[a-zA-Z_.]*\\[\\]'\" -- packages/server/src`, unit = declarations): 14 declarations, 11 fs-kind. Class (1) AGENT-NAMED is therefore 33 import statements in 29 files at this HEAD, down from 34 in 30.",
    "$classification-amendment-t8d-path-resolve": "AMENDMENT to $classification-no-restricted-imports, PHASE-5 T8 Step 3 (2026-08-03, T8D), RULING P5-R15 part 3. `agent/path-resolve.ts:16` MOVES from class (1) AGENT-NAMED to the class the T8 amendment created for the facade itself — THE CARRYING MACHINERY. THE TOTAL DOES NOT MOVE AND THAT IS THE POINT: this is not a site leaving the measured surface, it is a site being named as part of the layer the surface is being funnelled INTO. P5-R12's refusal (never move a site between classes to make the count fall) is not engaged, because nothing falls: 93 before, 93 after. THE STRUCTURAL REASON, RE-DERIVED AT THIS HEAD BY COMMAND rather than inherited (`git grep -n path-resolve -- packages/server/src/agent/effects`): `agent/effects/fs.ts:26` and `agent/effects/scopes.ts:35` BOTH import it, and it is what the facade resolves paths WITH — `canonicalizeAgentPath` and `resolveRealPathHardened` produce the two spellings every fs request and every minted grant carries. It therefore runs BEFORE the capability exists. Converting it is a cycle (the facade importing the module that would import the facade) and a contradiction (asking for authorization with the resolution that authorization is expressed in) at the same time. It is excluded BY NAME with this reason, never by directory. Class (1) AGENT-NAMED is therefore 32 import statements in 28 files at this HEAD; the carrying machinery is 3 (`agent/effects/fs.ts`, `agent/effects/proc.ts`, `agent/path-resolve.ts`); the 17 agent-triggered-platform-named and the 41 platform-internal are unchanged. Re-derive at your own HEAD; do not spend this.",
    "$fall-no-restricted-imports-t8d-c5": "93 -> 92, PHASE-5 T8 Step 3 category 5 (2026-08-03), by hand, gate-side. Net movement, measured (`node_modules/.bin/eslint packages/server/src --no-inline-config -f json`, unit = import statements): MINUS services/image-generation.ts:25 (`node:fs`, GONE — the provider's decoded bytes and the directory they need are written through the facade, on the resource `image_create`'s corrected declaration names). Nothing was added. AT THIS HEAD, with `agent/path-resolve.ts:16` reclassified to the carrying machinery by the amendment above: 92 = 31 agent-named (27 files) + 3 carrying machinery + 17 agent-triggered-platform-named + 41 platform-internal. T8D opened at 95 = 35 agent-named (31 files) + 2 + 17 + 41. Re-derive at your own HEAD; do not spend this. The retirement of `$raise-no-restricted-imports-t8` is still dated to Step 4, which RULING P5-R15 part 4 holds until the owner's answer on the seven platform-timed files arrives.",
    "$fall-no-restricted-imports-t8e-c6": "92 -> 91, PHASE-5 T8 Step 3 category 6 (2026-08-03, T8E), by hand, gate-side. Net movement, measured (`node_modules/.bin/eslint packages/server/src --no-inline-config -f json`, unit = import statements): MINUS agent/tools/cat/comms.ts:47 (`node:fs`, and its two named companions `existsSync`/`statSync` on the same statement, GONE — share_file probes, show_to_user probes and copies in, and imessage_send pre-flights its attachments, all through the facade on the resources those calls declared). Nothing was added. THE DECLARATION CORRECTED AT THE SITE: show_to_user gained the fs_write it has always performed, scoped to the CALLING agent uploads tree; the wider ~/.dojo/uploads was refused because it would authorize every other agent uploads directory. Class (1) AGENT-NAMED is therefore 30 import statements in 26 files at this HEAD, down from 31 in 27; the carrying machinery 3, the 17 agent-triggered-platform-named and the 41 platform-internal are unchanged. Re-derive at your own HEAD; do not spend this. The retirement of `$raise-no-restricted-imports-t8` is dated to Step 4.",
    "$classification-amendment-t8e-attachment-resolve": "AMENDMENT to $classification-no-restricted-imports, PHASE-5 T8 Step 3 category 7 (2026-08-03, T8E), RULING P5-R15 ADDENDUM mechanic 5. THE TOTAL DOES NOT MOVE — 91 before, 91 after — and that is the point, so P5-R12’s refusal is not engaged. Net movement, measured (`node_modules/.bin/eslint packages/server/src --no-inline-config -f json`, unit = import statements): MINUS agent/tools/cat/media.ts:41 (`node:fs`, GONE — image_create’s delivery copy and transcribe_audio’s two disk sources go through the facade). PLUS services/attachment-resolve.ts:35, a NEW member of the CARRYING MACHINERY class, by name and with its structural reason — the same class, and the same kind of reason, as agent/path-resolve.ts. THE REASON, re-derived at this HEAD rather than inherited: `transcribe_audio` names its file INDIRECTLY through an `attachment_id`, so the gate loop must resolve that id to a path BEFORE it mints the call’s capability. `resolveAttachmentPath` was moved verbatim out of services/transcription.ts into this leaf because the gate loop cannot import that module — it pulls in the local STT engines (voice/stt-service.ts, voice/model-manager.ts) and a `node:child_process` spawn, none of which belong in the executor’s graph; transcription.ts re-exports the name so no consumer moved. Like path-resolve.ts, this module runs BEFORE the capability exists, which is why it holds node:fs directly and is excluded BY NAME rather than by directory. THE ALTERNATIVE WAS MEASURED: leaving the reader in transcription.ts and having scopes.ts import it would have dragged the STT engines and a subprocess spawn into every tool dispatch’s module graph to hold one import site flat — refused. Class sizes at this HEAD: 91 = 29 agent-named (25 files) + 4 carrying machinery (agent/effects/fs.ts, agent/effects/proc.ts, agent/path-resolve.ts, services/attachment-resolve.ts) + 17 agent-triggered-platform-named + 41 platform-internal. Re-derive at your own HEAD; do not spend this.",
    "$fall-no-restricted-imports-t8e-c8": "91 -> 89, PHASE-5 T8 Step 3 category 8 (2026-08-03, T8E), by hand, gate-side. Net movement, measured (`node_modules/.bin/eslint packages/server/src --no-inline-config -f json`, unit = import statements): MINUS services/generation-jobs.ts:28 and services/audio-generation.ts:27 (both `node:fs`, GONE — the tts_create / music_create asset is written, read back and delivered through the facade on the resources those calls declared). Nothing was added. THESE TWO WERE RECLASSIFIED BEFORE THEY CONVERTED, and the distinction matters: both sat in T8C §4c as PLATFORM-REACHED, i.e. not convertible. RULING P5-R15’s ADDENDUM sharpened the criterion — \"platform-timed\" disqualifies only when the I/O runs in a context the DISPATCH DID NOT OPEN — and the re-read at this HEAD measured that both are reached on exactly ONE chain, all of it dispatch-opened: deliverAsset has one call site (runAudioOrMusicJob), which has one call site (enqueueAudioOrMusicJob), whose one production caller is the tts_create/music_create handler at agent/tools/cat/media.ts:593; audio-generation.ts has ONE production importer in the whole tree (generation-jobs.ts:262) and it sits on that same chain; the boot worker startGenerationJobsWorker calls setFailed and deliverError only and reaches no fs line. THIS IS NOT A SITE MOVING BETWEEN CLASSES TO MAKE A COUNT FALL (P5-R12): the count falls because the imports are GONE, and the reclassification is a correction of a measurement the ADDENDUM ordered re-taken. Class (1) AGENT-NAMED is therefore 27 import statements in 23 files at this HEAD, down from 29 in 25; the carrying machinery is 4, the 17 agent-triggered-platform-named and the 41 platform-internal are unchanged. Re-derive at your own HEAD; do not spend this.",
    "$fall-no-restricted-imports-t8e-c9": "89 -> 88, PHASE-5 T8 Step 3 category 9 (2026-08-03, T8E), by hand, gate-side. Net movement, measured (`node_modules/.bin/eslint packages/server/src --no-inline-config -f json`, unit = import statements): MINUS agent/tools/cat/fs.ts:35 (`node:fs`, GONE — file_read, file_write, file_append, file_patch and file_list all reach the disk through the facade, on the resources those calls declared). Nothing was added. TWO AUTHORIZED MECHANICS LANDED WITH IT. RULING P5-R15 ADDENDUM mechanic 6: file_patch’s temp-sibling-then-rename moved WHOLE into the carrying layer (same naming, same rename, same cleanup, error rethrown unchanged), because the tmp sibling is not a resource the tool NAMES and requiring a grant for it would make every declaration describe the mechanism instead of the effect. RULING P5-R15 part 1’s file_list tree resolver: an args. effect may now carry `scope: { at: \"argTree\" }`, meaning the argument names a directory and the effect covers what is inside it — without it every entry’s size column would have silently read \"-\", because the handler’s own catch swallows a per-entry refusal, and NOTHING would have failed. file_read has the identical `from` and deliberately does NOT carry it; a clause asserts that, which is what makes argTree a scope rather than a habit. THE TOOLBOX TREE NOW HAS EXACTLY ONE DIRECT HOLDER LEFT: agent/tools/util.ts:19, named in a clause rather than allowed by a pattern. It converts LAST for two measured reasons — its three probes run on the CALLER’s grant, so every tool that hands it a path must declare that path first, and its own `catch` returns null, so a premature conversion would narrow SILENTLY (no download URL, no canvas chip) instead of refusing loudly. Class (1) AGENT-NAMED is therefore 26 import statements in 22 files at this HEAD, down from 27 in 23; the carrying machinery is 4, the 17 agent-triggered-platform-named and the 41 platform-internal are unchanged. Re-derive at your own HEAD; do not spend this.",
    "$classification-amendment-t8f-tool-doc-read": "AMENDMENT to $classification-no-restricted-imports, PHASE-5 T8 Step 3 SURFACE SPLIT 1 (2026-08-03, T8F), RULING P5-R15 part 2. THE TOTAL DOES NOT MOVE — 88 before, 88 after — and that is the point, so P5-R12`s refusal (never move a site between classes to make the count fall) is NOT engaged: nothing falls, and the class changed because a MODULE SPLIT changed what the module does. Net movement, measured (`node_modules/.bin/eslint packages/server/src --no-inline-config -f json`, unit = import statements): tools/index-generator.ts:7 becomes tools/index-generator.ts:13 (same import, moved by the header the split earned) and MOVES from class (1) AGENT-NAMED to class (3) PLATFORM-INTERNAL. THE REASON, re-derived at this HEAD rather than inherited (`git grep -n generateToolDocs -- packages/server/src` and the resolved-specifier importer walk): the module used to hold TWO surfaces behind one import — `generateToolDocs`, whose ONE production caller is the boot step at index.ts:428, and `readToolDoc`, called inside the load_tool_docs dispatch. With the reader gone to its own leaf (services/tool-doc-read.ts is NOT it — the new module is tools/tool-doc-read.ts, which holds NO restricted import because it reads through the facade), what remains runs once at boot, before any agent exists, on paths the platform names: that is class (3) by the original enumeration`s own definition (`no tool path at all: boot, the database, migration import/export, logging...`). THE ALTERNATIVE WAS MEASURED: moving the GENERATOR out instead and leaving the reader in place — refused, because the file`s own name and header describe the generator, and because the generator statically imports the whole tool registry, which does not belong in the module graph of a dispatch that wants to read one markdown file. Class sizes at this HEAD: 88 = 25 agent-named (21 files) + 4 carrying machinery + 17 agent-triggered-platform-named + 42 platform-internal. Re-derive at your own HEAD; do not spend this.",
    "$fall-no-restricted-imports-t8f-pdf": "88 -> 87, PHASE-5 T8 Step 3 the PDF door (2026-08-03, T8F), by hand, gate-side. Net movement, measured (node_modules/.bin/eslint packages/server/src --no-inline-config -f json, unit = import statements): MINUS agent/pdf-tools.ts:23 (node:fs, GONE — pdf_create, pdf_get_info, pdf_read, pdf_merge, pdf_extract_pages, pdf_rotate_pages, pdf_reorder_pages, pdf_delete_pages, pdf_watermark and pdf_fill_form all reach the disk through the facade, on the resources those calls declared). Nothing was added. RULING P5-R15 ADDENDUM 2 MECHANIC 7 LANDED WITH IT: the resolver reads args.<name>[].<prop>, so pdf_create's image blocks resolve; an element without the property grants nothing for it. THE BARE-FILENAME DEFECT IS CORRECTED AT THE SITE, and it is the reason the conversion was possible at all: eight arguments (filename plus seven output_filename) were declared fs_write from args.<name> while being BARE NAMES, so the grant named a file in the server working directory the tool never touches and the real write went undeclared — converted as-is, every pdf tool would have refused its own output. The real target is now a scoped derived effect on the agent uploads tree and the names are ruled INERT in nonEffects with their reason, which is what T1's clause-6 tripwire asked for the moment the false declaration was removed. THE PROMPT-PREFIX RE-BLESSING IS EVIDENCED RATHER THAN ASSUMED (OR7): pdf_create's content.items gained the `path` property it has always accepted, which changes the tools array, so BOTH golden gates were RUN on the box — check-cache-prefix PASSED (byte-invariant across 9 turn-states, systemVolatile empty, golden sha 6f35ffce... re-verified by the gate itself) and check-assembled-context PASSED (golden sha d44571d4...). Class (1) AGENT-NAMED is therefore 24 import statements in 20 files at this HEAD, down from 25 in 21; the carrying machinery is 4, the 17 agent-triggered-platform-named unchanged, and platform-internal is 42 since the T8F surface-split amendment. Re-derive at your own HEAD; do not spend this."
  },
  "unusedSymbols": {
    "$comment": "Findings by TypeScript diagnostic code, from tsconfig.lint.json (noUnusedLocals + noUnusedParameters). That config is consumed ONLY by `npm run lint:types` — never by the build and never by `npm run typecheck` — so the unused-symbol population is counted without breaking either. TS6133 = declared but never read; TS6192 = all imports in a declaration unused; TS6196 = declared but never used (type position). 116 of the 118 sit in packages/server; the other 2 are in packages/shared, which the server program pulls in through its project reference.",
    "command": "node_modules/.bin/tsc -p tsconfig.lint.json --noEmit",
    "counts": {
      "TS6133": 114,
      "TS6192": 1,
      "TS6196": 3
    },
    "total": 118
  },
  "typeAwareRules": [
    "@typescript-eslint/no-floating-promises"
  ],
  "grandTotal": 226
}
