// Barrel export for v2 classifiers. Phase 1A–1E populate this directory.
// Each classifier is a small, pure (or DB-reading) function that answers
// one question. <5ms execution time. Independently testable.

export {};
