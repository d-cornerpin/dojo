{
  "$comment": "Size ratchets (PHASE-0 T2). Every path below may only SHRINK: check-ratchets.mjs fails the build when a listed file exceeds its pinned line count (wc -l, i.e. newlines). Seeded 2026-07-27 at commit 8a37099 from measured wc -l on a clean, instrument-free tree. Entries: every tracked *.ts/*.tsx under packages/*/src/ or watchdog/src/ (excluding __tests__, *.test.*, *.spec.*, db/migrations) that already exceeded maxNewFileLines. The 15 files the overhaul plan names are all here; the rest were seeded because the new-file rule would otherwise fail them on day one. To LOWER entries after real deletion: node deploy/checks/check-ratchets.mjs --tighten. To RAISE one, or to add a new file above the cap, edit this file BY HAND in a gate-side-only commit \u2014 that edit is meant to be seen and reviewed. No tool raises a number.",
  "$raises": [
    {
      "path": "packages/server/src/agent/tools/definitions.ts",
      "from": 2749,
      "to": 2757,
      "by": "PHASE-5 T12 (2026-08-03) \u2014 file_append declares the separator peek it has always performed",
      "why": "PHASE-5 T12, RULING P5-R14 \u2014 the declaration-correction class T8D established on `image_create`. +8 lines, ALL of them the reason comment; the `effects[]` line itself is unchanged in count (one entry becomes two on the same physical line). THE DEFECT IT FIXES: the exit battery `bmse5ef28xy` scenario `edit-existing-doc` recorded `Effect refused: file_append is not authorized for fs_read <path>` \u2014 at `ensure_newline`'s DEFAULT against a non-empty file `executeFileAppend` opens the same path for READING to inspect the trailing byte, and the declaration named only the write. A declaration can be honest and still be short by one, and the facade is what found it. It adds NO refusal: gate rows are declared in `tools/gates.ts` and are never derived from `effects[]` (RULING P5-R5), and the read names the SAME file as the write, so nothing widens \u2014 held by the clause that refuses a neighbour in both directions. THE ALTERNATIVES WERE MEASURED, NOT WAVED AWAY. (a) Drop the reason comment and keep the corrected declaration (+0): REFUSED for the reason every T8 category recorded on this same file \u2014 \"we looked and this is what it does\" and \"nobody looked\" must never read the same, and the next reader has to know the correction adds no refusal or it will be re-litigated. The first draft of this comment was TEN lines and was tightened to eight against its sibling `image_create`'s, which is the same correction in the same file; that is the saving taken instead. (b) Reformat the two effects onto separate physical lines like the multi-entry declarations elsewhere in this file (+2): REFUSED \u2014 `file_patch`, the tool with the identical two-direction shape on the identical `args.path`, uses the one-line form, and a second spelling of one shape is how a reader stops seeing them as one class. (c) Close the red by widening the grant, excluding the file, or changing the scenario (+0): REFUSED IN ADVANCE by the orchestrator's ruling and by this phase's posture \u2014 none of the three states what the code does, and a capability that only works because a grant was widened is the lost capability still lost. THE DIRECTION IS STILL ENFORCED: this file may only shrink from 2757."
    },
    {
      "path": "packages/server/src/index.ts",
      "from": 1172,
      "to": 1178,
      "by": "PHASE-5 T10 (2026-08-03) \u2014 owner decision D1, the sign-in tokens are encrypted where they are",
      "why": "+6, and it is one boot step: the pass that seals any Workspace OAuth token still stored in the clear, registered immediately after the legacy-config seed it mirrors so a row the seed has just created is sealed in the same boot. It is a boot step because it CANNOT be a numbered migration \u2014 the chain is `.sql`, SQLite has no AES, and the master key is reachable only from TypeScript; the platform already carries this shape twice (the account seed itself, and T6C's secrets-file mode repair). The call site is four lines of comment and two of code: a dynamic import and an awaited call, with NO try/catch, because the conversion owns its own non-fatality (`credentials/seal-existing.ts` never throws \u2014 a box whose master key cannot be read must still boot and still serve mail). The gate already earned its keep on this task: the first draft was a near-identical conversion loop copied into `google/accounts.ts` AND `microsoft/accounts.ts`, this ratchet refused the growth, and the duplicate was replaced by one owner with two thin table specs \u2014 which is the disease this project exists to remove, caught before it landed. From here this file may only shrink."
    },
    {
      "path": "packages/server/src/work/ask-title.ts",
      "from": 269,
      "to": 297,
      "by": "PHASE-6 T0B (2026-08-04) \u2014 the owner's flip: the ticket files ID-FIRST and the model's title replaces it",
      "why": "+28, measured as +18 comment/blank and +10 code (`git diff -U0` on the file, added lines classified by whether the trimmed line starts with a comment marker). THE ARM THIS CHANGE DELETED IS BIGGER THAN THE ONE IT ADDED \u2014 `ASK_TITLE_TIMEOUT_MS`, the `Promise.race`, its timer and `finally`, the duplicated late-rejection catch that existed only because of the race, and `resolveInboundAskTitle` are all gone; the file still grows because two mechanisms replaced one. The 10 code lines are `retitleIfStillUnnamed` (the guarded write: read and patch in ONE unit, writing only over the identifier the ticket was filed with, so a rename that landed while the model was thinking WINS) and `replaceAskTitleFromModel` (the fire-and-forget job the door and the one outer-transaction producer both start). THREE ALTERNATIVES WERE MEASURED AND REFUSED. (1) Move `retitleIfStillUnnamed` into `work/tracker-store.ts` beside `patchWork`: relocates ~14 lines into another pinned file and splits one job across two modules \u2014 pure relocation is exactly what retired the deletion-ratio gate (roadmap #7), and `one owner per job` says the ask-title machinery stays in the module named for it. (2) Cut the header's nine measured samples: those numbers ARE the record of the cost this commit removed (2296-5198 ms, median 3622, driven through the live seam), and the work order requires that record be kept \u2014 deleting the measurement to hit a line count is the exact failure the no-quota rule exists to prevent. (3) Cut the `NOTHING HERE IS UNBOUNDED BY THE DELETION` paragraph: it is the POSITIVE evidence that removing the bound did not create an unbounded call (the Ollama path's 300 s fetch timeout and the stream watchdog's 90 s/60 s abort, both independent of any caller signal) \u2014 non-negotiable #15 refuses a deletion that rests on an absence, and that paragraph is what keeps this one from doing so. From here this file may only shrink."
    },
    {
      "path": "packages/server/src/work/ask-title.ts",
      "from": 246,
      "to": 269,
      "by": "PHASE-5 T9 (2026-08-03) \u2014 the bounded wait's number is measured, not inherited",
      "why": "+23, and every line of it is the measurement that corrected an inherited number plus the failure mode that correction exposed. The timeout started at the voice opener's 1500 ms and DRIVING it on the box showed that value never fires: nine samples through the live seam ran 2296-5198 ms, median 3622. The header now carries those nine numbers, the command that produced them, and why 5000 ms (the platform's other existing system-model bound) is where it landed \u2014 because a threshold with no measurement beside it is a rumour, and this file is where the next reader will look for it. The remaining lines are a log line for a failure mode observed on the box: a system model answering with nothing at all fell back correctly and SILENTLY, and a mechanism whose failure mode is silence rots without anyone finding out. From here this file may only shrink."
    },
    {
      "path": "packages/server/src/gateway/routes/chat.ts",
      "from": 484,
      "to": 485,
      "by": "PHASE-5 T9 (2026-08-03) \u2014 decision D4, the tracker title is written by the system model",
      "why": "+1, and it is one import line. This route is the dashboard's inbound producer and now reaches the writer through the ingest door (`work/ask-title.ts`), while still importing `insertMessageIfAbsent` from the writer for its two session-divider rows, which are not a person's message and open no ticket. Two modules is two import statements; there is no line to trade for it and collapsing the divider writes into the door would send rows through a model call they must never make. From here this file may only shrink."
    },
    {
      "path": "packages/server/src/services/imessage-bridge.ts",
      "from": 2312,
      "to": 2314,
      "by": "PHASE-6 T0B (2026-08-04) \u2014 the owner's flip: this producer starts the replacement post-commit instead of resolving a title above its write",
      "why": "+2, and the +2 IS the reason text. T9's lift is unwound here: the `await resolveInboundAskTitle(...)` above the transaction and the `...(askTitle === null ? {} : { askTitle })` spread inside it are both DELETED, and one `void replaceAskTitleFromModel(inboundRow, msgId)` is added in the post-commit block the file already had. Net +2 after a 6-line comment stating why this ONE producer starts the job itself rather than through the shared door \u2014 it owns an outer `db.transaction` because the poll cursor advances with the row. THE ALTERNATIVE MEASURED AND REFUSED: dropping that comment saves 6 and clears the ceiling, but it is the only thing standing between a later reader and the conclusion that this site BYPASSES the ingest door \u2014 which is precisely the rot the producer-conformance walk exists to catch, and the walk names this file by hand. Two lines is the honest price of not making that walk's reason invisible. From here this file may only shrink."
    },
    {
      "path": "packages/server/src/services/imessage-bridge.ts",
      "from": 2300,
      "to": 2312,
      "by": "PHASE-5 T9 (2026-08-03) \u2014 decision D4, the tracker title is written by the system model",
      "why": "+12. This is the ONE inbound producer that owns an outer `db.transaction` of its own \u2014 the poll cursor advances with the row, so there is no crash window in which the cursor is past a row that was never persisted. The title must be resolved BEFORE that transaction opens (the model call may never be held inside a write), so the message literal is lifted into a `NewMessage`-typed local above it and the resolver is called there, exactly as `resolveOrCreateConversation` already is on the line above. That lift is the whole cost: the literal gains its `const` and closing lines, plus the requirement stated where a later reader will need it. `conversationId` stays spelled inside the write statement itself so the serve-boundary conformance walk still reads atomic identity stamping at this site. Routing it through the shared door instead would have meant the door opening a transaction the bridge already owns. From here this file may only shrink."
    },
    {
      "path": "packages/server/src/memory/message-store.ts",
      "from": 981,
      "to": 1012,
      "by": "PHASE-5 T9 (2026-08-03) \u2014 decision D4, the tracker title is written by the system model",
      "why": "+31, and the mechanism it replaces was DELETED in the same change: `title: content.slice(0, 120)` is gone from `persistAndMaybeOpenAsk`. COMPRESSION WAS TAKEN WHERE IT EXISTED, and it is why this is +31 and not +80: the ingest door (`insertInboundMessageIfAbsent`, `resolveInboundAskTitle`) and the whole model lane live in the NEW file `packages/server/src/work/ask-title.ts` (246 lines, under maxNewFileLines and therefore unpinned), not here. That placement is a graph fact rather than a tidiness preference \u2014 this module is the single SYNCHRONOUS writer for `messages`, and giving it an import of the model layer would point the write side at the router; the dependency runs one way, the door reaches for the writer. WHAT REMAINS HERE IS THE PART THAT CANNOT LIVE ANYWHERE ELSE: the `askTitle` field on `NewMessage` with its refusal (never derived from content), `wouldOpenAsk` \u2014 the ONE ask gate, exported so the door can ask it before opening a transaction rather than a second predicate being written \u2014 and the replaced `title:` line carrying the requirement, the refusal (the fallback is never the slice) and why the ticket's own identifier is available before the transaction. The alternative that would have held this ceiling is leaving the owner's typed words being copied into `work.title`, which is the task. From here this file may only shrink."
    },
    {
      "path": "packages/server/src/tools/unified-read.ts",
      "from": 753,
      "to": 754,
      "by": "PHASE-5 T1 Step 1 (2026-08-02) \u2014 effects declared on every tool definition",
      "why": "PHASE-5 T1 (2026-08-02) \u2014 the effects declaration on every tool definition. Each of the ten files below grows by EXACTLY one `effects: [...]` line per definition it declares (plus a multi-line form where a tool has several effects, and a `nonEffects` block where a resource-shaped field is ruled inert with its reason). The count is arithmetic, not an estimate: 320 definitions, 111 of them in agent/tools.ts, and the per-file split is \u00a7T0-PINS P4's exactly. THERE IS NO WHERE ELSE TO PUT IT \u2014 that is the whole content of the task. The plan's Step 1 asked for a scan of six field names; \u00a7T0-PINS P3 measured that list at 39 of 320, missing `file_paths` (show_to_user's own field) and blind to effects with no resource argument at all (web_search's fixed host, spawn_agent's child, cliclick, `npx @plaud-ai/cli`), so the DECLARATION on the definition is the source of truth and a declaration lives where the thing it describes lives. It cannot live inside `input_schema`: that object is handed to the provider verbatim and the cache-prefix golden hashes it, so a key inside it would move the cached prefix (OR7 / roadmap #10). COMPRESSION WAS TAKEN WHERE IT EXISTED: `agent/tools.ts` is +40 and not +123, because `applyTextPagination`, `stringifyArgs` and `coerceNumberArg` (88 lines) left for `agent/tools/pagination.ts` in the same task \u2014 which is what kills six of the seven pinned dynamic-import hacks. The alternative that would have held these ceilings is not declaring the effects, which is the task."
    },
    {
      "path": "packages/server/src/agent/pdf-tools.ts",
      "from": 1007,
      "to": 1017,
      "by": "PHASE-5 T1 Step 1 (2026-08-02) \u2014 effects declared on every tool definition",
      "why": "PHASE-5 T1 (2026-08-02) \u2014 the effects declaration on every tool definition. Each of the ten files below grows by EXACTLY one `effects: [...]` line per definition it declares (plus a multi-line form where a tool has several effects, and a `nonEffects` block where a resource-shaped field is ruled inert with its reason). The count is arithmetic, not an estimate: 320 definitions, 111 of them in agent/tools.ts, and the per-file split is \u00a7T0-PINS P4's exactly. THERE IS NO WHERE ELSE TO PUT IT \u2014 that is the whole content of the task. The plan's Step 1 asked for a scan of six field names; \u00a7T0-PINS P3 measured that list at 39 of 320, missing `file_paths` (show_to_user's own field) and blind to effects with no resource argument at all (web_search's fixed host, spawn_agent's child, cliclick, `npx @plaud-ai/cli`), so the DECLARATION on the definition is the source of truth and a declaration lives where the thing it describes lives. It cannot live inside `input_schema`: that object is handed to the provider verbatim and the cache-prefix golden hashes it, so a key inside it would move the cached prefix (OR7 / roadmap #10). COMPRESSION WAS TAKEN WHERE IT EXISTED: `agent/tools.ts` is +40 and not +123, because `applyTextPagination`, `stringifyArgs` and `coerceNumberArg` (88 lines) left for `agent/tools/pagination.ts` in the same task \u2014 which is what kills six of the seven pinned dynamic-import hacks. The alternative that would have held these ceilings is not declaring the effects, which is the task."
    },
    {
      "path": "packages/server/src/google/tools-forms.ts",
      "from": 817,
      "to": 829,
      "by": "PHASE-5 T1 Step 1 (2026-08-02) \u2014 effects declared on every tool definition",
      "why": "PHASE-5 T1 (2026-08-02) \u2014 the effects declaration on every tool definition. Each of the ten files below grows by EXACTLY one `effects: [...]` line per definition it declares (plus a multi-line form where a tool has several effects, and a `nonEffects` block where a resource-shaped field is ruled inert with its reason). The count is arithmetic, not an estimate: 320 definitions, 111 of them in agent/tools.ts, and the per-file split is \u00a7T0-PINS P4's exactly. THERE IS NO WHERE ELSE TO PUT IT \u2014 that is the whole content of the task. The plan's Step 1 asked for a scan of six field names; \u00a7T0-PINS P3 measured that list at 39 of 320, missing `file_paths` (show_to_user's own field) and blind to effects with no resource argument at all (web_search's fixed host, spawn_agent's child, cliclick, `npx @plaud-ai/cli`), so the DECLARATION on the definition is the source of truth and a declaration lives where the thing it describes lives. It cannot live inside `input_schema`: that object is handed to the provider verbatim and the cache-prefix golden hashes it, so a key inside it would move the cached prefix (OR7 / roadmap #10). COMPRESSION WAS TAKEN WHERE IT EXISTED: `agent/tools.ts` is +40 and not +123, because `applyTextPagination`, `stringifyArgs` and `coerceNumberArg` (88 lines) left for `agent/tools/pagination.ts` in the same task \u2014 which is what kills six of the seven pinned dynamic-import hacks. The alternative that would have held these ceilings is not declaring the effects, which is the task."
    },
    {
      "path": "packages/server/src/google/tools-read.ts",
      "from": 938,
      "to": 949,
      "by": "PHASE-5 T1 Step 1 (2026-08-02) \u2014 effects declared on every tool definition",
      "why": "PHASE-5 T1 (2026-08-02) \u2014 the effects declaration on every tool definition. Each of the ten files below grows by EXACTLY one `effects: [...]` line per definition it declares (plus a multi-line form where a tool has several effects, and a `nonEffects` block where a resource-shaped field is ruled inert with its reason). The count is arithmetic, not an estimate: 320 definitions, 111 of them in agent/tools.ts, and the per-file split is \u00a7T0-PINS P4's exactly. THERE IS NO WHERE ELSE TO PUT IT \u2014 that is the whole content of the task. The plan's Step 1 asked for a scan of six field names; \u00a7T0-PINS P3 measured that list at 39 of 320, missing `file_paths` (show_to_user's own field) and blind to effects with no resource argument at all (web_search's fixed host, spawn_agent's child, cliclick, `npx @plaud-ai/cli`), so the DECLARATION on the definition is the source of truth and a declaration lives where the thing it describes lives. It cannot live inside `input_schema`: that object is handed to the provider verbatim and the cache-prefix golden hashes it, so a key inside it would move the cached prefix (OR7 / roadmap #10). COMPRESSION WAS TAKEN WHERE IT EXISTED: `agent/tools.ts` is +40 and not +123, because `applyTextPagination`, `stringifyArgs` and `coerceNumberArg` (88 lines) left for `agent/tools/pagination.ts` in the same task \u2014 which is what kills six of the seven pinned dynamic-import hacks. The alternative that would have held these ceilings is not declaring the effects, which is the task."
    },
    {
      "path": "packages/server/src/microsoft/tools-office.ts",
      "from": 3446,
      "to": 3476,
      "by": "PHASE-5 T1 Step 1 (2026-08-02) \u2014 effects declared on every tool definition",
      "why": "PHASE-5 T1 (2026-08-02) \u2014 the effects declaration on every tool definition. Each of the ten files below grows by EXACTLY one `effects: [...]` line per definition it declares (plus a multi-line form where a tool has several effects, and a `nonEffects` block where a resource-shaped field is ruled inert with its reason). The count is arithmetic, not an estimate: 320 definitions, 111 of them in agent/tools.ts, and the per-file split is \u00a7T0-PINS P4's exactly. THERE IS NO WHERE ELSE TO PUT IT \u2014 that is the whole content of the task. The plan's Step 1 asked for a scan of six field names; \u00a7T0-PINS P3 measured that list at 39 of 320, missing `file_paths` (show_to_user's own field) and blind to effects with no resource argument at all (web_search's fixed host, spawn_agent's child, cliclick, `npx @plaud-ai/cli`), so the DECLARATION on the definition is the source of truth and a declaration lives where the thing it describes lives. It cannot live inside `input_schema`: that object is handed to the provider verbatim and the cache-prefix golden hashes it, so a key inside it would move the cached prefix (OR7 / roadmap #10). COMPRESSION WAS TAKEN WHERE IT EXISTED: `agent/tools.ts` is +40 and not +123, because `applyTextPagination`, `stringifyArgs` and `coerceNumberArg` (88 lines) left for `agent/tools/pagination.ts` in the same task \u2014 which is what kills six of the seven pinned dynamic-import hacks. The alternative that would have held these ceilings is not declaring the effects, which is the task."
    },
    {
      "path": "packages/server/src/microsoft/tools-read.ts",
      "from": 1604,
      "to": 1638,
      "by": "PHASE-5 T1 Step 1 (2026-08-02) \u2014 effects declared on every tool definition",
      "why": "PHASE-5 T1 (2026-08-02) \u2014 the effects declaration on every tool definition. Each of the ten files below grows by EXACTLY one `effects: [...]` line per definition it declares (plus a multi-line form where a tool has several effects, and a `nonEffects` block where a resource-shaped field is ruled inert with its reason). The count is arithmetic, not an estimate: 320 definitions, 111 of them in agent/tools.ts, and the per-file split is \u00a7T0-PINS P4's exactly. THERE IS NO WHERE ELSE TO PUT IT \u2014 that is the whole content of the task. The plan's Step 1 asked for a scan of six field names; \u00a7T0-PINS P3 measured that list at 39 of 320, missing `file_paths` (show_to_user's own field) and blind to effects with no resource argument at all (web_search's fixed host, spawn_agent's child, cliclick, `npx @plaud-ai/cli`), so the DECLARATION on the definition is the source of truth and a declaration lives where the thing it describes lives. It cannot live inside `input_schema`: that object is handed to the provider verbatim and the cache-prefix golden hashes it, so a key inside it would move the cached prefix (OR7 / roadmap #10). COMPRESSION WAS TAKEN WHERE IT EXISTED: `agent/tools.ts` is +40 and not +123, because `applyTextPagination`, `stringifyArgs` and `coerceNumberArg` (88 lines) left for `agent/tools/pagination.ts` in the same task \u2014 which is what kills six of the seven pinned dynamic-import hacks. The alternative that would have held these ceilings is not declaring the effects, which is the task."
    },
    {
      "path": "packages/server/src/google/tools-write.ts",
      "from": 1831,
      "to": 1867,
      "by": "PHASE-5 T1 Step 1 (2026-08-02) \u2014 effects declared on every tool definition",
      "why": "PHASE-5 T1 (2026-08-02) \u2014 the effects declaration on every tool definition. Each of the ten files below grows by EXACTLY one `effects: [...]` line per definition it declares (plus a multi-line form where a tool has several effects, and a `nonEffects` block where a resource-shaped field is ruled inert with its reason). The count is arithmetic, not an estimate: 320 definitions, 111 of them in agent/tools.ts, and the per-file split is \u00a7T0-PINS P4's exactly. THERE IS NO WHERE ELSE TO PUT IT \u2014 that is the whole content of the task. The plan's Step 1 asked for a scan of six field names; \u00a7T0-PINS P3 measured that list at 39 of 320, missing `file_paths` (show_to_user's own field) and blind to effects with no resource argument at all (web_search's fixed host, spawn_agent's child, cliclick, `npx @plaud-ai/cli`), so the DECLARATION on the definition is the source of truth and a declaration lives where the thing it describes lives. It cannot live inside `input_schema`: that object is handed to the provider verbatim and the cache-prefix golden hashes it, so a key inside it would move the cached prefix (OR7 / roadmap #10). COMPRESSION WAS TAKEN WHERE IT EXISTED: `agent/tools.ts` is +40 and not +123, because `applyTextPagination`, `stringifyArgs` and `coerceNumberArg` (88 lines) left for `agent/tools/pagination.ts` in the same task \u2014 which is what kills six of the seven pinned dynamic-import hacks. The alternative that would have held these ceilings is not declaring the effects, which is the task."
    },
    {
      "path": "packages/server/src/microsoft/tools-write.ts",
      "from": 2095,
      "to": 2136,
      "by": "PHASE-5 T1 Step 1 (2026-08-02) \u2014 effects declared on every tool definition",
      "why": "PHASE-5 T1 (2026-08-02) \u2014 the effects declaration on every tool definition. Each of the ten files below grows by EXACTLY one `effects: [...]` line per definition it declares (plus a multi-line form where a tool has several effects, and a `nonEffects` block where a resource-shaped field is ruled inert with its reason). The count is arithmetic, not an estimate: 320 definitions, 111 of them in agent/tools.ts, and the per-file split is \u00a7T0-PINS P4's exactly. THERE IS NO WHERE ELSE TO PUT IT \u2014 that is the whole content of the task. The plan's Step 1 asked for a scan of six field names; \u00a7T0-PINS P3 measured that list at 39 of 320, missing `file_paths` (show_to_user's own field) and blind to effects with no resource argument at all (web_search's fixed host, spawn_agent's child, cliclick, `npx @plaud-ai/cli`), so the DECLARATION on the definition is the source of truth and a declaration lives where the thing it describes lives. It cannot live inside `input_schema`: that object is handed to the provider verbatim and the cache-prefix golden hashes it, so a key inside it would move the cached prefix (OR7 / roadmap #10). COMPRESSION WAS TAKEN WHERE IT EXISTED: `agent/tools.ts` is +40 and not +123, because `applyTextPagination`, `stringifyArgs` and `coerceNumberArg` (88 lines) left for `agent/tools/pagination.ts` in the same task \u2014 which is what kills six of the seven pinned dynamic-import hacks. The alternative that would have held these ceilings is not declaring the effects, which is the task."
    },
    {
      "path": "packages/server/src/google/tools-slides.ts",
      "from": 2964,
      "to": 3022,
      "by": "PHASE-5 T1 Step 1 (2026-08-02) \u2014 effects declared on every tool definition",
      "why": "PHASE-5 T1 (2026-08-02) \u2014 the effects declaration on every tool definition. Each of the ten files below grows by EXACTLY one `effects: [...]` line per definition it declares (plus a multi-line form where a tool has several effects, and a `nonEffects` block where a resource-shaped field is ruled inert with its reason). The count is arithmetic, not an estimate: 320 definitions, 111 of them in agent/tools.ts, and the per-file split is \u00a7T0-PINS P4's exactly. THERE IS NO WHERE ELSE TO PUT IT \u2014 that is the whole content of the task. The plan's Step 1 asked for a scan of six field names; \u00a7T0-PINS P3 measured that list at 39 of 320, missing `file_paths` (show_to_user's own field) and blind to effects with no resource argument at all (web_search's fixed host, spawn_agent's child, cliclick, `npx @plaud-ai/cli`), so the DECLARATION on the definition is the source of truth and a declaration lives where the thing it describes lives. It cannot live inside `input_schema`: that object is handed to the provider verbatim and the cache-prefix golden hashes it, so a key inside it would move the cached prefix (OR7 / roadmap #10). COMPRESSION WAS TAKEN WHERE IT EXISTED: `agent/tools.ts` is +40 and not +123, because `applyTextPagination`, `stringifyArgs` and `coerceNumberArg` (88 lines) left for `agent/tools/pagination.ts` in the same task \u2014 which is what kills six of the seven pinned dynamic-import hacks. The alternative that would have held these ceilings is not declaring the effects, which is the task."
    },
    {
      "path": "packages/server/src/agent/tools.ts",
      "from": 10484,
      "to": 10524,
      "by": "PHASE-5 T1 Step 1 (2026-08-02) \u2014 effects declared on every tool definition",
      "why": "PHASE-5 T1 (2026-08-02) \u2014 the effects declaration on every tool definition. Each of the ten files below grows by EXACTLY one `effects: [...]` line per definition it declares (plus a multi-line form where a tool has several effects, and a `nonEffects` block where a resource-shaped field is ruled inert with its reason). The count is arithmetic, not an estimate: 320 definitions, 111 of them in agent/tools.ts, and the per-file split is \u00a7T0-PINS P4's exactly. THERE IS NO WHERE ELSE TO PUT IT \u2014 that is the whole content of the task. The plan's Step 1 asked for a scan of six field names; \u00a7T0-PINS P3 measured that list at 39 of 320, missing `file_paths` (show_to_user's own field) and blind to effects with no resource argument at all (web_search's fixed host, spawn_agent's child, cliclick, `npx @plaud-ai/cli`), so the DECLARATION on the definition is the source of truth and a declaration lives where the thing it describes lives. It cannot live inside `input_schema`: that object is handed to the provider verbatim and the cache-prefix golden hashes it, so a key inside it would move the cached prefix (OR7 / roadmap #10). COMPRESSION WAS TAKEN WHERE IT EXISTED: `agent/tools.ts` is +40 and not +123, because `applyTextPagination`, `stringifyArgs` and `coerceNumberArg` (88 lines) left for `agent/tools/pagination.ts` in the same task \u2014 which is what kills six of the seven pinned dynamic-import hacks. The alternative that would have held these ceilings is not declaring the effects, which is the task."
    },
    {
      "path": "packages/server/src/gateway/routes/update.ts",
      "from": 1023,
      "to": 1097,
      "by": "PHASE-5 T6B (2026-08-03) \u2014 integrity before the swap + the rollback ordering check",
      "why": "+74 into the file that owns version ordering and both download paths. THE ARTIFACT MACHINERY IS NOT HERE: hashing, manifest fetching, the verdict type and the whole what-this-buys/transition-case argument are a NEW 137-line module, update/artifact-integrity.ts, deliberately, so this file absorbs only the two call sites (5 lines each). What is left is `authorizeRollbackTarget` (~40 with its reason) and it CANNOT move without a cycle: it is defined in terms of `isValidVersionTag` and `compareVersions`, which live here and are imported from here, so a sibling module holding the gate would import update.ts while update.ts imports the gate. Version ordering is this file's job and the gate is version ordering. WHAT THE COMMENT BUYS is that neither arm reads as gratuitous: the SHAPE arm looks like belt-and-braces until you know the tag was interpolated into a GitHub API URL unvalidated, and the ORDER arm looks like a plain refusal until you know it redirects to `applyUpdate` and that the watchdog's automatic rollback does not come through this route at all \u2014 a reader who did not know the second could 'simplify' the gate away believing it blocks recovery. The alternative that would have held 1023 is not verifying the bytes before rsync --delete over a running install, which is the task."
    },
    {
      "path": "packages/server/src/agent/v2/loop.ts",
      "from": 9750,
      "to": 9774,
      "by": "PHASE-5 T6B (2026-08-03) \u2014 the credential read point (RULING P5-R11)",
      "why": "+24, and 21 of them are the reason at two sites that read as defects without it. The owner's requirement is that a stored credential must not sit in the clear at rest AND must stay usable; only the first half was holding, because assembly rebuilds the model's array from the STORED rows every tool-loop iteration, so from iteration 2 the agent read its own prior call with the placeholder in it and copied that forward (reproduced at HEAD, digest-compared). THE CODE IS TWO LINES: one import, and `messages: hydrateCredentialsInMessages(agentId, messages)` at the callModel call \u2014 plus the tool-result row now storing the placeholder instead of the value. THE MECHANISM IS ELSEWHERE ALREADY: every rule, every property and the whole rationale live in `credentials/secret-values.ts`, which is where they belong and where they were deliberately put to keep this file's growth to the call itself. What the 21 comment lines buy is that neither site can be 'tidied' by a later reader: at the callModel line, moving the call one step earlier (into assembly, where it looks more natural) would put real credential values into the context receipt, the dev instruments and the assembled-context golden \u2014 the OR7/#10 breach the seam exists to avoid; at the tool-result line, the previous release stored that row in the clear ON PURPOSE and said so, so deleting the redaction 'back to how it was' is a one-line regression of the exact hole this closes. The alternative that would have held 9750 is not putting the value back where the agent reads it, which is the task."
    },
    {
      "path": "packages/server/src/agent/v2/loop.ts",
      "from": 9548,
      "to": 9608,
      "by": "PHASE-4 T3 (2026-08-02) \u2014 the ordered steer queue",
      "why": "The single `pendingNudge` slot dies and 27 steer sites re-point onto an ordered queue with a declared precedence table. +60 lines, and the shape of the growth is the point: (a) the DELIVERY CONFIRMATION block at the receipt, ~9 lines, which is the whole 'delivered-to-model only on real assembly inclusion' requirement \u2014 it reads `collectMessageLaneIds(messages)` off the array the provider is handed and marks the entry delivered only if the lane is actually there, where the old code cleared a string on `injectRegistryMessage` returning true (pushed, not delivered); (b) the drain's else-branch, which counts a refused push so an un-injectable entry cannot block the queue behind it forever; (c) one-line `advance(state, {flag, pendingNudge})` calls becoming named `enqueueSteer({floor, content, atLoop})` calls the formatter wraps. COMPRESSION WAS TAKEN FIRST: six comment blocks folded (9614 -> 9608), and 22 latch-boolean declarations left `state.ts` in the same change (703 -> 578, a net -125 across the pair). The alternative that would have held 9548 is deleting the confirmation or the per-site floor ids, which are the task."
    },
    {
      "path": "packages/server/src/memory/lanes.ts",
      "from": 703,
      "to": 711,
      "by": "PHASE-4 T1 Step 2b (2026-08-02)",
      "why": "`LaneStatus` gains a fifth arm, `failed`. The requirement was written two lines above the defect and the code was violating it: `lanes.ts:572-574` says 'the briefing did not exist' and 'the briefing was dropped' are different facts the receipt must tell apart (research 06 s8) \u2014 and a lane whose `render` THREW was recorded byte-identically to a lane with nothing to say, because `assembler.ts` caught the throw, logged it, and pushed `render: null`. A log is not a receipt, and this one rotates. +8 lines buys: the arm, a `renderError` field on the candidate, and a branch that puts the error text into the grant's own `reason` so the reader never has to go and find the log line. Compression was taken first (two comment blocks folded). The alternative that would have held 703 was leaving the fact in the log, which is the defect."
    },
    {
      "path": "packages/server/src/agent/v2/outbound.ts",
      "from": 455,
      "to": 462,
      "by": "PHASE-4 T1 cluster 2 (2026-08-02)",
      "why": "The delivery boundary answers `LedgerOutcome` instead of `string | null`, and that null meant THREE different things at once: this crossing folded into the row the scope already owns (correct), the owner-close work row does not exist (a refusal), or the ledger write THREW and the outbound has no record at all (the failure `work.done` depends on not happening). +7 lines buys the three names: the import, the `no_change` fold return with the two-line argument for why a fold is not a failure, the `written` binding that lets `recordAtDoor` return the outcome AND keep the scope's id, and the note recording why `recordDashboardDelivery` deliberately keeps `string | null` (its early returns answer a different question). Compression was taken first \u2014 the file is +7 after folding two comment blocks and re-homing the readers onto `deliveries.ts`, which paid for the import lines in `gateway/routes/tracker.ts` and kept `a2a-transport.ts` at its own ceiling exactly. The alternative that would have held 455 was moving `recordDashboardDelivery` or `recordHeld` out of the scope module, i.e. relocation to satisfy a number, which is the move the gate\u2019s own author warned about. Growth at this boundary is DECLARED by this phase\u2019s Global Constraints: `Outcome<T>` is an addition by design and the phase buys truthfulness, not smallness."
    },
    {
      "path": "packages/server/src/work/store.ts",
      "from": 590,
      "to": 1145,
      "by": "PHASE-2 T4 (2026-07-28)",
      "why": "The fan-out join is +551 lines of the ONE writer, and it cannot live anywhere else: `single-writer-conformance.test.ts` keeps an EMPTY allowlist for `work`/`work_events`/`adjudications`, so a second module holding `openDelegationJoin`/`landPiece` fails the walk, and the only way to make it pass would be the move the walk exists to refuse. What it BUYS is a bigger cut elsewhere in the same commit: `a2a-transport.ts` 2037 -> 1773 (-264) and `loop.ts` 9336 -> 9341 net after the delegation rewrite, with the park string machine gone entirely. T3's report flagged this exact growth and asked, at T9, whether the walk should learn about a `work/` DIRECTORY rather than a file; that question is now overdue and is the real fix."
    },
    {
      "path": "packages/server/src/services/imessage-bridge.ts",
      "from": 2230,
      "to": 2300,
      "by": "PHASE-2 T5 (2026-07-28)",
      "why": "THE iMESSAGE DOOR, plus its two attachment siblings and the two platform sends that live in this file (sendAlert, the busy-ack). This is the largest single raise of the task and it is the one that buys the most: this file is the transport EVERY iMessage in the product passes through \u2014 the auto-route, the imessage_send tool, alerts, the busy-ack, the first-run welcome, the healer's approval prompt, the A2A owner hand-back \u2014 and before it, only the first two produced a ledger row and only when the caller had already decided the send worked. Recording here closes six of PINNED s8's unrecorded paths at one physical point instead of six. The two attachment siblings are wrapped for a named reason: sendIMessageWithAttachment calls sendIMessage for the caption, so without one scope over the pair every captioned file would have written TWO rows for one thing the person received."
    },
    {
      "path": "packages/server/src/agent/v2/loop.ts",
      "from": 9341,
      "to": 9389,
      "by": "PHASE-2 T5 (2026-07-28)",
      "why": "The engine's ten evidence-writing sites become identity DECLARATIONS. persistRoutingMarker lost its delivery parameter entirely (a net deletion inside this file) and each auto-route / engine-ack branch gained a `withOutboundAsync` wrapper around the send it was already making. The scope wrappers cost more lines than the argument objects they replaced, and what they buy is that the loop can no longer claim a delivery: the row is written by the transport from what it observed, which is what makes `outcome='failed'` expressible for the first time."
    },
    {
      "path": "packages/server/src/work/store.ts",
      "from": 1141,
      "to": 1175,
      "by": "PHASE-2 T5 (2026-07-28)",
      "why": "The boot reconciliation's THIRD outcome, which `index.ts` named as owed to T5 in its own comment. Deliveries are universal now, so `was this ask answered` is a recorded edge rather than an inference, and an ask whose claiming turn DELIVERED and was then killed is closed pointing at that delivery instead of being re-answered (re-serving a question the person already had answered) or held forever. Still the ONE writer of `work.state`; the single-writer walk's allowlist is still EMPTY. The `work/` DIRECTORY question T3 raised and T4 repeated is now three tasks old and belongs to T9."
    },
    {
      "path": "packages/server/src/agent/tools.ts",
      "from": 10611,
      "to": 10640,
      "by": "PHASE-2 T5 (2026-07-28)",
      "why": "ONE outbound scope per send-tool call, opened at the single dispatch door every path already goes through \u2014 the same place and the same reasoning as the P6a tool-call identity that sits three lines above it. This is what closes the ten unrecorded send paths in ONE change instead of ten: `gmail_reply`, `gmail_forward`, `outlook_reply`, `outlook_forward`, `teams_send_message`, `teams_send_channel_message` and `voice_call` are now recorded without any of them being named anywhere. The alias-resolution tail was extracted to `dispatchResolved` so the scope can wrap it; that extraction is most of the count."
    },
    {
      "path": "packages/server/src/agent/a2a-transport.ts",
      "from": 1773,
      "to": 1799,
      "by": "PHASE-2 T5 (2026-07-28)",
      "why": "The T4/T5 collision, resolved by ROUTING rather than duplicating. T4 recorded the join relay with its own `recordDelivery` call; T5 puts a recorder on the dashboard, iMessage, SMS and provider doors that same relay pushes through, so leaving both would have written TWO rows for one relay. The relay now declares its identity once and every door it crosses folds into that single row. `recordPieceDelivery` is routed through the same door recorder for the same reason. Against this: the file is still 238 lines below where T4 found it (2037 at the phase base)."
    },
    {
      "path": "packages/server/src/voice/voice-ws.ts",
      "from": 2277,
      "to": 2299,
      "by": "PHASE-2 T5 (2026-07-28)",
      "why": "THE BROWSER SPEECH-OUT DOOR, on both engine tiers (OR3 keeps both, so both are instrumented). Research 03 measured voice as recording NOTHING on either path. One row per BURST rather than per clause \u2014 the clause queue is how one reply is transported, not several replies \u2014 recorded where synthesis actually starts, so a turn whose text sanitizes to nothing speakable never claims a delivery, and corrected to `failed` if the stream dies mid-reply."
    },
    {
      "path": "packages/server/src/twilio/call-session.ts",
      "from": 873,
      "to": 884,
      "by": "PHASE-2 T5 (2026-07-28)",
      "why": "THE PHONE SPEECH-OUT DOOR. Research 03 named `every spoken utterance in call-session.ts` among the paths recording nothing. Inside the outbound scope the auto-route declares, the sentences of one reply fold into ONE row; outside a scope (the call's own opener) an utterance stands as its own row."
    },
    {
      "path": "packages/server/src/healer/approval-routing.ts",
      "from": 490,
      "to": 496,
      "by": "PHASE-2 T5 (2026-07-28)",
      "why": "The healer's approval prompt reaches the owner's phone and recorded nothing (research 03 names `approval-routing:473`). Six lines: it declares itself a PLATFORM outbound rather than an agent's, because the healer speaks for the box."
    },
    {
      "path": "packages/server/src/index.ts",
      "from": 1265,
      "to": 1267,
      "by": "PHASE-2 T5 (2026-07-28)",
      "why": "Two lines: the boot crash-reconciliation log now reports the third outcome it can produce. The comment above it stops saying T5 owes this rekey, because T5 landed it."
    },
    {
      "path": "packages/server/src/agent/pending-attachments.ts",
      "from": 236,
      "to": 267,
      "by": "PHASE-2 T5 (2026-07-28)",
      "why": "Not a raise \u2014 a first PIN, at the count the growth detector caught. `linkArtifactsToDelivery` lands here because this module is the sole owner of `turn_artifacts` writes, and the Phase-1 exit named `turn_artifacts.delivery_id` as a Phase-2 debt that must not be closed half-way. Putting the write anywhere else would give that table a second writer to pay for a column, which is the disease. Pinned decrease-only from here."
    },
    {
      "path": "packages/server/src/agent/v2/outbound.ts",
      "from": 0,
      "to": 455,
      "by": "PHASE-2 T5 (2026-07-28)",
      "why": "A NEW file above the 400-line cap, pinned deliberately rather than split. What it holds is ONE mechanism with no seam in it: a send declares an identity, the transport it passes through records the outcome, and the receipt writer links itself to that same row \u2014 three parties to one scope, and splitting them puts an import cycle between the scope and its own doors. Roughly half the count is the reasoning: which clause of the dashboard predicate exists for which observed defect, why `held` needs a named entry point instead of a door, why a failure downgrades and a success never upgrades. That text is the reason the next reader can tell an honest ledger from a convenient one, and stripping it to reach 400 would be the arithmetic-over-functionality move the owner ruled out on 2026-07-26. Pinned decrease-only from here; the honest shrink is PHASE-4's, when OR2 removes the engine-ack lane this file currently has to describe."
    },
    {
      "path": "packages/server/src/agent/v2/loop.ts",
      "from": 9389,
      "to": 9404,
      "by": "PHASE-2 T5 fix (2026-07-28)",
      "why": "+15 for the receipt-ordering fix in `25d83d4`. The iMessage and SMS auto-routes wrote their C26 receipt ONE STATEMENT below the outbound scope that had just recorded the send, so `deliveries.receipt_id` read 0 of 119 on the live ledger and would have shipped as the written-only column the Phase-1 exit named as the failure mode to avoid. Both receipts moved inside their scope callback, which costs a closure and the paragraph explaining why the placement is load-bearing. Caught by counting rows on the box, not by reading the diff \u2014 the comment says so, because the next person to move one of these lines needs to know what it breaks."
    },
    {
      "path": "packages/server/src/agent/v2/loop.ts",
      "from": 9404,
      "to": 9521,
      "by": "PHASE-2 T6 (2026-07-28)",
      "why": "PHASE-2 T6's five in-loop rekeys, net +117 after a trim pass that moved the reasoning to the module it belongs to. What LANDED here cannot live elsewhere: these are gates INSIDE the turn, and each one now reads a record where it used to compute an answer for itself \u2014 the redundant-closeout floor (the prose classifier is DELETED and the authority is the delivery ledger), the silent-closeout owe-filter, the turn-record's FIRST READER at finalize (R-0: `finalizeTurn` has always written the true outcome and NOTHING read it), the waiting-on-owner disposition and its reopen edge, and the settled read collapsing from an array-build to one query. What it BUYS, measured in the same task: `classifiers/output.ts` -24 (the twenty-phrase CLOSEOUT regex and its two consts, gone), `memory/directive.ts` -4 net on the selector with R-2 closed (a four-line JSON-shape probe becomes `answer_message_id IS NULL`), one dead flag stripped, and four hand-rolled copies of the answered-edge read replaced by calls. The line count is the wrong meter for this one and the plan says so (#7): what to read is the demolition ledger."
    },
    {
      "path": "packages/server/src/work/store.ts",
      "from": 1175,
      "to": 1229,
      "by": "PHASE-2 T6 (2026-07-28)",
      "why": "+54 for `abandonUnservableAsks()` \u2014 requirement 2b's THIRD cause of `abandoned`, and it must be in this module because `transition()` is the only writer of `work.state` and the single-writer walk's Part A allowlist is EMPTY. It disposes of tickets nobody can ever serve or close (no conversation identity, or a root message that no longer exists); 74 such rows on the dev box got their one-shot honest disposition at boot, 25 identity-less and 49 gone-root, each with its reason on its own `work_events` row. THIS IS THE FOURTH CONSECUTIVE TASK TO RAISE THIS FILE (T3 590->1145, T4, T5 1175, now 1229). The real fix is the question T3 asked and T4 and T5 repeated: should the single-module walk learn about a `work/` DIRECTORY? T9 owns that ruling and it is now four tasks overdue."
    },
    {
      "path": "packages/server/src/memory/message-store.ts",
      "from": 842,
      "to": 868,
      "by": "PHASE-2 T6 (2026-07-28)",
      "why": "+26, all of it ONE structural clause and the measurement that justifies it. `isOwnerAsk` now requires the row to carry a CHANNEL, because OR4 says channel/sender/trust/lane are stamped at INGEST and a `role='user'` row without one did not come through a door \u2014 it is the platform writing to itself in the second person. Three producers were doing exactly that (the PM's own situation report, the [ENGINE RENAME REQUEST], a spawned agent's kickoff), each opening an owner ask ticket nobody could ever serve, and the PM was being told THE OWNER was talking to it. The comment carries the query and its result because the evidence is the whole argument: every genuine ask on this box carries channel+sender+meta+conversation, every stuck one carries none of the four."
    },
    {
      "path": "packages/server/src/index.ts",
      "from": 1267,
      "to": 1279,
      "by": "PHASE-2 T6 (2026-07-28)",
      "why": "+12: the boot sweep that gives unservable ask tickets an honest terminal state, placed beside the crash reconciliation it belongs with (both answer 'what did a restart inherit that nobody can finish'). Twelve lines and a `try` \u2014 there is no smaller honest form."
    },
    {
      "path": "packages/server/src/agent/a2a-transport.ts",
      "from": 1799,
      "to": 1805,
      "by": "PHASE-2 T6 (2026-07-28)",
      "why": "+6 in `resolveCompilePendingJoins`, which T4's report named as owed to T6 BY NAME. The site stops holding its own reading of the answered edge and calls the one reader, which also means it now sees the ticket's `result_delivery_id` before the mig-113 stamp \u2014 strictly more evidence for the same decision. The +6 is the receipt appearing in the settle's reason string plus the two-line explanation of why `done` still is not asserted here."
    },
    {
      "path": "packages/server/src/agent/v2/answered-edge.ts",
      "from": 0,
      "to": 487,
      "by": "PHASE-2 T6 (2026-07-28)",
      "why": "NEW FILE, above the 400-line cap, and the addition is meant to be argued with. It is THE ANSWERED EDGE: the one place this tree answers 'has the person heard from us', replacing seven mechanisms that each answered it for themselves (a twenty-phrase prose classifier, two turn-local booleans, a legacy status scan joined by hand to a message column, an array the loop had to BUILD to learn a boolean, a loop-local variable set at four sites, `conv_key IS NULL`, `swept_at`) plus the turn record's own `exit_reason`, which finalizeTurn has always written and nothing read (R-0). Roughly two thirds of it is the argument, the measurements and the requirement-preserved lines \u2014 deliberately, because the danger with this particular collapse is not that it is wrong today but that somebody loosens a key later without knowing which incident bought it. Splitting it would put the answer-receipt read in one file and the closeout-owe read in another, which is the exact split requirement 2d exists to end."
    },
    {
      "path": "packages/server/src/work/store.ts",
      "from": 1229,
      "to": 1460,
      "by": "PHASE-2 T7 (2026-07-29)",
      "why": "The commitment half of requirement 4a \u2014 `openCommitment` / `resolveCommitment` / `dismissCommitment` and the three obligation reads \u2014 and it cannot live anywhere else for the same structural reason T4's raise gives: `single-writer-conformance.test.ts` PART A holds an EMPTY allowlist for `work`, so a second module writing commitment rows fails the walk. What this raise BUYS, measured in the same commit: `memory/open-loops.ts` is DELETED ENTIRELY (623 lines \u2014 the fenced-section parser, the Jaccard/coverage matcher, the store-contradiction regex guard, the party-attribution map and both renderers), its unit test goes with it (321 lines), and the `open_loops` table is dropped by migration 136. The two renderers come back as `work/obligations.ts`, a READ-ONLY module of 118 lines. The T3/T4 question stands and is now overdue: the walk should learn about a `work/` DIRECTORY rather than a single file (T9)."
    },
    {
      "path": "packages/server/src/agent/tools.ts",
      "from": 10640,
      "to": 10735,
      "by": "PHASE-2 T7 (2026-07-29)",
      "why": "`loop_resolve` (one verb, 18 lines of definition + 11 of dispatch) becomes `commitment_open` + `commitment_resolve` (two verbs, 37 + 68). The extra verb is the point of the task and not an accident: the deleted module had a RESOLUTION verb only, because a PARSER created the rows. Delete the parser without adding a creation door and the capability is deleted, not replaced \u2014 so the requirement-preserved line ('a promise the agent makes is tracked until it is delivered or dropped') would have been a lie. The dispatch is longer than the one it replaces because it refuses in three named ways the old one could not: outside a turn, on an id that does not resolve, and \u2014 the load-bearing one \u2014 on `kept` with no delivery recorded for the turn. PHASE-2 T8 absorbs both verbs into `work_open`/`work_close_request` with alias rows; `tools/aliases.ts:76-86` is the mechanism and it already exists."
    },
    {
      "path": "packages/server/src/agent/v2/loop.ts",
      "from": 9521,
      "to": 9530,
      "by": "PHASE-2 T7 (2026-07-29)",
      "why": "+6 lines at ONE site: the per-turn OPEN LOOPS injection becomes the OPEN WORK injection, and the added lines are the comment recording why the block is keyed on `conversation_id` rather than on `conv_key` (the deleted block compared conv_key strings \u2014 the column that also carried the claim token and the park sigils, so a parked row changed which party its items belonged to). The call itself is one line for one line."
    },
    {
      "path": "packages/server/src/tracker/tools.ts",
      "from": 4229,
      "to": 4342,
      "by": "PHASE-2 T8b (2026-07-29)",
      "why": "PHASE-2 T8b: the tracker's storage moved onto `work`. An ungated one-line `UPDATE legacy_tasks SET status = ?` becomes a `setTrackerStatus(...)` call that states its actor, its reason and what it points at, and a bare `SELECT * FROM legacy_tasks` becomes an explicit projection off the spine. Those are the lines. What the whole set BUYS is stated once rather than per file: `updateTask`'s status branch \u2014 PINNED s13's route R2, a state change with no gate, no reason, no event and no actor, with eleven call sites \u2014 is DELETED, the 49-site legacy-writer burn-down is at ZERO, and `legacy_tasks`/`legacy_projects` have no production reader or writer left. The two tables and their four side tables all die at T10, which is where these counts come back down."
    },
    {
      "path": "packages/server/src/scheduler/runner.ts",
      "from": 1613,
      "to": 1655,
      "by": "PHASE-2 T8b (2026-07-29)",
      "why": "PHASE-2 T8b: the tracker's storage moved onto `work`. An ungated one-line `UPDATE legacy_tasks SET status = ?` becomes a `setTrackerStatus(...)` call that states its actor, its reason and what it points at, and a bare `SELECT * FROM legacy_tasks` becomes an explicit projection off the spine. Those are the lines. What the whole set BUYS is stated once rather than per file: `updateTask`'s status branch \u2014 PINNED s13's route R2, a state change with no gate, no reason, no event and no actor, with eleven call sites \u2014 is DELETED, the 49-site legacy-writer burn-down is at ZERO, and `legacy_tasks`/`legacy_projects` have no production reader or writer left. The two tables and their four side tables all die at T10, which is where these counts come back down."
    },
    {
      "path": "packages/server/src/tracker/pm-agent.ts",
      "from": 1929,
      "to": 1957,
      "by": "PHASE-2 T8b (2026-07-29)",
      "why": "PHASE-2 T8b: the tracker's storage moved onto `work`. An ungated one-line `UPDATE legacy_tasks SET status = ?` becomes a `setTrackerStatus(...)` call that states its actor, its reason and what it points at, and a bare `SELECT * FROM legacy_tasks` becomes an explicit projection off the spine. Those are the lines. What the whole set BUYS is stated once rather than per file: `updateTask`'s status branch \u2014 PINNED s13's route R2, a state change with no gate, no reason, no event and no actor, with eleven call sites \u2014 is DELETED, the 49-site legacy-writer burn-down is at ZERO, and `legacy_tasks`/`legacy_projects` have no production reader or writer left. The two tables and their four side tables all die at T10, which is where these counts come back down."
    },
    {
      "path": "packages/server/src/agent/v2/loop.ts",
      "from": 9530,
      "to": 9556,
      "by": "PHASE-2 T8b (2026-07-29)",
      "why": "PHASE-2 T8b: the tracker's storage moved onto `work`. An ungated one-line `UPDATE legacy_tasks SET status = ?` becomes a `setTrackerStatus(...)` call that states its actor, its reason and what it points at, and a bare `SELECT * FROM legacy_tasks` becomes an explicit projection off the spine. Those are the lines. What the whole set BUYS is stated once rather than per file: `updateTask`'s status branch \u2014 PINNED s13's route R2, a state change with no gate, no reason, no event and no actor, with eleven call sites \u2014 is DELETED, the 49-site legacy-writer burn-down is at ZERO, and `legacy_tasks`/`legacy_projects` have no production reader or writer left. The two tables and their four side tables all die at T10, which is where these counts come back down."
    },
    {
      "path": "packages/server/src/agent/spawner.ts",
      "from": 1335,
      "to": 1359,
      "by": "PHASE-2 T8b (2026-07-29)",
      "why": "PHASE-2 T8b: the tracker's storage moved onto `work`. An ungated one-line `UPDATE legacy_tasks SET status = ?` becomes a `setTrackerStatus(...)` call that states its actor, its reason and what it points at, and a bare `SELECT * FROM legacy_tasks` becomes an explicit projection off the spine. Those are the lines. What the whole set BUYS is stated once rather than per file: `updateTask`'s status branch \u2014 PINNED s13's route R2, a state change with no gate, no reason, no event and no actor, with eleven call sites \u2014 is DELETED, the 49-site legacy-writer burn-down is at ZERO, and `legacy_tasks`/`legacy_projects` have no production reader or writer left. The two tables and their four side tables all die at T10, which is where these counts come back down."
    },
    {
      "path": "packages/server/src/agent/v2/answered-edge.ts",
      "from": 487,
      "to": 496,
      "by": "PHASE-2 T8b (2026-07-29)",
      "why": "PHASE-2 T8b: the tracker's storage moved onto `work`. An ungated one-line `UPDATE legacy_tasks SET status = ?` becomes a `setTrackerStatus(...)` call that states its actor, its reason and what it points at, and a bare `SELECT * FROM legacy_tasks` becomes an explicit projection off the spine. Those are the lines. What the whole set BUYS is stated once rather than per file: `updateTask`'s status branch \u2014 PINNED s13's route R2, a state change with no gate, no reason, no event and no actor, with eleven call sites \u2014 is DELETED, the 49-site legacy-writer burn-down is at ZERO, and `legacy_tasks`/`legacy_projects` have no production reader or writer left. The two tables and their four side tables all die at T10, which is where these counts come back down."
    },
    {
      "path": "packages/server/src/gateway/routes/memory.ts",
      "from": 627,
      "to": 635,
      "by": "PHASE-2 T8b (2026-07-29)",
      "why": "PHASE-2 T8b: the tracker's storage moved onto `work`. An ungated one-line `UPDATE legacy_tasks SET status = ?` becomes a `setTrackerStatus(...)` call that states its actor, its reason and what it points at, and a bare `SELECT * FROM legacy_tasks` becomes an explicit projection off the spine. Those are the lines. What the whole set BUYS is stated once rather than per file: `updateTask`'s status branch \u2014 PINNED s13's route R2, a state change with no gate, no reason, no event and no actor, with eleven call sites \u2014 is DELETED, the 49-site legacy-writer burn-down is at ZERO, and `legacy_tasks`/`legacy_projects` have no production reader or writer left. The two tables and their four side tables all die at T10, which is where these counts come back down."
    },
    {
      "path": "packages/server/src/agent/tools.ts",
      "from": 10735,
      "to": 10742,
      "by": "PHASE-2 T8b (2026-07-29)",
      "why": "PHASE-2 T8b: the tracker's storage moved onto `work`. An ungated one-line `UPDATE legacy_tasks SET status = ?` becomes a `setTrackerStatus(...)` call that states its actor, its reason and what it points at, and a bare `SELECT * FROM legacy_tasks` becomes an explicit projection off the spine. Those are the lines. What the whole set BUYS is stated once rather than per file: `updateTask`'s status branch \u2014 PINNED s13's route R2, a state change with no gate, no reason, no event and no actor, with eleven call sites \u2014 is DELETED, the 49-site legacy-writer burn-down is at ZERO, and `legacy_tasks`/`legacy_projects` have no production reader or writer left. The two tables and their four side tables all die at T10, which is where these counts come back down."
    },
    {
      "path": "packages/server/src/memory/assembler.ts",
      "from": 2564,
      "to": 2570,
      "by": "PHASE-2 T8b (2026-07-29)",
      "why": "PHASE-2 T8b: the tracker's storage moved onto `work`. An ungated one-line `UPDATE legacy_tasks SET status = ?` becomes a `setTrackerStatus(...)` call that states its actor, its reason and what it points at, and a bare `SELECT * FROM legacy_tasks` becomes an explicit projection off the spine. Those are the lines. What the whole set BUYS is stated once rather than per file: `updateTask`'s status branch \u2014 PINNED s13's route R2, a state change with no gate, no reason, no event and no actor, with eleven call sites \u2014 is DELETED, the 49-site legacy-writer burn-down is at ZERO, and `legacy_tasks`/`legacy_projects` have no production reader or writer left. The two tables and their four side tables all die at T10, which is where these counts come back down."
    },
    {
      "path": "packages/server/src/agent/runtime.ts",
      "from": 1339,
      "to": 1340,
      "by": "PHASE-2 T8b (2026-07-29)",
      "why": "PHASE-2 T8b: the tracker's storage moved onto `work`. An ungated one-line `UPDATE legacy_tasks SET status = ?` becomes a `setTrackerStatus(...)` call that states its actor, its reason and what it points at, and a bare `SELECT * FROM legacy_tasks` becomes an explicit projection off the spine. Those are the lines. What the whole set BUYS is stated once rather than per file: `updateTask`'s status branch \u2014 PINNED s13's route R2, a state change with no gate, no reason, no event and no actor, with eleven call sites \u2014 is DELETED, the 49-site legacy-writer burn-down is at ZERO, and `legacy_tasks`/`legacy_projects` have no production reader or writer left. The two tables and their four side tables all die at T10, which is where these counts come back down."
    },
    {
      "path": "packages/server/src/agent/v2/counterparty.ts",
      "from": 890,
      "to": 891,
      "by": "PHASE-2 T8b (2026-07-29)",
      "why": "PHASE-2 T8b: the tracker's storage moved onto `work`. An ungated one-line `UPDATE legacy_tasks SET status = ?` becomes a `setTrackerStatus(...)` call that states its actor, its reason and what it points at, and a bare `SELECT * FROM legacy_tasks` becomes an explicit projection off the spine. Those are the lines. What the whole set BUYS is stated once rather than per file: `updateTask`'s status branch \u2014 PINNED s13's route R2, a state change with no gate, no reason, no event and no actor, with eleven call sites \u2014 is DELETED, the 49-site legacy-writer burn-down is at ZERO, and `legacy_tasks`/`legacy_projects` have no production reader or writer left. The two tables and their four side tables all die at T10, which is where these counts come back down."
    },
    {
      "path": "packages/server/src/healer/diagnostic.ts",
      "from": 1035,
      "to": 1036,
      "by": "PHASE-2 T8b (2026-07-29)",
      "why": "PHASE-2 T8b: the tracker's storage moved onto `work`. An ungated one-line `UPDATE legacy_tasks SET status = ?` becomes a `setTrackerStatus(...)` call that states its actor, its reason and what it points at, and a bare `SELECT * FROM legacy_tasks` becomes an explicit projection off the spine. Those are the lines. What the whole set BUYS is stated once rather than per file: `updateTask`'s status branch \u2014 PINNED s13's route R2, a state change with no gate, no reason, no event and no actor, with eleven call sites \u2014 is DELETED, the 49-site legacy-writer burn-down is at ZERO, and `legacy_tasks`/`legacy_projects` have no production reader or writer left. The two tables and their four side tables all die at T10, which is where these counts come back down."
    },
    {
      "path": "packages/server/src/healer/injury-recovery.ts",
      "from": 809,
      "to": 810,
      "by": "PHASE-2 T8b (2026-07-29)",
      "why": "PHASE-2 T8b: the tracker's storage moved onto `work`. An ungated one-line `UPDATE legacy_tasks SET status = ?` becomes a `setTrackerStatus(...)` call that states its actor, its reason and what it points at, and a bare `SELECT * FROM legacy_tasks` becomes an explicit projection off the spine. Those are the lines. What the whole set BUYS is stated once rather than per file: `updateTask`'s status branch \u2014 PINNED s13's route R2, a state change with no gate, no reason, no event and no actor, with eleven call sites \u2014 is DELETED, the 49-site legacy-writer burn-down is at ZERO, and `legacy_tasks`/`legacy_projects` have no production reader or writer left. The two tables and their four side tables all die at T10, which is where these counts come back down."
    },
    {
      "path": "packages/server/src/work/tracker-store.ts",
      "from": 0,
      "to": 492,
      "by": "PHASE-2 T8b (2026-07-29)",
      "why": "A NEW file above the 400-line new-file cap, added to `files` deliberately so the addition is seen and argued with. It is the tracker's WRITE side on the spine: creation, the typed attribute patcher, and `setTrackerStatus` (= `transition()` with the tracker's vocabulary outside). It cannot live anywhere else: clause (a) of the single-writer property says every `work` write is under `work/`, and it is deliberately NOT in `store.ts`, because clause (b) says `work.state` UPDATEs live only there \u2014 putting the attribute patcher beside the state writer is exactly the two-mechanism shape the split exists to prevent. Its sibling `tracker-view.ts` (350) is under the cap and stays unlisted. Both shrink at T8c (the schedule and stamp families collapse) and again at T10."
    },
    {
      "path": "packages/server/src/work/store.ts",
      "from": 1573,
      "to": 1612,
      "by": "PHASE-2 T10 (2026-07-29), RULING 7 rider (b)",
      "why": "THIRTY-NINE lines, and thirty of them are the incident written down. `loudOnPieceAbort` wraps the two piece-settling transitions so a RAISE(ABORT) during piece landing logs at `error` with the child work id, its kind, root_kind, root_id, parent and agent \u2014 the facts the six-hour phantom-trigger window had none of. It belongs in this file for the reason the empty-piece refusal already states in its own comment: inside the settle function, no caller can forget it, and the two catch sites in `a2a-transport.ts` are the callers that DID forget. This number comes back down in this same task when the drops land."
    },
    {
      "path": "packages/server/src/agent/a2a-transport.ts",
      "from": 1805,
      "to": 1814,
      "by": "PHASE-2 T10 (2026-07-29), RULING 7 rider (b)",
      "why": "NINE lines at the catch site the MAP-TRIAGE bisect named. The `warn` carried the error string and nothing else; the join owns this thread, so the ordinary \"surface this deliverable\" hint is deliberately suppressed (C21) \u2014 when the landing fails here NOBODY tells the owner, which is a consequence `warn` does not describe. Now `error`, carrying the agent, the thread, the sender and the intent. Comes back down in this task when `a2a_threads` drops."
    },
    {
      "path": "packages/server/src/gateway/routes/tracker.ts",
      "from": 904,
      "to": 905,
      "by": "PHASE-2 T10G (2026-07-29), RULING 10",
      "why": "ONE line, and it is an import that DELETED a duplication. `/tasks/:id/log` restated the audit-trail kind vocabulary inline as a 6-line union type, and that copy had already drifted \u2014 it was missing the two verdict kinds the trail names once `task_log` is absorbed. The union is now `TaskLogEntryKind[]` imported from the seam that owns it, so the file is net \u22125 lines of duplication for +1 line of import. The two `task_log` statements in `/hygiene` were re-pointed in the same change (validate rates now come from `adjudications`, the verdict record itself, instead of string-matching `action_taken LIKE '%valid=true%'` on rows labelled `transition`)."
    },
    {
      "path": "packages/server/src/gateway/routes/tracker.ts",
      "from": 877,
      "to": 886,
      "by": "PHASE-2 T8b2 (2026-07-29)",
      "why": "NINE lines, and eight of them are the argument. T8b routed the dashboard's task PUT through `transition()`, which brought G7 (`done` means DELIVERED) onto that path for the first time; `setTaskStatus` returns null on a refusal and this handler ignored the null. Measured on the box before the fix: PUT {status:'complete'} on a task with no resolvable delivery answered HTTP 200 {ok:true} with the UNCHANGED task, and then wrote a `task_log` transition entry saying it moved to complete. A false receipt in the one history the owner reads is worse than the refusal it hid. The refusal now returns 409 carrying the gate's own name and detail, and the audit entry is not written for a move that did not happen. The lines cannot live elsewhere: the verdict has to be read where the response is composed. They come back down at T10 with the route's remaining legacy shims."
    },
    {
      "path": "packages/server/src/agent/v2/loop.ts",
      "from": 9556,
      "to": 9569,
      "by": "PHASE-2 T8V (2026-07-29)",
      "why": "PHASE-2 T8V: the verb collapse. 24 public tool names became six `work_*` verbs, and the 76-site measurement T8b2 stopped on is the reason these five files move: ~20 engine behaviours decided what a call MEANT by matching its NAME, and after the collapse a name no longer identifies an operation. Every one of those sites therefore rekeys to `toolOpKey(name, args)` \u2014 the operation id, derived in ONE new module (`tools/work-verbs.ts`) and nowhere else. The lines are the rekeys plus the reason each set is an OPERATION set and not a name set, which is the part a future reader needs and cannot re-derive. What the whole change BUYS, stated once: `agent/tools.ts` loses 270 lines (24 tool definitions -> 6, 24 dispatch case labels -> operation labels, one merged edit body), the retired 24 names exist only as alias rows that SWEEP-F T7 deletes, and the PM overseer gate \u2014 which a name-only match could no longer express, because `work_update` performs both the close the PM may do and the status flip it may not \u2014 states its rule as operations instead of relying on the old one-name-per-operation accident. Nineteen name-matching gates rekeyed (the close-out gate and its satisfaction, the thrash-gate clear, the multi-step floor's two counters and its disarm rule, the start-ack, the add-notes nudge, the bookkeeping nudge, the promise floor, countsAsTaskWork, transitionedThisTurn) plus three named operation sets and `argsForResult`. The helper is the load-bearing addition: a tool RESULT carries no arguments, so the four result-side gates would otherwise have no way to tell a `work_update` status flip from a `work_update` list. It cannot live in work-verbs.ts \u2014 it walks the loop's own turn state. AS-BUILT: 9569, not 9614 \u2014 the three op sets moved to tools/work-verbs.ts so they could be tested from a leaf, so the file grew by 13 lines rather than 58."
    },
    {
      "path": "packages/server/src/tracker/pm-agent.ts",
      "from": 1957,
      "to": 2020,
      "by": "PHASE-2 T8V (2026-07-29)",
      "why": "PHASE-2 T8V: the verb collapse. 24 public tool names became six `work_*` verbs, and the 76-site measurement T8b2 stopped on is the reason these five files move: ~20 engine behaviours decided what a call MEANT by matching its NAME, and after the collapse a name no longer identifies an operation. Every one of those sites therefore rekeys to `toolOpKey(name, args)` \u2014 the operation id, derived in ONE new module (`tools/work-verbs.ts`) and nowhere else. The lines are the rekeys plus the reason each set is an OPERATION set and not a name set, which is the part a future reader needs and cannot re-derive. What the whole change BUYS, stated once: `agent/tools.ts` loses 270 lines (24 tool definitions -> 6, 24 dispatch case labels -> operation labels, one merged edit body), the retired 24 names exist only as alias rows that SWEEP-F T7 deletes, and the PM overseer gate \u2014 which a name-only match could no longer express, because `work_update` performs both the close the PM may do and the status flip it may not \u2014 states its rule as operations instead of relying on the old one-name-per-operation accident. The PM overseer allow-list moved from NAMES to OPERATIONS, and gained `pmMayCall` + `PM_ONLY_WORK_OPS`. This is the site the collapse breaks hardest and the lines are the reason why: the list encodes 'the PM may close a project but may NOT flip a worker's status', and after the collapse both are `work_update`, so a name set can no longer say it. The NAME list the three tools_policy writers consume is now DERIVED from the op list, so the advertised surface and the executor gate cannot drift apart. AS-BUILT: 2020, not the 2011 first recorded \u2014 nine more lines for A NAME MATCH THE TYPESCRIPT SWEEP COULD NOT SEE. `audit_log.target` holds the tool NAME and the complete-dodges-poke smell asked `target NOT LIKE 'tracker_%'`; after the collapse no live tool carries that prefix, so the condition was TRUE for every work call and the smell would have found a 'non-tracker tool' in every closing turn and never fired again. It now matches the live prefix AND keeps the retired one, because audit_log is history."
    },
    {
      "path": "packages/shared/src/visibility.ts",
      "from": 815,
      "to": 865,
      "by": "PHASE-2 T8V (2026-07-29)",
      "why": "PHASE-2 T8V: the verb collapse. 24 public tool names became six `work_*` verbs, and the 76-site measurement T8b2 stopped on is the reason these five files move: ~20 engine behaviours decided what a call MEANT by matching its NAME, and after the collapse a name no longer identifies an operation. Every one of those sites therefore rekeys to `toolOpKey(name, args)` \u2014 the operation id, derived in ONE new module (`tools/work-verbs.ts`) and nowhere else. The lines are the rekeys plus the reason each set is an OPERATION set and not a name set, which is the part a future reader needs and cannot re-derive. What the whole change BUYS, stated once: `agent/tools.ts` loses 270 lines (24 tool definitions -> 6, 24 dispatch case labels -> operation labels, one merged edit body), the retired 24 names exist only as alias rows that SWEEP-F T7 deletes, and the PM overseer gate \u2014 which a name-only match could no longer express, because `work_update` performs both the close the PM may do and the status flip it may not \u2014 states its rule as operations instead of relying on the old one-name-per-operation accident. `classifyTool` gained an optional args parameter and a two-entry op map. Measured, not assumed: `reminder_create` had no override and no bookkeeping prefix, so the verb heuristic classified it EFFECTFUL on the token 'create' \u2014 the only one of the 24 that was not bookkeeping. Folded into `work_open` it would silently have become bookkeeping, dropping its user-visible badge and its vote in four loop gates. `@dojo/shared` cannot import the server's matcher (the dependency runs the other way), so the one discriminator that changes a class is read here, with the reason."
    },
    {
      "path": "packages/server/src/prompt/assembler.ts",
      "from": 1400,
      "to": 1404,
      "by": "PHASE-2 T8V (2026-07-29)",
      "why": "PHASE-2 T8V: the verb collapse. 24 public tool names became six `work_*` verbs, and the 76-site measurement T8b2 stopped on is the reason these five files move: ~20 engine behaviours decided what a call MEANT by matching its NAME, and after the collapse a name no longer identifies an operation. Every one of those sites therefore rekeys to `toolOpKey(name, args)` \u2014 the operation id, derived in ONE new module (`tools/work-verbs.ts`) and nowhere else. The lines are the rekeys plus the reason each set is an OPERATION set and not a name set, which is the part a future reader needs and cannot re-derive. What the whole change BUYS, stated once: `agent/tools.ts` loses 270 lines (24 tool definitions -> 6, 24 dispatch case labels -> operation labels, one merged edit body), the retired 24 names exist only as alias rows that SWEEP-F T7 deletes, and the PM overseer gate \u2014 which a name-only match could no longer express, because `work_update` performs both the close the PM may do and the status flip it may not \u2014 states its rule as operations instead of relying on the old one-name-per-operation accident. `startsWith('tracker_')` -> `isWorkVerb`, plus the three-line note that this is the same question asked of the collapsed surface."
    },
    {
      "path": "packages/server/src/agent/v2/state.ts",
      "from": 686,
      "to": 687,
      "by": "PHASE-2 T8V (2026-07-29)",
      "why": "PHASE-2 T8V: the verb collapse. 24 public tool names became six `work_*` verbs, and the 76-site measurement T8b2 stopped on is the reason these five files move: ~20 engine behaviours decided what a call MEANT by matching its NAME, and after the collapse a name no longer identifies an operation. Every one of those sites therefore rekeys to `toolOpKey(name, args)` \u2014 the operation id, derived in ONE new module (`tools/work-verbs.ts`) and nowhere else. The lines are the rekeys plus the reason each set is an OPERATION set and not a name set, which is the part a future reader needs and cannot re-derive. What the whole change BUYS, stated once: `agent/tools.ts` loses 270 lines (24 tool definitions -> 6, 24 dispatch case labels -> operation labels, one merged edit body), the retired 24 names exist only as alias rows that SWEEP-F T7 deletes, and the PM overseer gate \u2014 which a name-only match could no longer express, because `work_update` performs both the close the PM may do and the status flip it may not \u2014 states its rule as operations instead of relying on the old one-name-per-operation accident. One doc comment re-wrapped: the flag's docstring enumerated the disarming tools by their retired names."
    },
    {
      "path": "packages/server/src/tools/work-verbs.ts",
      "from": 0,
      "to": 357,
      "by": "PHASE-2 T8V (2026-07-29)",
      "why": "A NEW file past the 240-line visibility threshold, pinned deliberately so the addition is argued rather than noticed later. It is the ONE place a (tool name, arguments) pair becomes the operation it performs. It has to exist and it has to be single: the verb collapse took 24 tool names \u2014 each of which identified exactly one operation, which is why ~20 engine behaviours could decide things by matching a name \u2014 down to six verbs where the name no longer identifies anything. Every one of those behaviours now asks THIS module, so 'one matcher per marker' still holds; the alternative is the same discriminator ladder re-derived at nineteen call sites, which is the two-mechanism disease with nineteen copies. Roughly half the file is the 23-operation table with the retired verb each absorbed named on its own line, which is what makes the collapse auditable without leaving the file, and the absorb-don't-refuse ladders (#77) that carry the weak-model forgiveness the retired verbs' own handlers held. It SHRINKS at SWEEP-F T7, when the alias rows go and the shape-inference fallbacks stop having to cover old call shapes. AS-BUILT: pinned at 357, not the 256 first recorded \u2014 the six behavioural OPERATION SETS moved here from agent/v2/loop.ts so that the liveness test can hold each one against the retired names it replaced without importing loop.ts, which has a module-init circular import and hangs when imported standalone. loop.ts is 9569 for the same reason (below its raised pin, not at it)."
    },
    {
      "path": "packages/server/src/tracker/pm-agent.ts",
      "from": 2020,
      "to": 2060,
      "by": "PHASE-2 T8c item 1 (2026-07-29)",
      "why": "The ladder rekey's own docblocks. `poke_log` reads/writes are GONE from this file (getLastPoke/logPoke/clearPokeLog imports deleted; the smell detector's raw `SELECT sent_at FROM poke_log` deleted) and the rung is now a query into `work_events` via `work/poke-ladder.ts` \u2014 the mechanism SHRANK and moved out. What grew is prose: the measured evidence for the PM-voice change (why the situation report is an ENGINE event and not the owner talking) and the re-arm-is-a-marker invariant, both of which have to be readable at the site or the next editor re-introduces the DELETE. `tracker/schema.ts` fell 1051 -> 1000 in the same change."
    },
    {
      "path": "packages/server/src/memory/message-store.ts",
      "from": 868,
      "to": 900,
      "by": "PHASE-2 T8c item 1 (2026-07-29)",
      "why": "One SQL clause (`AND task_id IS NULL`) plus the measurement that justifies KEEPING the legacy assignment-notice arm instead of deleting it: 0 such rows on this dev box but 185 on the owner's real backup body, 14 of them still unclaimed. That is a roadmap #15 near-miss written down at the exact line where the next worker would otherwise read the dev box's zero as death, and it is worth more than the 28 lines it costs."
    },
    {
      "path": "packages/server/src/agent/spawner.ts",
      "from": 1359,
      "to": 1378,
      "by": "PHASE-2 T8c item 1 (2026-07-29)",
      "why": "The spawn kickoff moves from an owner-lane `role='user'` row to `insertEngineEventIfAbsent` \u2014 one call swapped for another, plus the reasoning for why a kickoff that says \"Your task: ... Begin working immediately\" is the ENGINE writing and never a person, which is the whole point of the change and is invisible from the call itself."
    },
    {
      "path": "packages/server/src/agent/v2/loop.ts",
      "from": 9569,
      "to": 9578,
      "by": "PHASE-2 T8c item 1 (2026-07-29)",
      "why": "The PM rename request joins the same events-lane door as the situation report; the note points at pm-agent.ts rather than restating the argument, which is why this is +6 and not +30."
    },
    {
      "path": "packages/server/src/agent/tools.ts",
      "from": 10472,
      "to": 10475,
      "by": "PHASE-2 T8c item 1 (2026-07-29)",
      "why": "`clearPokeLog` becomes `recordRemediation` in the reassign case \u2014 a one-line import split plus the same one-line call."
    },
    {
      "path": "packages/server/src/tracker/tools.ts",
      "from": 4342,
      "to": 4394,
      "by": "PHASE-2 T8c item 2 (2026-07-29)",
      "why": "The delivered-work backstop becomes a TESTED PREDICATE. PINNED s12 carries a STOP: grep-zero on `deliverable_shown` is FORBIDDEN until the retask backstop's requirement is a test, and it had none because the guard was inline in an async handler that also delivers over A2A, broadcasts, writes a task-log entry and may trigger a stalemate. `retaskWouldOverwriteDeliveredWork` / `retaskIsRefused` are pure functions of four facts with `retask-delivered-work-backstop.test.ts` covering BOTH branches and the `allow_regenerate` escape hatch, which is precisely the condition T10 needs to drop the column. The cost is the reasoning, at the line where the incident happened."
    },
    {
      "path": "packages/server/src/work/tracker-store.ts",
      "from": 492,
      "to": 502,
      "by": "PHASE-2 T8c item 2 (2026-07-29)",
      "why": "`stampTicket`'s six-column UPDATE with three COALESCEs becomes one `appendWorkEvent` (T8a's booked stamp collapse). The statement shrank; the note explaining why an event log makes the never-touch-updated_at rule true BY CONSTRUCTION rather than by remembering is what grew."
    },
    {
      "path": "packages/server/src/tracker/pm-agent.ts",
      "from": 2060,
      "to": 2068,
      "by": "PHASE-2 T8c item 2 (2026-07-29)",
      "why": "The `deliverable_shown` reads in the closeout brief are STRIPPED (measured: 245/245 rows on this box and 53/53 on the owner's real backup carry 0, so the brief printed a constant as if it were a fact) and the stamp projection collapses into the shared fragment. Net mechanism: less. Net lines: +8, all the measurement."
    },
    {
      "path": "packages/server/src/work/tracker-view.ts",
      "from": "(unlisted, new-file cap)",
      "to": 410,
      "by": "PHASE-2 T8c item 2 (2026-07-29)",
      "why": "NEW ENTRY (the file crossed the 400-line new-file cap). It gains section 6, the stamp collapse: six correlated-subquery expressions replacing six denormalized columns AND the four near-identical projections that named them across tracker/tools.ts, tracker/pm-agent.ts, memory/assembler.ts and agent/v2/loop.ts. Three of those four copies are deleted, which is the actual disease being removed; this file is where the one surviving definition lives, and that is the whole point of the module."
    },
    {
      "path": "packages/server/src/agent/v2/answered-edge.ts",
      "from": 496,
      "to": 520,
      "by": "PHASE-2 T8c item 2 (2026-07-29)",
      "why": "T6 concern 5 booked the mig-113 answer stamp to go PURE-LEGACY here once tasks lived on the spine. Re-derived at this HEAD the measurement REFUSED it: 586 stamped messages have no ask ticket (genuinely legacy) but 19 have a ticket, and all 19 are state='abandoned' \u2014 asks a person WAS answered on that T6's reaper later abandoned. Ranking the ticket above the stamp would flip those nineteen to 'not answered' and re-tell somebody something they already have, which is the direction T6 chose against on purpose. The union stands, and what these lines buy is that it now stands on a measurement plus a RETIREMENT CONDITION WRITTEN AS A COMMAND that T10 can run, instead of on the next worker re-deriving the same thing or, worse, doing the booked change without measuring."
    },
    {
      "path": "packages/server/src/work/tracker-view.ts",
      "from": 410,
      "to": 432,
      "by": "PHASE-2 T8c item 3 (2026-07-29)",
      "why": "`engine_scaffold` becomes the third tracker root kind \u2014 the ENGINE_AUTO_MARKER rekey. What it BUYS is measured elsewhere in the same change: `agent/v2/loop.ts` -190 and `tracker/tools.ts` -110, because the prose marker had THREE declarations (two of them hand-kept mirrors apologising for the duplication in their own comments), five LIKE/startsWith readers, and an edit-guard that silently re-prefixed the string on every description rewrite. One declaration replaces all of it."
    },
    {
      "path": "packages/server/src/tools/work-verbs.ts",
      "from": 357,
      "to": 375,
      "by": "PHASE-2 T8c item 3 (2026-07-29)",
      "why": "`OPENING_WORK_OPS` \u2014 the narrow question DECIDED D4 needs (did a work ROW come into existence) as opposed to the wide DISARMING set (did the agent tend its work). D4's contradiction was precisely that the >=6 floor borrowed the wide question and was therefore disarmed by the >3 nudge's own success. Two names and their reasoning; the alternative was the floor asking its question inline, which is how a set of tool names becomes eleven scattered predicates again."
    },
    {
      "path": "packages/server/src/agent/v2/classifiers/multistep.ts",
      "from": 468,
      "to": 484,
      "by": "PHASE-2 T8c item 3 (2026-07-29)",
      "why": "The ENGINE_AUTO_MARKER constant is REPLACED by its own tombstone: the string is gone, the paragraph explaining what the marker was, why it had three declarations and an edit-guard, and where the fact lives now stays. A deleted constant with no note is how the next worker re-invents it (#15)."
    },
    {
      "path": "packages/server/src/agent/v2/state.ts",
      "from": 687,
      "to": 703,
      "by": "PHASE-2 T8c item 3 (2026-07-29)",
      "why": "`workRowOpenedThisTurn` and the paragraph that says why it is NOT `trackerWriteThisTurn`. The distinction IS D4's root cause, and a flag whose difference from its neighbour is undocumented gets collapsed into it by the next refactor."
    },
    {
      "path": "packages/server/src/work/tracker-store.ts",
      "from": 502,
      "to": 505,
      "by": "PHASE-2 T8c item 3 (2026-07-29)",
      "why": "`rootKind` becomes an optional field on `OpenTrackerTaskInput` so the engine floor can stamp its own row. One field, one default, one comment."
    },
    {
      "path": "packages/server/src/scheduler/runner.ts",
      "from": 1655,
      "to": 1685,
      "by": "PHASE-2 T8c item 4 (2026-07-29)",
      "why": "The occurrence claim stops being a timestamp CAS and becomes a durable `work(kind='occurrence')` row, so the fire path states which sequence it is claiming, releases that sequence by name when a group has nobody free, and settles it with the run that closed it. +30 here is mostly the block comment recording WHY the shape changed: keying exactly-once on a clock is what made the defect unfindable \u2014 the caller could only obtain that clock through `msToText`, which drops milliseconds, so every schedule started from a real clock reading was permanently unclaimable and the logs read like a benign race. What it BUYS is in `work/tracker-store.ts` (\u221216, the old CAS deleted) and in the constraint: `ux_work_occurrence` has bitten on nothing since T2 wrote it, and now it is the mechanism."
    },
    {
      "path": "packages/server/src/work/tracker-view.ts",
      "from": 432,
      "to": 443,
      "by": "PHASE-2 T8c item 4 (2026-07-29)",
      "why": "`scheduleRowColumns` gains the RAW `next_run_at_ms` / `last_run_at_ms` beside the display copies, and a docblock naming the rule they exist for: `msToText` is right for rendering and lossy for comparing, so every comparison takes the raw column. Two lines of SQL and nine of the reasoning that stops the next reader from 'tidying up' the duplicate projection \u2014 which is exactly how this defect would come back. Enforced by `work/__tests__/single-writer-conformance.test.ts` PART C, with a planted-fault proof against the real file."
    },
    {
      "path": "packages/server/src/work/occurrences.ts",
      "from": "(unlisted, new-file cap)",
      "to": 252,
      "by": "PHASE-2 T8c item 4 (2026-07-29)",
      "why": "NEW ENTRY (the file crossed 60% of the 400-line new-file cap, so the decision is made here rather than argued at 400). It is one job \u2014 the occurrence claim \u2014 split from `tracker-store.ts`, which lost the old timestamp CAS in the same change. Five exported functions and roughly 110 lines of the 252 are the block comment recording WHY the shape changed, because the defect it fixes was invisible for two weeks precisely because nobody had written down that `msToText` truncates. Pinned decrease-only from here; T9's reaper and T10's `task_runs` absorption are the two tasks that should shrink it."
    },
    {
      "path": "packages/server/src/gateway/routes/tracker.ts",
      "from": 886,
      "to": 899,
      "by": "PHASE-2 T8c2 item 5 (2026-07-29)",
      "why": "The owner's own close reaches `done`. Two call sites gain `?? recordOwnerCloseReceipt(...)` (the task board and the project board), and the +13 is the block comment saying WHY the phase's central constraint is deliberately satisfied here by something that is not a send: G7 stops the ENGINE and the AGENT asserting completion on their own say-so, and the owner is the person the work was FOR. The argument itself, the enumeration of all ten `deliveries` readers, and why this is the owner's exemption and NOT the PM's, live in `agent/v2/deliveries.ts` beside the function; this file carries the pointer so a reader of the route is not left guessing."
    },
    {
      "path": "packages/server/src/work/store.ts",
      "from": 1460,
      "to": 1477,
      "by": "PHASE-2 T8c2 item 7 (2026-07-29)",
      "why": "SIXTH consecutive raise on this file, and this one is +1 line of code and +16 of the measurement that justifies it. `on_deck` was UNREACHABLE: eleven production callers ask to put work back in the queue (the scheduler's recurring cadence reset, its unfired release, the PM ladder's rung-4 auto-reset, scheduling for later, resuming a paused schedule, resolving missed runs, the healer, the owner's own drag) and every one moves from `claimed` or `paused`, neither of which carried `on_deck` in the legal table. Measured on this box: 536 recorded transitions across 8 distinct moves, ZERO into `on_deck` from any state, zero rows in it. A regression with a date \u2014 before T8b these were an ungated `updateTask({status})` and simply worked. The comment is the measurement plus the RULE (`on_deck` is a non-terminal queue state; every non-terminal state may return to it; terminal states keep their single reopen edge), because a table row with no reasoning beside it is how the omission happened the first time. The directory split T6 authorized is still unexecuted and is T9's."
    },
    {
      "path": "packages/server/src/work/store.ts",
      "from": 1477,
      "to": 1573,
      "by": "PHASE-2 T8T (2026-07-29)",
      "why": "THE SEVENTH CONSECUTIVE RAISE ON THIS FILE, and it is recorded as that rather than as a number. RULING 1's gate G9 lands here: a task/project close by anyone but an authority or a system closer is a Key-1 REQUEST, the adjudication moves to the TOP of the transaction so migration 139's BEFORE-UPDATE trigger can see it, and the whole transaction takes ONE instant instead of four Date.now() readings. Most of the count is the argument for each of those, which this project has decided belongs beside the code. The `work/` directory split the T6 acceptance authorised is still unexecuted and is now overdue: T9 owns it, and this raise is the strongest evidence for it on the record."
    },
    {
      "path": "packages/server/src/work/tracker-view.ts",
      "from": 443,
      "to": 508,
      "by": "PHASE-2 T8T (2026-07-29)",
      "why": "Key 1 became a QUERY. `pendingCloseRequestExpr` + `closeRequestDeliveryExpr` are the successors to the state `complete + complete_validated=0`, which migration 139 makes unrepresentable for a worker's own close, plus the done-only scoping clause on `validatedExpr` that keeps the PM's verdict distinct from the trigger's key. The prose is the reason the two questions are different \u2014 reading one as the other restores migration 108's demolished forgery."
    },
    {
      "path": "packages/server/src/tracker/pm-agent.ts",
      "from": 2068,
      "to": 2090,
      "by": "PHASE-2 T8T (2026-07-29)",
      "why": "The validation rung has TWO shapes now (`done`+unblessed for the engine's receipt closes, `claimed`+pending-request for a worker's own) and so do the three pending-validation counts. Without the second arm the queue reads empty while work waits in it."
    },
    {
      "path": "packages/server/src/agent/tools.ts",
      "from": 10475,
      "to": 10481,
      "by": "PHASE-2 T8T (2026-07-29)",
      "why": "Group-delete teardown re-stamped `by: 'engine'` with its evidence: the agent that owned the task has just been terminated and is not claiming anything, and under RULING 1 an agent close would leave every orphaned task open behind a request nobody will validate."
    },
    {
      "path": "packages/server/src/gateway/routes/tracker.ts",
      "from": 899,
      "to": 904,
      "by": "PHASE-2 T8T (2026-07-29)",
      "why": "The dashboard's bulk project close declares the OWNER as its authority. Without it the owner's own close would file a Key-1 request per row and the board would not move \u2014 the same defect T8b2 found on the single-task PUT, one route over."
    },
    {
      "path": "packages/server/src/agent/spawner.ts",
      "from": 1378,
      "to": 1382,
      "by": "PHASE-2 T8T (2026-07-29)",
      "why": "A dangler that INHERITS the primary task's close is the engine drawing an inference, not the agent asserting one; it now says so and points at the delivery (G6)."
    },
    {
      "path": "packages/server/src/scheduler/runner.ts",
      "from": 1685,
      "to": 1691,
      "by": "PHASE-2 T8T (2026-07-29)",
      "why": "The 5-minute unvalidated-transition escalation reads BOTH shapes of an unblessed close. Migration 139 means a worker's own close leaves the row claimed with a Key-1 request instead of moving it to done, so the done-only arm would have let exactly the rows this sweep exists for age out of its sight - silently, with every test still green."
    },
    {
      "path": "packages/server/src/work/override-requests.ts",
      "from": 0,
      "to": 246,
      "by": "PHASE-2 T8T RESUMED-2 (2026-07-29)",
      "why": "NEW FILE, pinned decrease-only at birth because the growth detector asked. It is the override queue's whole mechanism, moved off `task_override_requests` onto `work_events` + `adjudications` under orchestrator RULING 4 - and it REPLACES 19 production statements spread across four files, so the tree is one module heavier and four call-site clusters lighter. Roughly two thirds of the count is the header: what each of the five preserved requirements was, why `status` is a query rather than four columns kept in step by hand, and why a DENIAL deliberately writes no adjudication (it would make `revertCountExpr` count it, moving the stalemate threshold - a live behaviour change smuggled in as a storage change)."
    },
    {
      "path": "packages/server/src/agent/a2a-transport.ts",
      "from": 1814,
      "to": 1951,
      "by": "PHASE-2 T13 Step 0 (2026-07-30), issues-log #19",
      "why": "The SECOND TIMEOUT ARM: a join past its deadline whose parent ask has already closed. 137 lines, and the split this gate normally forces was considered and refused with a reason. `resolveJoinUnderClosedParent` needs FOUR module-private collaborators that exist nowhere else \u2014 `askRowForJoin`, `findUnlandedInboundReply`, `recordPieceDelivery` and `deliverJoinResultToOwner` (which is 100 lines of channel branching, each branch an incident with a name: C18 group-chat, C18 sender revalidation, B-2 multi-account reply-from, BUG-3 sms). Extracting the new arm means exporting all four, or passing them in as a dependency object, and either way the ONE composer for 'the join's answer reaches the owner' becomes two entry points into it. The alternative that keeps the file smaller is a second delivery path, which is the disease. What the lines buy: the owner is told when a delegated half never comes back \u2014 measured on the defect's own rows, `dueJoins` reaches 0 of the 13 parents and the new arm reaches 13. About 60 of the 137 are the incident and the two refused designs written down. THE REAL FIX IS STRUCTURAL AND HAS AN OWNER: this file is 1,951 lines and PHASE-6 owns the engine decomposition; SWEEP-A owns the surviving A2A machinery (roadmap, D2's survivors). This number should come DOWN there, not creep up again here."
    },
    {
      "path": "packages/server/src/work/store.ts",
      "from": 1612,
      "to": 1665,
      "by": "PHASE-2 T13 Step 0 (2026-07-30), issues-log #19",
      "why": "`dueJoinsUnderClosedParent`, the second arm's finder \u2014 53 lines of which 44 are the docblock, and the docblock is the point: it records WHY the population exists (the parent closes on the delivered reply and the join opens in the same turn, so every delegating turn makes one), why the first arm was NOT widened instead (`failJoinClosed` transitions the parent and takes its state as the exactly-once guard), and why the scope is `state='done'` rather than any terminal state (the first arm's own fail-closed leaves exactly the shape this finder hunts \u2014 found by the test, not by reasoning, and the duplicate-notice defect it prevented is written down where the next reader will meet it). It cannot live outside this file: `single-writer-conformance.test.ts` holds the `work` reader/writer property here by design and the same T6 ruling that let the walk learn the `work/` DIRECTORY still puts the finders beside the transitions they feed."
    },
    {
      "path": "packages/server/src/memory/message-store.ts",
      "from": 975,
      "to": 980,
      "by": "PHASE-3 T2 (2026-07-31)",
      "why": "THE WRITE-PATH FIX for the live `token_count` unit corruption, plus the record of what it removed. The +5 is a DELETION being written down: `tokenCount?: number` left the input type entirely (its only three callers passed the provider OUTPUT count for a whole turn into a column the budget spends as INPUT cost \u2014 3.24x on the assistant lane, +36.9% store-wide, measured on the live body) and the four-line note in its place is the demolition record #9 requires, naming the three call sites and where the real numbers went instead. The code got SHORTER: an escape hatch and a `??` became one derived assignment."
    },
    {
      "path": "packages/server/src/agent/model.ts",
      "from": 2655,
      "to": 2659,
      "by": "PHASE-3 T2 (2026-07-31)",
      "why": "BOTH transports stopped owning a token estimator. The /3.5 Anthropic fork and the /3 OpenAI fork (eight sites across two functions) now call the one estimator in `memory/budget.ts`; the +4 is the two-line measurement note on each, recording that /3 was labelled conservative and measured 30% over against 1,409 real calls. Nothing was added to this file that is not an import and an explanation of a removal."
    },
    {
      "path": "packages/server/src/memory/assembler.ts",
      "from": 2587,
      "to": 2590,
      "by": "PHASE-3 T2 (2026-07-31)",
      "why": "The assembler stopped deriving its own budget. `DEFAULTS.contextThreshold = 0.75` and the local `toolAndOutputReserve = 15000` are gone (one of five declarations of one number, and the one that disagreed with the compaction gate's 0.96). The +3 is `assertSystemPromptFits` and its two-line reason: a window small enough to make the budget negative used to produce a single-message context SILENTLY, with nothing logged and nothing broadcast. That is a guard being added where an absence was, which is what the ratchet should cost the most to skip."
    },
    {
      "path": "packages/server/src/costs/tracker.ts",
      "from": 531,
      "to": 541,
      "by": "PHASE-3 T2 Step 3 (2026-07-31)",
      "why": "PHASE-3 T2 Step 3 (research 06 A2). `cost_records` has always carried what the PROVIDER charged for input and never what the platform BELIEVED it would cost, so the estimator error research 06 called \"the 33% systematic error\" was a sentence nobody could re-derive \u2014 T2 had to reconstruct it from 1,459 receipt FILES joined to this table by timestamp just to pick a divisor. The +10 is one optional field, one column pair in the INSERT, and the note saying why the DIVISOR is stored beside the estimate (a 4-chars/token row and a 3.5-chars/token row are different measurements). This is a measurement being added where none existed; the ratchet is right that it is growth."
    },
    {
      "path": "packages/server/src/agent/model.ts",
      "from": 2659,
      "to": 2664,
      "by": "PHASE-3 T2 Step 3 (2026-07-31)",
      "why": "The two transports that compute an input estimate now hand it to recordCost \u2014 the Anthropic path's `inputEstimate` (the same sum its own hard cap compares against `hardInputLimit`) and the OpenAI path's post-trim `finalInputEstimate`. Recording the number that DECIDED whether history was trimmed, rather than a fresh guess taken afterwards, is the whole value; +5 lines for a pair of call arguments and the two sentences saying which estimate each is."
    },
    {
      "path": "packages/server/src/agent/runtime.ts",
      "from": 1375,
      "to": 1415,
      "by": "PHASE-3 T3 / S3 (2026-07-31)",
      "why": "THE WRITE THAT LEFT THE READ PATH HAD TO LAND SOMEWHERE, AND THIS IS ITS OWNER. `memory/assembler.ts` ran `UPDATE agents SET config` from inside its own assembly READ path to clear the one-shot A2A-preempt and Stop markers it had just rendered (`:1468`, `:1497` pre-repin). Assembly happens MANY times per turn (every tool iteration re-assembles) and in places that are not turns at all \u2014 the dev cache-prefix probe and the assembled-context golden both assemble \u2014 so the marker was consumed by whoever assembled first, not necessarily by the call that showed it to the model. The assembler now REPORTS what it consumed and `clearConsumedOneShotFlags` here writes once, in a transaction, at the turn. This file is the owner because `stopAgent` twelve lines above SETS one of the two flags: one owner per job. The +40 is that function and the paragraph explaining why it is not where it used to be. `memory/assembler.ts` is 33 lines SHORTER in the same change."
    },
    {
      "path": "packages/server/src/agent/model.ts",
      "from": 2664,
      "to": 2682,
      "by": "PHASE-3 T3 / S1 (2026-07-31)",
      "why": "THE CACHE BREAKPOINT MOVED, AND THE +18 IS MOSTLY THE MEASUREMENT THAT JUSTIFIES IT. The `cache_control` marker sat on `arr.length - 1` \u2014 the LAST tool, whichever it happened to be \u2014 while the array's membership GROWS mid-session via `load_tool_docs`. So every mid-session tool load re-wrote the cached prefix and the next call re-billed ~24.7K tokens: measured at \u00a7T0-E as roughly 13x the entire prefix growth the owner accepted on 2026-07-30, on ~1.1% of assistant rows (a floor, not a rate \u2014 this is a battery body). It now sits on the last ALWAYS-LOADED tool, which `partitionToolsForApiCall` guarantees is a deterministic head. Six lines are the partition call and the breakpoint expression; the rest is the note recording what was measured, because a reviewer who cannot see the cost cannot check the fix."
    },
    {
      "path": "packages/server/src/prompt/registry/entries.ts",
      "from": 493,
      "to": 505,
      "by": "PHASE-3 T3 / RULING P3-R1 (2026-07-31)",
      "why": "A DELETION THAT COSTS TWELVE LINES, AND THAT IS THE DEMOLITION RULE WORKING. The `msg.tracker-notif` ENTRY (11 lines) is GONE \u2014 its only injector died in `d00f270`'s sanctioned demolition and `skipWake: true`, the flag that made same-turn injection possible at all, now has zero callers whole-tree. Non-negotiable #9 requires a removal to carry its verdict and a `requirement preserved:` line naming what the mechanism encoded, and #15 requires the evidence to be POSITIVE rather than an absence \u2014 so the 23-line tombstone in its place names the surviving owner (`tracker/notify.ts:injectTaskAssignmentNotification`, which persists, broadcasts and WAKES), the two live call sites, and the grep that proves the same-turn duplicate is unreachable rather than merely unused. The alternative to the twelve lines is a deletion nobody can audit."
    },
    {
      "path": "packages/server/src/agent/v2/loop.ts",
      "from": 9578,
      "to": 9588,
      "by": "PHASE-3 T3 (2026-07-31)",
      "why": "THREE CALL SITES AND THEIR REASONS. (1) RULING P3-R1: `msg.peer-status` is INJECTED, between msg.turn-context and msg.current-time \u2014 the entry has been registered since `5cb1758` with no injection site ever, so the live peer idle/working state that commit relocated out of the cached roster has never reached a model, and the near-tail order 1850 -> 1875 -> 1900 is a preserved contract so the position is not a choice. (2) S3: the turn clears the one-shot markers the assembly reported consuming, instead of the assembler clearing them from a read. (3) S3: restart rehydration of session tool docs, once per agent per process, moved off the assembly path where it re-broke the cached tools prefix for every agent on the first assembly after any restart. Seven of the ten lines are the comments recording (1) and (3); this file is under a pre-committed >3,000-line tripwire in PHASE-6 and every line added to it is meant to hurt."
    },
    {
      "path": "packages/server/src/tools/tool-docs.ts",
      "from": 0,
      "to": 523,
      "by": "PHASE-3 T3 / S1+S3 (2026-07-31)",
      "why": "NEW ENTRY \u2014 the file crossed the 400-line new-file cap (378 -> 523) and the addition is meant to be argued with. Two things landed. S1: `partitionToolsForApiCall` splits the API tools array into a DECLARED always-loaded head and a session-loaded tail, so the head is a stable cacheable prefix and the `cache_control` breakpoint has a fixed home. The old `filterToolsForApiCall` was one `.filter()` in REGISTRY order, which is how a mid-session `load_tool_docs` could insert a tool at index 0 and invalidate the cached prefix from its first byte. S3: `rehydrateSessionToolsFromHistory`, the restart-recovery requirement lifted OUT of `memory/assembler.ts`'s read path (`:1262-1281` pre-repin) and given to the module that owns the session state, running once per agent per process instead of once per assembly. Roughly two thirds of the growth is the two block comments carrying the \u00a7T0-E measurement and the requirement-preservation record. It SHRINKS when PHASE-5's toolbox work folds the six always-loaded sets into one declaration."
    },
    {
      "path": "packages/server/src/memory/lanes.ts",
      "from": 0,
      "to": 644,
      "by": "PHASE-3 T3 (2026-07-31)",
      "why": "NEW ENTRY past the 400-line cap, argued rather than noticed later. This is THE ALLOCATOR: the lane table (id, slot, priority, min, max, truncate), the two-pass fit, the allocation report and the generated scaffolding ack. It replaces, in `memory/assembler.ts`, twelve independent admission gates consumed in BUILD order, two adds with no gate at all, an EVENTS push with neither gate nor add, and a hardcoded 576-char ack that told the model the priority order was the exact REVERSE of the one the budget spent (research 06 \u00a72). `memory/assembler.ts` fell 2,590 -> 2,557 in the same change and its 1,500-line hand-rolled budget body became a table of render/truncate pairs. Roughly 45% of this file is comment, and deliberately: the priority ladder's four non-prose rungs each carry the reason they sit where they sit, every declared cap names the \u00a7T0-B site it came from, and every post-budget reserve names the measurement it was derived from (#14 \u2014 a reserve with no derivation is a rumour). It SHRINKS at PHASE-3 T9, when the 76-site cluster's dispositions retire the limits this table currently has to carry verbatim."
    },
    {
      "path": "packages/server/src/memory/assembler.ts",
      "from": 2590,
      "to": 2622,
      "by": "PHASE-3 T3 (2026-07-31, the second commit)",
      "why": "TWO DEFECTS THE ALLOCATOR FOUND ON ITS OWN FIRST LIVE RUN, both with the measurement written down. (1) `storedRowCost` \u2014 the tail's cost expression was `msg.tokenCount ?? estimateTokens(msg.content)` and `0 ?? x` is **0**, so a row whose stored `token_count` is 0 was FREE TO CARRY. `memory/budget.ts:estimateStoredTokens` has carried the floor since T2 (\"a row that costs nothing to carry does not exist\") and the READ side never applied it; \u00a7T0-D measured 116 such rows before migration `150`, and the kit's own golden fixture writes six. It surfaced as a whole fresh tail costed at zero and therefore REJECTED. One helper, three call sites, `||` not `??`. (2) The per-turn recall query is per-TURN now instead of per-ASSEMBLY, memoised against `turnBoundary`. Measured: each tool iteration appends two rows, pushing the human row out of the 10-row window, so the query fell through to \"the newest substantive row\" \u2014 different every iteration \u2014 and both relevance-selected lanes re-selected AHEAD of the tail boundary. Message 0 went 20,324 -> 19,914 chars between iterations of one turn before; sha `85ad91c8\u2026` identical across four after. That is the K10 defect `check-message-prefix` has been red on since 2026-07-27. Both are comment-heavy because both are the kind of finding that gets re-broken by the next person who reads `??` as a null check."
    },
    {
      "path": "packages/server/src/agent/v2/loop.ts",
      "from": 9588,
      "to": 9597,
      "by": "PHASE-3 T3 (2026-07-31, the second commit)",
      "why": "+9 for ONE assignment and the paragraph that makes it re-derivable: `volatileFrom`, the index the loop's tail-append begins at, recorded on the context receipt. Without it `check-message-prefix` judged the whole array and therefore called the cache-prefix law's own prescribed shape a defect \u2014 the near-tail volatile lane is RE-EMITTED after each iteration's new rows, so the earlier array is structurally never a whole prefix of the later one. With it the gate judges the CACHEABLE REGION and goes green for the first time since 2026-07-27, and its bite is proven three ways (a byte moved inside the region -> exit 1; a byte moved in the volatile tail -> exit 0; the field absent -> the old whole-array rule and it SAYS the boundary is unknown)."
    },
    {
      "path": "packages/server/src/memory/lanes.ts",
      "from": 644,
      "to": 649,
      "by": "PHASE-3 T3 (2026-07-31, the second commit)",
      "why": "+5: the allocator must not reject a lane that RENDERED CONTENT merely because the content measured zero cost. Found live, not imagined \u2014 see the assembler's `storedRowCost` entry above: a fresh tail whose rows all carried `token_count = 0` costed 0, and `grant = min(cost, available)` then made 0 a rejection. Two clauses and their reason: the rejection is gated on `cost > 0`, and the emission filter admits `granted > 0 || cost === 0`. Both carry a regression clause in `lanes.test.ts`."
    },
    {
      "path": "packages/server/src/agent/model.ts",
      "from": 2682,
      "to": 2742,
      "by": "PHASE-3 T4 (2026-07-31)",
      "why": "THE EXIT VALIDATOR'S ONE INSERTION, plus `getModelOutputCap`. Research 06 C9 asks for one `validateAssembly()` between assembly and `callModel`, running after every mutation, the SAME for every agent type. One call site is the requirement, not a convenience: the two front-trimmers this replaces are two size authorities in this same file that drifted to different estimators, different reserves and different orphan repairs precisely because each provider branch grew its own. Most of the +60 is the comment block stating WHY the call sits two lines below the planned pin (the budget is a function of `getModelInfo`'s window and output cap) and why `AssemblyValidationError` is re-thrown while an instrument fault is swallowed. It SHRINKS at T4 Step 2b, on or after 2026-08-07: the flip commit deletes the Anthropic front-trimmer (`:2313-2361`, 49 lines) and the OpenAI one (`:1276-1344`, 69 lines) together, which is a net cut of roughly 118 lines from this file."
    },
    {
      "path": "packages/server/src/tools/tool-docs.ts",
      "from": 523,
      "to": 550,
      "by": "PHASE-3 T4 (2026-07-31)",
      "why": "`measureAgentToolPayloadTokens` \u2014 the number that made the reserve honest. It is 8 lines of code and 19 of derivation. It lives HERE because this module already owns `partitionToolsForApiCall`, i.e. the answer to 'what tools does this agent actually send', and a second module computing the payload size would be a second way to size the tools array \u2014 the exact shape this phase deletes. It measures the same expression the transport serialises rather than estimating it."
    },
    {
      "path": "packages/server/src/memory/compaction.ts",
      "from": 1267,
      "to": 1289,
      "by": "PHASE-3 T4 (2026-07-31)",
      "why": "`estimateAssembledTokens` becomes async and returns `reserveTokens`. The growth is the docstring explaining why: the assembler's reserve is now MEASURED per agent, and a gate that modelled it with a constant while the assembler measured it would be a second model of the budget \u2014 the defect T2 deleted eleven days of work ago. The re-export of `TOOL_AND_OUTPUT_RESERVE` is DELETED in the same change, with its `requirement preserved:` line; that deletion is real and is why the net is only +22."
    },
    {
      "path": "packages/server/src/memory/assembler.ts",
      "from": 2622,
      "to": 2638,
      "by": "PHASE-3 T4 (2026-07-31)",
      "why": "The policy call takes its measured inputs, and `reserveTokens` joins `AssembledContext` so the loop reads the number the assembly ACTUALLY used instead of importing a constant. +16 for the two measurements and the note recording that the old 15,000 was smaller than the primary's tool schemas alone (17,502), which is why the assembler's ceiling used to sit above the window on 32K models."
    },
    {
      "path": "packages/server/src/agent/v2/loop.ts",
      "from": 9597,
      "to": 9601,
      "by": "PHASE-3 T4 (2026-07-31)",
      "why": "+4, and three of them are the comment. `TOOL_AND_OUTPUT_RESERVE` leaves the import list and `assemblerOverheadTokens` reads `ctx.reserveTokens` \u2014 the loop stops re-deriving a number the assembler already decided. The smallest raise in this set and the one that removes a mechanism."
    },
    {
      "path": "packages/server/src/memory/assembly-validation.ts",
      "from": 0,
      "to": 561,
      "by": "PHASE-3 T4 (2026-07-31)",
      "why": "A NEW file past the 400-line cap, pinned so the addition is argued rather than noticed later \u2014 which is what the cap is for. It is 561 lines of which 169 are comment and 46 blank: 346 lines of code for research 06's whole C section (C9 the five clauses, C10 the priority-ordered repair, C11 the loud failure) plus the provider-boundary entry point and the divergence instrument. SPLITTING IT WAS CONSIDERED AND REFUSED: validate / repair / boundary are one subject \u2014 the repair re-runs the validation after every drop, and the boundary is the only caller of either \u2014 so three files would be three imports of one job, which is the disease this overhaul exists to delete. The comment weight is deliberate and load-bearing: it carries the measurement that decided the shape-vs-size line (73 real calls, 17 divergences, 0 of them size), the derivation of the detect window, and the grep recipes the day-7 flip decision is taken from. IT SHRINKS AT T4 STEP 2B ONLY IN THE SENSE THAT ITS COUNTERPARTS DO: the flip deletes ~118 lines of front-trimmer from `agent/model.ts`, which is what this file replaces."
    },
    {
      "path": "packages/server/src/memory/compaction.ts",
      "from": 1291,
      "to": 1337,
      "by": "PHASE-3 T5 Steps 1b/1c/2 (2026-08-01)",
      "why": "+46 across two changes, both of which DELETE a mechanism. (1c) The leaf summariser input is now ONE exported `buildLeafSummaryInput` instead of an inline builder here and a byte-identical `buildLeafInput` in summary-rebuild.ts - and that duplication was the dangerous kind: summary-rebuild's whole job is to REPLAY this exact transformation and ask whether the result is still contaminated, so the moment the two drifted it was measuring a summariser that did not exist. Extracting it costs the function signature plus a docstring recording what each clause is (the noise filter, the party tag, the technique/credential scrubs), each of which is an incident. (2) Three REFUSAL branches, one per generateSummary call site, because the summariser now returns {ok:false, reason} instead of a deterministic truncation of the RAW input. Each branch is four lines of code and four of reasoning, and the reasoning is the load-bearing part: skipping createLeafSummary is what leaves the messages uncompacted, and writing NO_CONVERSATION_PLACEHOLDER there instead would be an IRREVERSIBLE lie about a span the summariser never read."
    },
    {
      "path": "packages/server/src/memory/summary-rebuild.ts",
      "from": 433,
      "to": 460,
      "by": "PHASE-3 T5 Steps 1b/1c/2 (2026-08-01)",
      "why": "+27. The refusal branch (the summariser can now say no, and a refusal is a skip that leaves the row flagged for the next night), and the two SUMMARY_REBUILD_CLEAN_STOCK log lines that report the streak. The COUNTER itself is a new 69-line module, summary-clean-stock.ts, deliberately not in this file: this file is the thing being measured and the measurer should not live inside it. Note this file also SHRANK by its own buildLeafInput copy in the same change, so the net cost of Step 2 here is smaller than 27."
    },
    {
      "path": "packages/server/src/memory/summary-clean-stock.ts",
      "from": 0,
      "to": 69,
      "by": "PHASE-3 T5 Steps 1b/1c/2 (2026-08-01)",
      "why": "A NEW file, pinned at birth. It holds the number SWEEP C retires the nightly summary rebuild on, and the reason it needs to be a NUMBER rather than an impression is roadmap #15: one clean night is equally consistent with a clean stock and a broken detector. So it records three facts - how many summaries were actually examined, what was found, and the consecutive-clean streak - persisted in config so a nightly job's evidence survives a restart, and a run that scanned ZERO does not advance the streak."
    },
    {
      "path": "packages/server/src/agent/v2/turn-record.ts",
      "from": 155,
      "to": 197,
      "by": "PHASE-3 T5 rider (c) / G24 (2026-08-01)",
      "why": "+49, and PINNED DECREASE-ONLY rather than re-recorded to the growth baseline - the stronger of the two remedies, and the right one for the module that owns turn identity. The addition is `currentTurnNumber`, which REPLACES four copies of `SELECT MAX(turn_number) FROM messages WHERE agent_id = ?` in memory/assembler.ts (three readers) and memory/compaction.ts (the writer of the window those readers compare against) - PHASE-2's handed-forward grep token and T9's exit gate. The function is nine lines; the rest is the record of two measurements a later reader cannot re-derive cheaply. (1) Why MAX and not 'the open turn': 139 of 3,090 live turn rows have ended_at IS NULL because a died turn never reaches finalizeTurn, and agent kevin - the agent both prompt goldens are bound to - held an open row at turn 246 while 264 had been allocated, so that reader would have answered eighteen turns stale. (2) The shift this introduces: on 29 of 48 agents the old expression was exactly one higher, on the other 19 it was LOWER by up to two because those agents ran turns that persisted nothing; the continuity-brief window is shape-preserved because its writer moves with its readers, and the absolute stub age shifts 40 of 2,766 live tool rows (1.4%), all toward keeping content one turn longer. Deleting that record to hit a line count would leave the next worker re-deriving it from the live database."
    },
    {
      "path": "packages/server/src/memory/assembly-validation-sink.ts",
      "from": 0,
      "to": 146,
      "by": "PHASE-3 T5 Steps 1b/1c/2 (2026-08-01)",
      "why": "A NEW file from this task's opening rider, pinned at birth rather than left to drift toward the 400-line cap. It is the durable evidence store for T4's seven-day detect window, and roughly half of it is the measured retention arithmetic and the day-7 read procedure - the thing whose absence made T4's own 17 day-0 divergence lines unrecoverable within hours."
    },
    {
      "path": "packages/shared/src/markers.ts",
      "from": 0,
      "to": 286,
      "by": "PHASE-3 T5 Step 1 (2026-08-01)",
      "why": "A NEW file, PINNED DECREASE-ONLY at its birth count rather than left to drift to the 400-line cap \u2014 the stronger of the two remedies the growth detector offers, and the right one here because this file is a VOCABULARY and every future task will be tempted to add one more marker to it. It is THE marker taxonomy (research 06 E17): five families, each with an anchored and an unanchored matcher BUILT FROM ONE PREFIX ARRAY, so the two forms cannot drift apart the way summary-rebuild.ts's unanchored list and platform-noise.ts's anchored list already had. Roughly 150 of the 286 lines are the reasoning: what each family owns, why membership stays with the consumers (a New Session divider is user-visible to the display taxonomy and excluded by the summary filter - two questions), why the A2A legacy prefixes are deliberately split out of the platform half (folding them into platform-noise.ts would delete real vault memories), and the four measured drifts each matcher closes. Stripping that to hit a number would be the arithmetic-over-functionality move the owner ruled out on 2026-07-26. From here it may only shrink."
    },
    {
      "path": "packages/shared/src/origin.ts",
      "from": 235,
      "to": 246,
      "by": "PHASE-3 T5 Step 1 (2026-08-01)",
      "why": "+11 and PINNED DECREASE-ONLY, having crossed the growth detector's 240-line early-warning line. The growth is the marker-taxonomy import block and the comment recording what the three re-declarations it replaces actually cost: the A2A envelope's thread capture was correct HERE and wrong in four other files; the legacy source regex knew only AGENT MESSAGE, so GROUP BROADCAST and PM AGENT POKE fell past the agent branch; and ENGINE_PREFIXES matched with startsWith, so '[System note:' matched neither '[System:' (colon required) nor '[SYSTEM' (case-sensitive) and classified engine scaffolding as the owner's own words. Pinned rather than re-recorded because this file is the attribution authority and OR4 makes its size worth arguing about."
    },
    {
      "path": "packages/server/src/memory/summary-rebuild.ts",
      "from": 428,
      "to": 433,
      "by": "PHASE-3 T5 Step 1 (2026-08-01)",
      "why": "ONE MARKER TAXONOMY (research 06 E17/E18). Six files pay a small import + pointer cost so that five marker FAMILIES stop being spelled seven ways. What was deleted does not show up in these counts because it was deleted from OTHER files or replaced in place: two byte-identical A2A alternations one /i flag apart, three inlined hex-only envelope regexes, two re-declared fresh-read sentinels plus two inlined literals, two copies of the fresh-read extractor regex, an eight-entry hand-written [SOURCE: ...] anchor list and its unanchored near-twin. The residual growth in each file below is its IMPORT STATEMENT plus a one- or two-line pointer; the explanations live once, in packages/shared/src/markers.ts. +5: the two-line import, plus the note recording that this file's contamination list was an accidental superset in one direction (bare 'PM AGENT POKE') and a subset in the other (missing '[SOURCE: ENGINE'), and that the bare poke form is KEPT on purpose so a legacy row with no sender still counts."
    },
    {
      "path": "packages/server/src/agent/a2a-transport.ts",
      "from": 1951,
      "to": 1953,
      "by": "PHASE-3 T5 Step 1 (2026-08-01)",
      "why": "ONE MARKER TAXONOMY (research 06 E17/E18). Six files pay a small import + pointer cost so that five marker FAMILIES stop being spelled seven ways. What was deleted does not show up in these counts because it was deleted from OTHER files or replaced in place: two byte-identical A2A alternations one /i flag apart, three inlined hex-only envelope regexes, two re-declared fresh-read sentinels plus two inlined literals, two copies of the fresh-read extractor regex, an eight-entry hand-written [SOURCE: ...] anchor list and its unanchored near-twin. The residual growth in each file below is its IMPORT STATEMENT plus a one- or two-line pointer; the explanations live once, in packages/shared/src/markers.ts. +2: the import, and one line saying the 8 in threadId.slice(0, 8) is the MARKER's shape rather than this function's opinion - the readers slice the same number out of the same envelope."
    },
    {
      "path": "packages/server/src/agent/v2/loop.ts",
      "from": 9601,
      "to": 9603,
      "by": "PHASE-3 T5 Step 1 (2026-08-01)",
      "why": "ONE MARKER TAXONOMY (research 06 E17/E18). Six files pay a small import + pointer cost so that five marker FAMILIES stop being spelled seven ways. What was deleted does not show up in these counts because it was deleted from OTHER files or replaced in place: two byte-identical A2A alternations one /i flag apart, three inlined hex-only envelope regexes, two re-declared fresh-read sentinels plus two inlined literals, two copies of the fresh-read extractor regex, an eight-entry hand-written [SOURCE: ...] anchor list and its unanchored near-twin. The residual growth in each file below is its IMPORT STATEMENT plus a one- or two-line pointer; the explanations live once, in packages/shared/src/markers.ts. +2: the import, and the fresh-read extractor becoming a call. The regex it replaced was byte-identical to memory/assembler.ts's copy."
    },
    {
      "path": "packages/server/src/memory/compaction.ts",
      "from": 1289,
      "to": 1291,
      "by": "PHASE-3 T5 Step 1 (2026-08-01)",
      "why": "ONE MARKER TAXONOMY (research 06 E17/E18). Six files pay a small import + pointer cost so that five marker FAMILIES stop being spelled seven ways. What was deleted does not show up in these counts because it was deleted from OTHER files or replaced in place: two byte-identical A2A alternations one /i flag apart, three inlined hex-only envelope regexes, two re-declared fresh-read sentinels plus two inlined literals, two copies of the fresh-read extractor regex, an eight-entry hand-written [SOURCE: ...] anchor list and its unanchored near-twin. The residual growth in each file below is its IMPORT STATEMENT plus a one- or two-line pointer; the explanations live once, in packages/shared/src/markers.ts. +2: the import plus its note. This file's A2A_INBOUND_MARKER_RE carried /i and assembler.ts's identical alternation did not - research 06 section 5's seed pair. Its own comment already PROMISED the two matched; the import is what makes the promise true."
    },
    {
      "path": "packages/server/src/techniques/tools.ts",
      "from": 890,
      "to": 892,
      "by": "PHASE-3 T5 Step 1 (2026-08-01)",
      "why": "ONE MARKER TAXONOMY (research 06 E17/E18). Six files pay a small import + pointer cost so that five marker FAMILIES stop being spelled seven ways. What was deleted does not show up in these counts because it was deleted from OTHER files or replaced in place: two byte-identical A2A alternations one /i flag apart, three inlined hex-only envelope regexes, two re-declared fresh-read sentinels plus two inlined literals, two copies of the fresh-read extractor regex, an eight-entry hand-written [SOURCE: ...] anchor list and its unanchored near-twin. The residual growth in each file below is its IMPORT STATEMENT plus a one- or two-line pointer; the explanations live once, in packages/shared/src/markers.ts. +2: the re-export plus the import. This file DECLARED TECHNIQUE_FRESH_SENTINEL and was imported by nobody while four other sites re-declared or inlined the literal; its own header said not to change the sentinel without grepping for every consumer, which is the instruction that does not scale."
    },
    {
      "path": "packages/server/src/vault/tools.ts",
      "from": 608,
      "to": 609,
      "by": "PHASE-3 T5 Step 1 (2026-08-01)",
      "why": "ONE MARKER TAXONOMY (research 06 E17/E18). Six files pay a small import + pointer cost so that five marker FAMILIES stop being spelled seven ways. What was deleted does not show up in these counts because it was deleted from OTHER files or replaced in place: two byte-identical A2A alternations one /i flag apart, three inlined hex-only envelope regexes, two re-declared fresh-read sentinels plus two inlined literals, two copies of the fresh-read extractor regex, an eight-entry hand-written [SOURCE: ...] anchor list and its unanchored near-twin. The residual growth in each file below is its IMPORT STATEMENT plus a one- or two-line pointer; the explanations live once, in packages/shared/src/markers.ts. +1, and it is the import line. The inline literal it replaces was on an existing line."
    },
    {
      "path": "packages/server/src/memory/assembly-validation.ts",
      "from": 561,
      "to": 576,
      "by": "PHASE-3 T5 opening rider (2026-08-01)",
      "why": "+15, and the mechanism itself was moved OUT rather than in. T4's detect-only window is read on day 7 from this validator's own log lines, and those went to ~/.dojo/logs/dojo.log, which logger.ts:48-66 rotates at 10MB keeping ONE backup. Re-derived at ababf11 by command: dojo.log.1 held 19,081,331 bytes spanning 03:56:01Z->04:00:33Z (4m33s) and dojo.log 4,265,426 bytes spanning 04:00:33Z->04:12:15Z, i.e. 16m14s of total retained history for a SEVEN-DAY window \u2014 and T4's own 17 day-0 divergence lines already returned 0 hits across both files. The durable sink fixes that. Its whole mechanism (path, record, append, read-back, failure counter, and the day-7 read procedure) is a NEW 146-line module, assembly-validation-sink.ts, well under maxNewFileLines, precisely so this file did not absorb it: the first draft put it here and measured 692. What stays here is one import, the append call on the divergence branch (six lines, reusing the `violations` array the logger line already built), and a header block naming the sink and pointing at it. A validator whose header still told day 7 to grep a file with sixteen minutes of retention would be the defect, not the size. From here this file may only shrink."
    },
    {
      "path": "packages/server/src/memory/budget.ts",
      "from": 0,
      "to": 301,
      "by": "PHASE-3 T4 (2026-07-31)",
      "why": "PINNED DECREASE-ONLY rather than re-recorded, which is the stronger of the two remedies the growth detector offers, and the right one for this file: it is THE budget module and every future task will be tempted to add one more number to it. 237 -> 301 is the honest reserve \u2014 `OUTPUT_RESERVE_TOKENS` with its derivation (agent/model.ts:2314's own minOutputReserve, checked against 8,244 measured cost_records rows: p99.9 = 3,121) and `toolAndOutputReserve()`, which REPLACES the `TOOL_AND_OUTPUT_RESERVE = 15000` literal that had no derivation anywhere. Roughly 50 of the 64 added lines are that derivation and the arithmetic showing the old constant was smaller than the primary agent's tool schemas alone (15,000 vs 17,502), which put the assembler's ceiling ABOVE the window on 32K models. From here the file may only shrink."
    },
    {
      "path": "packages/server/src/memory/assembler.ts",
      "from": 2638,
      "to": 2680,
      "by": "PHASE-3 T6 (2026-08-01)",
      "why": "THE PM PATH STOPS BEING A BYPASS, and the assembly starts recording itself. +42 buys two things that cannot live anywhere else. (1) The PM branch returned after `while (messages[0].role !== 'user') shift()` \u2014 role normalised, leading tool_result not \u2014 which is 3 of T4's 17 day-0 divergences, all on `kelly`. That loop is DELETED and `applyIntegrityPass` runs instead, and the branch now returns the same shape as every other path (messageEntryIds, freshTailDropped, reserveTokens, an AllocationReport naming its one lane with its reason in words, consumedOneShotFlags). The report has to be built where the PM assembly happens; a helper in `lanes.ts` would be the same lines against a file already at its own ceiling. (2) The lane tag at emission plus the two `readStoredTurnThreshold` call sites. WHAT IT BUYS ELSEWHERE, in the same task: `agent/model.ts` 2742 -> 2696 (-46), because the orphan repair moved to `agent/tool-pairing.ts` where it could finally be unit tested \u2014 which is how its 14-of-17 defect was found and fixed."
    },
    {
      "path": "packages/server/src/agent/v2/loop.ts",
      "from": 9603,
      "to": 9609,
      "by": "PHASE-3 T6 (2026-08-01)",
      "why": "+6 for the allocator receipt reaching the receipt writer: one import, one argument line at the pending-question injection, and four fields at the `writeContextReceipt` call (`allocation`, `freshTailDropped`, `systemVolatileChars`, `reserveTokens`). Four of the six are irreducible data \u2014 research 06 \u00a78's complaint is precisely that these numbers exist and never reach a receipt. The comment block that first accompanied them was cut from ten lines to two before this raise was asked for; the mechanism it described is documented in `memory/message-lane-tag.ts`'s own header."
    },
    {
      "path": "packages/server/src/agent/v2/turn-record.ts",
      "from": 197,
      "to": 250,
      "by": "PHASE-3 T6 (2026-08-01)",
      "why": "The read-side SANITY BOUND on stored turn thresholds (T5 adjudication 1), placed beside the clock because the bound IS the writer's own horizon and a horizon re-declared on the read side is the writer/reader pair research 06 \u00a75 catalogues. 42 of the 53 lines are the derivation: the five-row measurement of the live body that produced it (kevin 1598 vs turn 264, healer 122 vs 9, imaginer 19 vs 0 \u2014 three agents whose continuity brief and whole post-compaction scaffolding block had been permanently admitted), and why equality sits INSIDE the bound. #14 forbids a threshold with no derivation beside it, and this file's existing `currentTurnNumber` docstring is the precedent \u2014 it carries its own measurement for the same reason."
    },
    {
      "path": "packages/server/src/memory/lanes.ts",
      "from": 649,
      "to": 665,
      "by": "PHASE-3 T6 (2026-08-01)",
      "why": "+16, all of it the DERIVATION of the re-derived `lane.loop-tail` reserve (T3 acceptance assignment a): the 234-receipt distribution, the per-entry maxima, and the named gap the number deliberately does not cover (`msg.technique-strong` at the loop's own 25,000-char cap = ~6,250 tokens, 7x this reserve). #14 forbids a threshold without its command beside it, and this file's `POST_BUDGET_LANES` type carries a `measured` field for exactly that reason \u2014 the raise is the rule working, not an exception to it. The number itself went DOWN, 1,420 -> 900."
    },
    {
      "path": "packages/server/src/memory/lanes.ts",
      "from": 665,
      "to": 703,
      "by": "PHASE-3 T7 (2026-08-01)",
      "why": "+38 for THE DELIVERIES LANE's declaration, and declaration only \u2014 the lane's code lives in its own module (`memory/deliveries-lane.ts`, a new file), which is the move this ratchet exists to force. 26 of the 38 lines are the `measured` string the POST_BUDGET_LANES type demands: the reserve DERIVED from the generator (316 = the worst case the renderer can produce under its own caps, pinned to `deliveriesLaneWorstCaseTokens()` by a test), the live distribution behind the row cap (43 conversation-days: 28 hold one delivery, 3 hold two, 7 hold three, and three storm buckets hold 49/87/196), and what the reserve BUYS \u2014 316 tokens off every assembly, against an echo row that is persisted history and therefore re-billed on every turn until it ages out, and can never be truncated. The remaining 12 are the four declared caps (row cap, quote, echo probe, quote floor), three of them lifted from the loop where they were bare literals. #14 forbids a number with no derivation beside it."
    },
    {
      "path": "packages/server/src/prompt/registry/entries.ts",
      "from": 505,
      "to": 524,
      "by": "PHASE-3 T7 (2026-08-01)",
      "why": "+19 for ONE registry entry, `msg.deliveries` at slot 1860 \u2014 14 of them its `reason` field, which is where this registry records why an injection exists and what it replaced (the cross-conversation echo ROW duplication and the registry-exempt pending-question header). The render is a one-line payload pass-through, the same shape as `msg.technique-strong`, `msg.pending-nudge` and `msg.delegation-hint`: the content is computed where the in-flight array is, the DECLARATION lives here. Emitting through the registry is what makes T6's lane tagging automatic, so the alternative to these 19 lines is a second door into the message array."
    },
    {
      "path": "packages/server/src/agent/v2/loop.ts",
      "from": 9599,
      "to": 9613,
      "by": "PHASE-3 T7 Step 2 (2026-08-01)",
      "why": "+14 for the quiet window's DURABLE record: an import and one `recordReAnswerCheck({...})` call beside the detector that has been log-only since 2026-07-10, plus the six comment lines saying why the `logger.warn` under it is no longer the window's evidence. The sink itself is a new module (`agent/v2/re-answer-sink.ts`, 220 lines, pinned from birth). Net against T7's own starting pin of 9609: +4 \u2014 Step 1 took 10 lines OUT of this file when the registry-exempt pending-question block became a lane call. The window it opens is what licenses deleting the SETTLED_HINT, the settled-HOLD carve-outs and the echo rows, all of which live in THIS file: this is 14 lines borrowed against a much larger strip, with the strip's own date on it (research 18 \u00a7open-1)."
    },
    {
      "path": "packages/server/src/agent/v2/receipt.ts",
      "from": 399,
      "to": 433,
      "by": "PHASE-3 T7 (2026-08-01)",
      "why": "THIS FILE WAS NEVER PINNED and sat at 399 \u2014 ONE LINE under the 400-line new-file cap, so the first honest addition to it was always going to trip the gate. It is pinned here at its real count, which is what the gate asks for in its own refusal message. The +34: `lane.deliveries` is the SECOND lane the assembler declares and the loop fills, so T6's `withMeasuredLoopTail` (which exists because the assembler's report can only say 'did not fire' about a lane it does not emit) becomes a two-lane split \u2014 the deliveries message is measured against ITS 316-token reserve and taken OUT of the loop tail's, because the same tokens billed against two reserves is a number in the receipt that never happened. FOUND LIVE: the first real turn carrying the lane printed `post-budget lane did not fire on this turn` while the rendered message sat six rows below it in the same receipt. Also in the +34: `RECEIPTS_ROOT` became a per-call resolution, because a module-load constant ignores a test's redirected HOME and wrote into the real ~/.dojo \u2014 the rule both durable sinks already follow."
    },
    {
      "path": "packages/server/src/memory/assembly-validation.ts",
      "from": 576,
      "to": 610,
      "by": "PHASE-3 T4 Step 2b, THE FLIP (2026-08-01)",
      "why": "+34 HERE against -93 in agent/model.ts in the SAME commit, which is the trade this file's own T4 entry promised and the only reason it was ever pinned past the cap. Not one line of the +34 is new machinery: the mode constant flips 'detect'->'repair' (a one-character-class change), the shape-only branch stops returning and THROWS with the violation list on the error, and the rest is the evidence block that licenses both -- day 0's 73 calls / 17 divergent / 0 budget, the two shape classes root-caused and fixed at T6 efc949c, the sink's 2 lines both PREDATING those fixes, and the driven pre-flip arm at 1d112ad reading checked=63 diverged=0 with the union OR8 set 9/9 GREEN. The owner cancelled the 7-day calendar window on 2026-08-01 and an evidence threshold replaced it, so the measurement IS the authorisation and it belongs in the file it authorises: a reviewer who cannot see why shape became enforcing cannot check it. The counterpart pin (model.ts 2696 -> 2603) moves DOWN in this same commit and is recorded there."
    },
    {
      "path": "packages/server/src/agent/model.ts",
      "from": 2696,
      "to": 2609,
      "by": "PHASE-3 T4 Step 2b, THE FLIP (2026-08-01)",
      "why": "A LOWERING, recorded with the same weight as a raise because it is the payment on T4's own promissory note ('It SHRINKS at T4 Step 2b ... a net cut of roughly 118 lines'). Measured: -87 (2696 -> 2609), not -118. The difference is stated rather than smoothed: the two trimmer bodies came out whole (Anthropic :2244-2292, OpenAI :1224-1292, 118 lines between them) and 31 lines went back in -- 25 of STRIP notes, plus 6 repairing a comment the deletion left lying about the tree, carrying each deletion's `requirement preserved:` line, the upstream owner of that requirement, and the measurement that showed the loop never ran (0 budget violations in 73 day-0 calls and 0 in 63 driven pre-flip calls). Two of those lines keep the post-trim sanitizeOrphanToolBlocks HONEST rather than deleting it: its stated reason died with the loop, the plan pinned it as a separate guard that stays, and deleting a guard because its reason went away is the inference #15 forbids. From here this file may only shrink."
    },
    {
      "path": "packages/server/src/memory/budget.ts",
      "from": 301,
      "to": 308,
      "by": "PHASE-3 T4 Step 2b, THE FLIP (2026-08-01)",
      "why": "+7, and every one of them is a fact that CHANGED rather than a fact that was added. This file's TOOL_AND_OUTPUT_RESERVE derivation opened by citing `agent/model.ts`'s `minOutputReserve = 4096` as the transport's own declared floor and the stricter of the two the tree enforced. Step 2b deleted both front-trimmers, so that declaration no longer exists and the sentence had become a pointer into a hole -- the exact shape a later worker re-derives wrongly. The +7 records that this is now the ONLY declaration of the number, that it is therefore not a copy of anything, and that the provenance above it is kept deliberately: 4,096 is not an arbitrary reserve, it is the floor a real transport demanded, and a reader who sees only the literal is one step from re-tuning it on taste."
    },
    {
      "path": "packages/server/src/agent/v2/loop.ts",
      "from": 9613,
      "to": 9632,
      "by": "PHASE-3 STRIP-3, the two conv-key/conversation-id class defects (2026-08-01, RULING P3-R3)",
      "why": "+19, and all 19 are comment. The CODE delta is zero: one argument changed (`recordedAnswerInConversation(agentId, chosenConvKey)` -> `chosenConversationId`, with its gate), and one `.set()` MOVED from beside its conv-key sibling to after the pickup repair that reassigns what it publishes -- moved, not doubled, so the map still has exactly one writer. The 19 lines are the two measurements, kept at the sites rather than only in the report, because both defects are INVISIBLE to every mechanical check this repo owns: both values are `string`, so tsc cannot see them, and the key crosses a function boundary, so the 51-bind-site grep STRIP-2 ran returns zero. A comment is the only thing left that can stop a future worker re-introducing them. THIS PIN COMES BACK DOWN IN THIS SAME TASK: the strip half of T7 Step 2 deletes `persistCrossConvSendEcho` and its three call sites, both SETTLED_HINT branches and the re-answer detector's call site from this file, and the lowering is recorded as its own entry with the measured figure."
    },
    {
      "path": "packages/server/src/agent/v2/loop.ts",
      "from": 9632,
      "to": 9548,
      "by": "PHASE-3 T7 Step 2, THE STRIP (2026-08-01, RULING P3-R3)",
      "why": "A LOWERING, and it pays the note the raise earlier in this same task wrote (`9613 -> 9632`, the two conv-key measurements). Net from the pre-task 9613: -65. Three mechanisms left this file. (1) The cross-conversation send ECHO: `persistCrossConvSendEcho` and its three call sites, including the whole `gmail_send`/`outlook_send` branch whose entire body was the echo call -- requirement preserved by the deliveries lane (T7 Step 1) and `persistRoutingMarker`. (2) The SETTLED_HINT, both branches -- ~65 tokens of prose per turn telling the model not to re-answer conversations `scopeToHumanConversation` had already removed from the window; requirement preserved structurally there, deterministically by the now-WIRED `check-reanswer-ghost.mjs`, behaviourally by `settled-work-stays-settled`. (3) The cross-conversation re-answer DETECTOR call site and its two imports; `re-answer-guard.ts` (-133) and `re-answer-sink.ts` (-220) are deleted outright and the sink's pin is removed from this manifest with it. As with the trimmers at Step 2b, the deletions are larger than the net: STRIP notes went back in, each carrying its `requirement preserved:` line, the mechanism that now owns that requirement, and the measurement behind it -- and here the measurement matters more than usual, because the detector is being deleted as an alarm PROVEN never to have worked (its exclusion compared a conv key against a UUID column) rather than as one that went quiet."
    },
    {
      "path": "packages/server/src/work/store.ts",
      "from": 1665,
      "to": 1678,
      "by": "PHASE-4 T2 Step 2, withUnit adoption + the un-atomic clusters closed (2026-08-02)",
      "why": "+13 across four functions (`rejectClaim`, `stampClaimingTurn`, `clearJoinCompilePending`, `openCommitment`), and the CODE delta is close to zero: each pair of writes gains a `withUnit(() => {` / `});` and its body is re-indented. Six of the thirteen are the `revertAskClaimOnAbort` correction block \u2014 the pin list calls it un-atomic and it is NOT (one write per path), which is a fact the next reader must be able to find at the site rather than in a report they will not open. The rest buy the property this task exists for: an adjudication with no event, or a claim stamp with no `claim_turn` row, is a silence."
    },
    {
      "path": "packages/server/src/techniques/store.ts",
      "from": 609,
      "to": 615,
      "by": "PHASE-4 T2 Step 2, withUnit adoption + the un-atomic clusters closed (2026-08-02)",
      "why": "+6 in `deleteTechnique`: one `withUnit` wrapper (2 lines) plus a four-line note stating WHY the four DELETEs need it \u2014 three of the four tables have no FK to `techniques`, so the comment standing above them ('cascading from ON DELETE CASCADE') was describing a cascade that does not cover them. A technique deleted half-way left usage rows, versions and an embedding behind: searchable, and unloadable."
    },
    {
      "path": "packages/server/src/work/occurrences.ts",
      "from": 417,
      "to": 423,
      "by": "PHASE-4 T2 Step 2, withUnit adoption + the un-atomic clusters closed (2026-08-02)",
      "why": "+6 in `settleOccurrence`: the wrapper (2) plus a four-line note. The event this now commits with is the ONLY carrier of the run's own word \u2014 `abandoned` is the terminal state for BOTH 'finished and reached nobody' and 'never ran', and the owner's run history renders those differently. Losing the event loses the discriminator, which is exactly why it may not be a second transaction."
    },
    {
      "path": "packages/server/src/scheduler/runner.ts",
      "from": 1699,
      "to": 1701,
      "by": "PHASE-4 T2 Step 2, withUnit adoption + the un-atomic clusters closed (2026-08-02)",
      "why": "+2 net across FOUR edits, and the smallness is the story: three `withUnit` wrappers in `onTaskRunComplete` (the tracker state and the schedule columns describe ONE event and could disagree) plus a 12-line correction block at `checkScheduledTasks`, minus the lines that fold away. The correction is why the pin does not rise further: the plan's 'schedule advance + occurrence' cluster is ALREADY atomic inside `claimOccurrence`, so the wrap the pin asked for was not owed, and the claim-then-status split that remains is the DESIGN (the claim must be won before anything asserts the row is running)."
    },
    {
      "path": "packages/server/src/memory/message-store.ts",
      "from": 980,
      "to": 981,
      "by": "PHASE-4 T2 Step 2, withUnit adoption + the un-atomic clusters closed (2026-08-02)",
      "why": "+1, and it is the `import { withUnit }` line. The five already-atomic sites in this file were ADOPTED, not rewritten: `db.transaction(...)()` became `withUnit(...)`, no behaviour change, suite unchanged. The one line buys uniform ownership of the transaction, which is what gives `afterCommit` somewhere to hang."
    },
    {
      "path": "packages/server/src/techniques/store.ts",
      "from": 615,
      "to": 621,
      "by": "PHASE-4 T2 Step 3, commit-then-emit (2026-08-02)",
      "why": "+6 in `recordTechniqueUsage`: the `withUnit` wrapper (2) and a four-line note. The usage ROW and `techniques.usage_count` are one event \u2014 the counter SUMMARISES the rows, and split across two transactions it can disagree with them. That is the maintained-integer shape this overhaul has already had to repair once (`revert_count` on `tasks`, replaced by a COUNT for exactly this reason), so it is worth six lines to stop it happening on a second column. This is the file's SECOND raise in this task and the two are separate on purpose: the first was `deleteTechnique`'s four-table unit, this one is the usage pair, and folding them into one entry would hide that the file gained two independent properties."
    },
    {
      "path": "packages/server/src/work/store.ts",
      "from": 1678,
      "to": 1684,
      "by": "PHASE-4 T4-SCHEMA, the `work_events.kind` union (2026-08-02)",
      "why": "+6 on the ONE writer of `work_events`, and every line of it is the guard or its reason: `import type { WorkEventKind }` (1), the narrowed parameter (0 - in place), and a four-line note saying what `135`'s \"12-value enum\" comment actually was. The mechanism did not grow, it SHRANK in kind: `kind: string` became a 25-member union, so the 29 call sites in this directory are now checked by the compiler and the file can no longer write a value the column will refuse. The full derivation (11 literals + 5 constant objects, swept over all 178 commits since `work_events` was born) lives in the NEW file `work/event-kinds.ts` rather than here, which is why this raise is six lines and not sixty."
    },
    {
      "path": "packages/server/src/work/occurrences.ts",
      "from": 423,
      "to": 426,
      "by": "PHASE-4 T4-SCHEMA, the `work_events.kind` union (2026-08-02)",
      "why": "+3: `import type { WorkEventKind }` (1) and two lines of the docblock over `OCCURRENCE_EVENT`, which now says what it IS - a view onto the declared list, `satisfies`-bound - instead of what it used to be. The old wording was load-bearing and is now FALSE (\"`work_events.kind` has no CHECK, so the guard against a writer/reader typo is that both sides import these\"); migration `152` gives it a CHECK, and leaving a stale reason beside a live constant is how the next reader concludes the constant is the only guard there is."
    },
    {
      "path": "packages/server/src/work/tracker-view.ts",
      "from": 508,
      "to": 509,
      "by": "PHASE-4 T4-SCHEMA, the `work_events.kind` union (2026-08-02)",
      "why": "+1, exactly the `import type { WorkEventKind }` line. `WORK_EVENT`'s five values are `satisfies`-bound to `work/event-kinds.ts` now, so this file can no longer declare a kind the CHECK does not admit - one line for a compile-time binding on the file that holds five of the twenty-four."
    },
    {
      "path": "packages/server/src/work/override-requests.ts",
      "from": 246,
      "to": 247,
      "by": "PHASE-4 T4-SCHEMA, the `work_events.kind` union (2026-08-02)",
      "why": "+1, exactly the `import type { WorkEventKind }` line, binding `OVERRIDE_EVENT`'s two values to the declared list. Same one-line shape as `tracker-view.ts` in this commit."
    },
    {
      "path": "packages/server/src/work/store.ts",
      "from": 1684,
      "to": 1736,
      "by": "PHASE-4 T4, the claimed-delivery rekey (2026-08-02)",
      "why": "+52 for `owedSendObligations` and its type - the OBLIGATION half of the owner's live fixture. The floor it feeds used to trigger on a regex over the model's prose and fired three times on \"told Michael\" in a transcript; it triggers on a row now, and the row has to be readable from the module that owns obligations. The reader is 12 statements; the rest is the argument for why `claimed` is excluded and why `result_delivery_id IS NULL` is asked directly."
    },
    {
      "path": "packages/server/src/agent/v2/loop.ts",
      "from": 9608,
      "to": 9609,
      "by": "PHASE-4 T4, the claimed-delivery rekey (2026-08-02)",
      "why": "+1 net across two call sites. The prose detector's inline nudge text and its ledger-suppression branch came OUT of this file into `agent/v2/claimed-delivery.ts`, the decision came back in as one call, and the finalize tripwire was re-pointed at the same decision so the two readers cannot drift. One line, for a floor that stops accusing people."
    },
    {
      "path": "packages/server/src/agent/v2/loop.ts",
      "from": 9609,
      "to": 9676,
      "by": "PHASE-4 T4, the E1/E2 conversions (2026-08-02)",
      "why": "+67. Two engine-composed user-facing lines came out (a pool pick and a template literal, four lines between them) and the OR2 ladder went in twice: a second bounded steer with its own text, the delivery-ledger verification that must happen before silence may be called a ghost, and the ghost call itself. The ladder is the requirement OR2 states - detect, steer, VERIFY, retry bounded, then surface as the system - and none of its five steps can be inlined away. The ghost's own machinery (row, platform note, health frame) is in `agent/v2/floor-ghost.ts`, not here."
    },
    {
      "path": "packages/server/src/agent/v2/steer-queue.ts",
      "from": 283,
      "to": 291,
      "by": "PHASE-4 T4, the reminder-silence floor (2026-08-02)",
      "why": "+8: one union member, one table row, and the six-line argument for why the 27th floor exists at all - it was an ENGINE-COMPOSED LINE when T3 derived the surface by writer, so no writer-derived inventory could have found it. A floor with no written `why` is a number, which this table's own integrity clause refuses."
    },
    {
      "path": "packages/shared/src/ws.ts",
      "from": 1082,
      "to": 1088,
      "by": "PHASE-4 T4, the FLOOR_GHOSTED health code (2026-08-02)",
      "why": "+6: one `ChatErrorEvent` code and the five lines saying what it means. OR2 requires a true ghost to reach the owner as a PLATFORM fault; the dashboard's health surface is the existing door and it is keyed on this union, so the code has to be declared where every other platform-level code is declared."
    },
    {
      "path": "packages/server/src/agent/a2a-transport.ts",
      "from": 1953,
      "to": 2008,
      "by": "PHASE-4 T4, the fail-closed notice converted (2026-08-02)",
      "why": "+55: the transport door's VOICE parameter and the five lines that route the voiced body through it, `tellAgentTheJoinFailed` (the agent's own copy carrying the 2026-07-30 nudge, ~20 lines), and the argument for the split - a RELAY of a peer's own answer is not the same statement as a NOTICE about what the platform observed, and D13's recorded ask->park->answer->re-ask incident is why the relay half must not move."
    },
    {
      "path": "packages/server/src/agent/v2/loop.ts",
      "from": 9676,
      "to": 9712,
      "by": "PHASE-4 T4, the thrash-block notice converted (2026-08-02)",
      "why": "+36, and 30 of them are the argument. The first-person paragraph came out; a durable platform-voiced owner-alert note went in (8 lines). The rest is the record of WHY this site's ladder has no steer - the loop breaks three statements below, so a steer here would be written and never seen - and why it therefore writes NO `floor_ghosted` row. A conversion that differs from its four siblings and does not say so is how the next reader 'fixes' it back."
    },
    {
      "path": "packages/server/src/tracker/tools.ts",
      "from": 4394,
      "to": 4410,
      "by": "PHASE-4 T4, the fallen-project note converted (2026-07-30 owner ruling)",
      "why": "+16: the owner-facing write is replaced by an agent-facing engine event with the ruling's nudge (about the same size), and the rest is the CORRECTED measurement - the inherited comment claiming this note reached nobody but the model is false, and the query that disproves it (nine owner-lane user-visible rows on the live box) is written at the site so the next reader does not re-inherit the claim that sent this to the wrong phase."
    },
    {
      "path": "packages/server/src/agent/v2/loop.ts",
      "from": 9712,
      "to": 9735,
      "by": "PHASE-4 T4, the settled-hold dissolution (2026-08-02)",
      "why": "+23. The six-term conjunction became a call plus its six named inputs (about even), and the rest is the record: the scar-ledger verdict this discharges, its `requirement preserved:` line, and the reason the design law is unchanged (only the PUSH is withheld). The rule itself moved OUT to `agent/v2/outbound-root.ts` where it can be exhausted against an oracle; what stays here is the call and why."
    },
    {
      "path": "packages/server/src/agent/v2/answered-edge.ts",
      "from": 520,
      "to": 524,
      "by": "PHASE-4 T4, the OR2-PROVISIONAL burn-down (2026-08-02)",
      "why": "+4 comment lines on `NON_ANSWERING_TOOLS`. The marker said the engine-ack lane would be REMOVED; it was not, and the exclusion now stands on the reason it always really had (a start-ack is not an ANSWER, whoever wrote it). A closed marker that does not say what changed sends the next reader to delete a live exclusion."
    },
    {
      "path": "packages/server/src/agent/v2/loop.ts",
      "from": 9735,
      "to": 9745,
      "by": "PHASE-4 T4, the OR2-PROVISIONAL burn-down (2026-08-02)",
      "why": "+10 comment lines at the engine-ack channel-push door. The marker there predicted the lane would be converted away; it was not, and a CLOSED marker that does not say what actually happened is how a live exclusion gets deleted by the next reader. The replacement states the measured fact: one production caller, delivering the model's own opening line."
    },
    {
      "path": "packages/server/src/work/store.ts",
      "from": 1736,
      "to": 1741,
      "by": "PHASE-4 T4, the OR2-PROVISIONAL burn-down (2026-08-02)",
      "why": "+5 comment lines on `NON_ANSWERING_DELIVERY_TOOLS`, the twin of `answered-edge.ts`'s set. The two are kept in step by a conformance test, so their reasons have to stay in step too."
    },
    {
      "path": "packages/server/src/agent/model.ts",
      "from": 2609,
      "to": 2619,
      "by": "PHASE-4 T5, provider errors classified from status codes (2026-08-02)",
      "why": "+10 across the two provider catches and the Ollama one. The old classification was three lines of `message.includes('429'|'503'|'529')` immediately below a line that had ALREADY read `err.status` into the log payload - the status was in scope and the verdict ignored it. What replaces them is `classifyProviderError(err)` plus `provider: facts` on each `AgentError`, and the facts travelling on the error are what let five downstream files delete their own copies of the same substring table. The derivation and the adversarial fixtures live in the NEW file `agent/provider-error.ts`, which is why this raise is ten lines and not a hundred."
    },
    {
      "path": "packages/server/src/agent/v2/recovery.ts",
      "from": 596,
      "to": 605,
      "by": "PHASE-4 T5, provider errors classified from status codes (2026-08-02)",
      "why": "+9: the cascade reads the provider's facts ONCE at its entry and hands them to all four steps (three signatures gain a `facts` parameter, one line each), and `classifyError`'s numeric branches become one call. Net of the comment it is a parameter thread, not new logic - the point of the thread is that `tryPlatformErrorRecovery` can no longer decide `auth_invalid` (agent locked, owner told his API key is dead) from a `401` inside a token count, because the status is now in scope where the verdict is made."
    },
    {
      "path": "packages/server/src/healer/injury-recovery.ts",
      "from": 810,
      "to": 822,
      "by": "PHASE-4 T5, provider errors classified from status codes (2026-08-02)",
      "why": "+12: `classifyError` here holds a persisted STRING (`agents.last_error`) and nothing else, so it kept the sixth private copy of the substring table. It now calls the ONE shared prose table and keeps only the two dojo-specific buckets that have no HTTP equivalent (context corruption, config), reordered above the generic 4xx bucket that used to swallow `invalid_request`. A `switch` over a typed union is longer than nine `lower.includes()` lines and is the whole reason a digit run inside a token count can no longer read as a 401."
    },
    {
      "path": "packages/server/src/agent/provider-error.ts",
      "from": 0,
      "to": 317,
      "by": "PHASE-4 T5, provider errors classified from status codes (2026-08-02) - NEW FILE, pinned at birth",
      "why": "The growth detector asked for this decision to be visible, so here it is. 317 lines, of which roughly half are the record: what the five substring tables got wrong in BOTH directions (an over-length 400 read as `auth_invalid` on the `401` inside its token count; a real 429 whose body says only \"quota\" read as unclassified), and why the prose fallback is safe to keep at all (every status matched as a token). It is one module because it REPLACES five private copies of the same table - splitting it would re-create the disease it exists to close - and it is pinned decrease-only from here."
    },
    {
      "path": "packages/server/src/gateway/routes/agents.ts",
      "from": 820,
      "to": 822,
      "by": "PHASE-4 T5 Step 3, R23 - blind route catches record why (2026-08-02)",
      "why": "+2: the `import { noteRouteFailure }` line and the `noteRouteFailure(c, logger, err)` call in the ONE answering catch this file had that recorded nothing - `GET /:id/system-prompt` answered `{ ok: true, data: { content: '' } }` on any read failure, so a soul file that had gone unreadable rendered as an agent with an empty system prompt and left no trace at all."
    },
    {
      "path": "packages/server/src/gateway/routes/setup-deps.ts",
      "from": 512,
      "to": 513,
      "by": "PHASE-4 T5 Step 3, R23 - blind route catches record why (2026-08-02)",
      "why": "+1, exactly the `import { noteRouteFailure, routeFailure }` line. Its five converted catches each got SHORTER or stayed level; the import is the whole raise."
    },
    {
      "path": "packages/server/src/gateway/routes/tracker.ts",
      "from": 905,
      "to": 906,
      "by": "PHASE-4 T5 Step 3, R23 - blind route catches record why (2026-08-02)",
      "why": "+1, exactly the `import { routeFailure }` line. Three `catch { return c.json({ ok:false, error:'invalid JSON body' }, 400) }` blocks now bind and record their error while telling the caller the same words as before."
    },
    {
      "path": "packages/server/src/gateway/routes/update.ts",
      "from": 1022,
      "to": 1023,
      "by": "PHASE-4 T5 Step 3, R23 - blind route catches record why (2026-08-02)",
      "why": "+1, exactly the `import { routeFailure }` line, for two 500 answers that logged nothing on the route that applies releases - the single place on this box where a silent failure costs the most."
    },
    {
      "path": "packages/server/src/gateway/routes/upload.ts",
      "from": 607,
      "to": 608,
      "by": "PHASE-4 T5 Step 3, R23 - blind route catches record why (2026-08-02)",
      "why": "+1, exactly the `import { routeFailure }` line, for three `catch { \u2026 'Database not available' }` blocks that reported a DB open failure to the browser and to nobody else."
    },
    {
      "path": "packages/server/src/agent/v2/loop.ts",
      "from": 9745,
      "to": 9749,
      "by": "PHASE-4 T5b, RULING P4-R2 - credential write-path redaction (2026-08-02)",
      "why": "+4 net on a file that DELETED 31 lines here: the inline credential value-scrub (24 lines, NEXT-WAVE item 5) moves out to credentials/secret-fields.ts and comes back as one call, and the 35 added lines are the T5b seams that could not live anywhere else - the note-the-declared-secrets call at the point the model result settles (it must run before the embed, the persist and the broadcast, all three of which are in this function), the reasoning-column scrub on both persist branches, and the persist-side arguments for the cross-turn failure signature. Each carries the comment that says WHICH surface it closes, because the finding they answer (a key the owner typed into chat, at rest in the agent's own tool_use arguments and replayed to the provider every later turn) is not visible from the code. The real accounting is elsewhere: credentials/tools.ts -42, and the one owner for `which fields are secret` + `which values are secret` is a new 244-line module."
    },
    {
      "path": "packages/server/src/credentials/secret-fields.ts",
      "from": "(unlisted, growth detector's 240-line visibility threshold)",
      "to": 244,
      "by": "PHASE-4 T5b, RULING P4-R2 - credential write-path redaction (2026-08-02)",
      "why": "A NEW file four lines past the 240-line visibility threshold, pinned rather than split, and the split is the thing being refused: this module answers ONE question - which characters on this box are a credential - and it answers it in the only way that is not prose-keying, by naming the tool FIELDS that declare secret material (three, on three tools, derived by reading all 437 tool definitions). Splitting `which fields are secret` from `which values are secret` would make two owners for one job and let them drift, which is exactly how a redactor goes quietly blind. Roughly half the file is the doc block and the map's own entries, including the DELIBERATELY-NOT-HERE list with a reason per exclusion, because the next person's temptation is to add a value-shaped regex and the argument against it has to be in the file. It SHRINKS if Phase 5's tool registry lands the declaration on the tool definitions themselves, at which point the map here becomes a reader."
    },
    {
      "path": "packages/shared/src/visibility.ts",
      "from": 865,
      "to": 881,
      "by": "PHASE-4 T6 (2026-08-02) \u2014 the fallen-project note's grave",
      "why": "+16 lines and every one of them is a TOMBSTONE, which is the one growth this project has decided repeatedly to pay for. `OWNER_ALERT_PROJECT_ATTENTION_PREFIX` lost its last writer when T4 removed the fallen-project note under the owner's 2026-07-30 ruling (the platform does not alert the owner directly; the AGENT is told and decides). The constant STAYS, and #15 is the reason: it is a CLASSIFIER KEY for history, not a template \u2014 a lived-in box already carries nine of these rows (measured live: `owner | system | owner-alert | user-visible | 9`), and `dashboard/src/pages/Chat.tsx:145,192` allowlists then strips the tag so the owner does not read jargon. Deleting the key would not delete the rows; it would render nine existing owner-visible notes with a raw marker on them. The file's own CONTRACT comment named THREE write sites and one of them no longer exists, which is the `engine-steer.ts:16-18` class this phase exists to stop \u2014 a comment teaching a deleted rule is a live instruction to the next writer. So the block records: which site died and under which ruling, the command that proves zero writers remain, why the constant survives its writer, and who reads it. The alternative to paying these lines is a constant with no writer, no explanation and a stale contract note pointing at a function that was deleted."
    },
    {
      "path": "packages/server/src/agent/brokers/deny.ts",
      "from": 0,
      "to": 335,
      "by": "PHASE-5 T2 Step 2 (2026-08-02) \u2014 the ONE deny list",
      "why": "PHASE-5 T2 (2026-08-02) \u2014 the two brokers' own modules, added at their measured size in a gate-side-only commit so the decision is visible. NEITHER IS A GOD FILE IN THE MAKING AND THE ARITHMETIC SAYS SO: this task's product diff is NET NEGATIVE on the files it touches. `agent/permissions.ts` falls 687 \u2192 145 (TIGHTENED in this same edit, not left slack), because its four deny lists, its glob engine, its symlink canonicalizer, its manifest reader and its squad fallback all left for named leaves \u2014 `brokers/deny.ts` (335) is where the FOUR lists became ONE table, and `brokers/grants.ts` (284) is where a ten-field manifest became `grant_rule` rows with `ORDER BY mode DESC`. 335 + 284 = 619 against 542 removed from one file alone, before `path-guards.ts` (241 \u2192 123) is counted. SPLITTING EITHER ONE WOULD BE THE DISEASE, not the cure: the whole requirement of `deny.ts` is that there is exactly ONE place a denied path is written down \u2014 it replaced two lists that drifted precisely because they were two files \u2014 and `grants.ts` holds one projection function, one reader and one evaluator that must agree by construction. Most of both files' lines are the REASON each rule exists, carried verbatim from the incidents that earned them (v2.3.19 Scenario 3's `>>` redirect, N3's planted symlink, FA-P5's re-opened deny, PHASE-0 T10's case fold). A deny list nobody can read is a deny list nobody dares change."
    },
    {
      "path": "packages/server/src/agent/brokers/grants.ts",
      "from": 0,
      "to": 284,
      "by": "PHASE-5 T2 Step 2 (2026-08-02) \u2014 grants as `grant_rule` rows",
      "why": "PHASE-5 T2 (2026-08-02) \u2014 the two brokers' own modules, added at their measured size in a gate-side-only commit so the decision is visible. NEITHER IS A GOD FILE IN THE MAKING AND THE ARITHMETIC SAYS SO: this task's product diff is NET NEGATIVE on the files it touches. `agent/permissions.ts` falls 687 \u2192 145 (TIGHTENED in this same edit, not left slack), because its four deny lists, its glob engine, its symlink canonicalizer, its manifest reader and its squad fallback all left for named leaves \u2014 `brokers/deny.ts` (335) is where the FOUR lists became ONE table, and `brokers/grants.ts` (284) is where a ten-field manifest became `grant_rule` rows with `ORDER BY mode DESC`. 335 + 284 = 619 against 542 removed from one file alone, before `path-guards.ts` (241 \u2192 123) is counted. SPLITTING EITHER ONE WOULD BE THE DISEASE, not the cure: the whole requirement of `deny.ts` is that there is exactly ONE place a denied path is written down \u2014 it replaced two lists that drifted precisely because they were two files \u2014 and `grants.ts` holds one projection function, one reader and one evaluator that must agree by construction. Most of both files' lines are the REASON each rule exists, carried verbatim from the incidents that earned them (v2.3.19 Scenario 3's `>>` redirect, N3's planted symlink, FA-P5's re-opened deny, PHASE-0 T10's case fold). A deny list nobody can read is a deny list nobody dares change."
    },
    {
      "path": "packages/server/src/agent/permissions.ts",
      "from": 687,
      "to": 145,
      "by": "PHASE-5 T2 Step 2 (2026-08-02) \u2014 TIGHTENED, not raised",
      "why": "`checkPermission` became a reader of the brokers rather than a second implementation of them. 542 lines left this file for named owners and NONE of it was deleted: deny lists \u2192 `brokers/deny.ts`, glob engine + canonicalizer \u2192 `agent/path-resolve.ts`, manifest \u2192 `agent/manifest.ts`, squad fallback \u2192 `agent/squad-workspace.ts`. Every export this module published is still published from it, so none of its nine callers moved. Pinned at the new measured size so the reduction is decrease-only from here."
    },
    {
      "path": "packages/server/src/agent/tools/gate-eval.ts",
      "from": 0,
      "to": 299,
      "by": "PHASE-5 T2 Step 3 (2026-08-02) \u2014 the gate evaluator",
      "why": "The 15-branch permission ladder in `agent/tools.ts` (224 lines, `:4547-4770` derived at `16bd0b8`) became a DECLARATION (`tools/gates.ts`, 194) and an EVALUATOR (this file, 299). The cap did its job once already here: the two started as one 472-line module and the split landed on a real seam \u2014 the declaration is a pure function of (name, args) that a test enumerates without a database, the evaluator is where the brokers, the grant rows and the identity predicates get involved. NET ON THE TOOLBOX IS NEGATIVE: 224 deleted from tools.ts against 493 across two files, but tools.ts also gave up PRIMARY_ONLY_TOOLS and the exec sensitive-file scan to their owners, so its own pin TIGHTENS 10524 \u2192 10341 in this same edit. Most of this file's lines are the per-ROW parity it must preserve: six ladder branches returned PERMISSION_DENIED and six a bare isError, one audited itself as `spawn` rather than `spawn_agent`, and web_fetch's unparseable-URL arm was never a permission refusal \u2014 PHASE-4 T1 cluster 3 made errorCode the thing the outcome classifier reads, so each row's answer is written out rather than flattened. Flattening would have been shorter and would have re-classified half the platform's refusals."
    },
    {
      "path": "packages/server/src/agent/tools.ts",
      "from": 10524,
      "to": 10343,
      "by": "PHASE-5 T2 Step 3 (2026-08-02) \u2014 TIGHTENED by the ladder deletion",
      "why": "The permission ladder is gone (224 lines at the bounds derived at this worker's own HEAD), and PRIMARY_ONLY_TOOLS plus the exec sensitive-file scan left for `tools/gates.ts` and `brokers/proc.ts`. Pinned at the measured result so the reduction is decrease-only."
    },
    {
      "path": "packages/server/src/agent/tools/gate-eval.ts",
      "from": 299,
      "to": 324,
      "by": "PHASE-5 T2 (2026-08-02) \u2014 a global deny is never staged",
      "why": "PHASE-5 T2 (2026-08-02), the global-deny carve-out. Driving the two hardening refusals on the REAL dev body found that Step 4's staging window covered them: a non-primary agent read `~/.dojo/secrets.yaml` through a planted symlink and got jwt_secret / dashboard_password_hash / credential_master_key back in the clear, and a `file_write` into `dojo.db-wal` CORRUPTED THE DATABASE \u2014 both with the broker's own \"would have refused\" line beside them. +25 in gate-eval.ts and +20 in deny.ts is the fix plus the incident written down where the next person to widen the window will meet it. Not one line of it is new mechanism: `logOnly` gained one condition and `isGlobalDenyRule` is a two-line reader of the table it sits in. Deleting the paragraphs to hold the ceiling would be deleting the only reason anybody would keep the condition."
    },
    {
      "path": "packages/server/src/agent/brokers/deny.ts",
      "from": 335,
      "to": 355,
      "by": "PHASE-5 T2 (2026-08-02) \u2014 a global deny is never staged",
      "why": "PHASE-5 T2 (2026-08-02), the global-deny carve-out. Driving the two hardening refusals on the REAL dev body found that Step 4's staging window covered them: a non-primary agent read `~/.dojo/secrets.yaml` through a planted symlink and got jwt_secret / dashboard_password_hash / credential_master_key back in the clear, and a `file_write` into `dojo.db-wal` CORRUPTED THE DATABASE \u2014 both with the broker's own \"would have refused\" line beside them. +25 in gate-eval.ts and +20 in deny.ts is the fix plus the incident written down where the next person to widen the window will meet it. Not one line of it is new mechanism: `logOnly` gained one condition and `isGlobalDenyRule` is a two-line reader of the table it sits in. Deleting the paragraphs to hold the ceiling would be deleting the only reason anybody would keep the condition."
    },
    {
      "path": "packages/server/src/agent/brokers/grants.ts",
      "from": 284,
      "to": 333,
      "by": "PHASE-5 T3 Step 1 (2026-08-02) \u2014 exec becomes two doors, and the projection has to say so",
      "why": "`exec_allow` now projects to BOTH `proc` rows (the argv door) and `shell` rows (the /bin/zsh door), `shell_allow`/`shell_deny` override the shell side when a manifest declares them, and `system_control` additionally projects `applescript` rows. +49 is ~14 lines of projection and ~35 of the comment that explains WHY one manifest field feeds two kinds \u2014 which is the capability-preservation argument, and burying it would be how a later reader 'tidies' it into one."
    },
    {
      "path": "packages/server/src/agent/destructive-gate.ts",
      "from": 552,
      "to": 583,
      "by": "PHASE-5 T3 / RULING P5-R3 (2026-08-02) \u2014 the approval seam moves with exec's shape",
      "why": "The classifier reads BOTH doors' real shapes through `execCallText`, and the manifest pre-check answers from `authorizeExecShapedArgs` \u2014 the SAME function the dispatcher's gate row calls, which is what turns this file's own comment ('the EXACT call executeTool makes') from a coincidence of spelling into a fact. +31 is ~10 lines of code and ~21 of the two paragraphs recording the empty-string incident class the ruling exists to refuse."
    },
    {
      "path": "packages/server/src/agent/tools/gate-eval.ts",
      "from": 324,
      "to": 346,
      "by": "PHASE-5 T3 Steps 1\u20132 (2026-08-02) \u2014 the `proc` and `applescript` gate rows",
      "why": "Row 3 became two doors (`proc`/`shell`, answered through one seam) and row 15's applescript half became its own case, because what must be authorized about osascript is the SCRIPT and `system_control`'s category compare never looked at it. +22 net after the old `shell` case was replaced."
    },
    {
      "path": "packages/shared/src/types.ts",
      "from": 508,
      "to": 527,
      "by": "PHASE-5 T3 Step 1 (2026-08-02) \u2014 `shell_allow` / `shell_deny` on the permission manifest",
      "why": "Two OPTIONAL fields and the paragraph that makes their absence meaningful: a manifest that does not mention them inherits `exec_allow`, which is the entire reason splitting exec into two doors costs no agent on any box the reach it has today. The prose IS the migration contract; the code is two lines."
    },
    {
      "path": "packages/server/src/agent/brokers/deny.ts",
      "from": 355,
      "to": 365,
      "by": "PHASE-5 T3 Step 1 (2026-08-02) \u2014 the exec global denies join RULING P5-R6's never-staged set",
      "why": "`isGlobalDenyRule` now recognises `global-exec-deny:` / `global-exec-substring:` rule ids. Latent, not harmless: at T2 every exec refusal carried `basis:'ladder-parity'` so none ever reached this check; T3 adds the first exec refusal with a `bypass-hardening` basis (the basename-normalised `/bin/rm -rf /` spelling), and without this a SUB-AGENT would have run it log-only \u2014 the exact incident shape that earned P5-R6 on the filesystem side."
    },
    {
      "path": "packages/server/src/agent/brokers/proc.ts",
      "from": 0,
      "to": 347,
      "by": "PHASE-5 T3 Step 1 (2026-08-02) \u2014 PINNED, not raised: a new file crossing the growth detector's 60% mark",
      "why": "The growth detector offers split-or-pin and this is the pin, with the reason stated so it is arguable. `proc.ts` is the exec family's DENY FLOOR (the global denies, the basename-normalised spelling, the sensitive-read scans, the per-command authority) plus its two consumers, the argv door and the shell door. The AppleScript door WAS split out (`brokers/applescript.ts`, 73 lines) because it is a second interpreter with its own grant. Splitting argv from shell would separate the two things that must give identical answers about the same command line and force the shared floor through more exported wrappers \u2014 the opposite of one-owner-per-job. Pinned decrease-only at its current count; T4's toolbox split is where it is revisited."
    },
    {
      "path": "packages/server/src/agent/brokers/proc.ts",
      "from": 347,
      "to": 375,
      "by": "PHASE-5 T3 Step 1 correction (2026-08-02) \u2014 the interpreter rule refuses INLINE script text only",
      "why": "The first draft refused any shell interpreter as argv[0], which would have deleted `exec({argv:['sh','/path/count.sh']})` \u2014 the kit's own coding-task workflow. The rule is now the `-c` form, scanned over every element so an `env` re-point cannot slip it, with shell BUILTINS refused separately. The added lines are the two narrower predicates plus the paragraph recording the capability loss the first draft would have caused, which is the part a later reader must not re-widen."
    },
    {
      "path": "packages/server/src/agent/tools/effect-conformance.ts",
      "from": 0,
      "to": 259,
      "by": "PHASE-5 T3 Step 3 (2026-08-02) \u2014 PINNED, not raised: the declarations walk crossed the growth detector's 60% mark",
      "why": "The growth detector offers split-or-pin and this is the pin, with the reason stated so it is arguable. Step 3's validation boundary reads `allowEmpty` and `requiredNotEnforced` off the `fields` sibling BY FIELD NAME, so clause 7b was added to make a renamed property fail the build: silently dropping an `allowEmpty` declaration turns `file_write({content: \"\"})` from a working call into a refusal, which is precisely the silent capability loss this phase exists to prevent. Splitting the walk would put clauses 1-7 in one file and 7b in another while both judge the same definition in the same pass \u2014 the opposite of one-owner-per-job, and a reader would have to find two files to answer \"what does a definition have to satisfy?\". Pinned decrease-only at its current count; T4's toolbox split is where it is revisited."
    },
    {
      "path": "packages/shared/src/outcome.ts",
      "from": 268,
      "to": 270,
      "by": "PHASE-5 T3 Step 3 (2026-08-02) \u2014 +2 lines for a new tool-seam reason and its one line of vocabulary",
      "why": "`invalid_args` joins blocked/crashed/cancelled because the schema-validation boundary gave `INVALID_ARGS` its first writer, and without the arm a malformed call classified `crashed` \u2014 the platform reporting itself broken for a call it understood and refused. The raise is the enum member, its entry in TOOL_SEAM_REASONS, and TWO lines of doc: what it means, and the one distinction a reader must not get wrong (it is NOT `blocked`; retry with corrected arguments, not never). The longer rationale deliberately lives at the classifier in `agent/tool-outcome.ts` rather than here, which is why this is +2 and not +13 \u2014 the first draft was, and the ratchet is what forced the prose to the file that owns the decision."
    },
    {
      "path": "packages/server/src/agent/tools/cat/fs.ts",
      "from": 0,
      "to": 848,
      "by": "PHASE-5 T4 sitting 2 (2026-08-02) \u2014 new-file-cap PIN, not a raise",
      "why": "PHASE-5 T4, the toolbox split. Relocated: the nine file/scratchpad/exec handler bodies AND the implementations they were the only production callers of \u2014 executeArgv, executeShellScript, executeFileRead/Write/Append/Patch/List, the stored-attachment lookup and their helpers. agent/tools.ts lost 95 lines of switch plus 675 lines of those functions in the same commit, and the file went 4,926 -> 3,942. Not new code. executeFileRead alone is ~190 lines because it is the vision path: image and PDF reads return provider content blocks. Not split, on the precedent set for cat/healer.ts. Decrease-only from here."
    },
    {
      "path": "packages/server/src/agent/tools/cat/session.ts",
      "from": 0,
      "to": 241,
      "by": "PHASE-5 T4 sitting 2 (2026-08-02) \u2014 new-file-cap PIN, not a raise",
      "why": "PHASE-5 T4, the toolbox split. Relocated bodies for `tunnel`, `set_user_presence` and `reset_session` (208 lines of switch deleted in the same commit). Three single-key categories that act on the DOJO's own state; they are together rather than in cat/platform.ts because platform.ts is at its own measured size and reset_session is deliberately NOT a DOJO control \u2014 PRIMARY_ONLY_TOOLS excludes it so the Healer, a non-primary service agent, can clear a wedged agent's context. Decrease-only from here."
    },
    {
      "path": "packages/server/src/agent/tools/cat/agents.ts",
      "from": 0,
      "to": 1284,
      "by": "PHASE-5 T4 sitting 2 (2026-08-02) \u2014 new-file-cap PIN, not a raise",
      "why": "PHASE-5 T4, the toolbox split. Relocated handler bodies for the seventeen Managing-Other-Agents dispatch keys plus resolveSpawnSquad, which moved with spawn_agent, its only caller; agent/tools.ts lost 1,133 lines of switch plus that function in the same commit. Not new code, and it is the largest category in the file for a reason a split cannot change: send_to_agent alone is ~260 lines (A2A intent, no-wake classification, reply recording, delivery), spawn_agent ~120 with P4's mandatory squad resolution, update_agent ~110 with the model-switch message sanitiser, delete_group ~50 with its cascade. Not split, on the precedent set for cat/healer.ts. Decrease-only from here; T7's demolition pass is where it should fall."
    },
    {
      "path": "packages/server/src/agent/tools/util.ts",
      "from": 0,
      "to": 338,
      "by": "PHASE-5 T4 sitting 2 (2026-08-02) \u2014 new-file-cap PIN, not a raise",
      "why": "PHASE-5 T4, the toolbox split. Research 05 \u00a7(a) names `util.ts` in the split skeleton and it exists for a STRUCTURAL reason, not a tidiness one: a category module may not import `agent/tools.ts` (that is the cycle the split is undoing), so a helper two categories share has to live in a leaf. Every line here is relocated from agent/tools.ts, which lost the same content in the same commits: auditLog + AUDIT_ACTION_MAP (sitting 1), then getDownloadUrl/registerSharedFile/toDashboardPath, agentCanSelfComplete(+ById) and permissionDeniedMessage (the comms move), then the six-function canvas-open cluster (broadcastCanvasUpdate, canvasMime, queueCanvasDocAttachment, syncCanvasAfterWrite, openFileInCanvas, localOfficePathFromResult) that the fs verbs, the office block, the pdf interceptor and the canvas verbs all ask the same question of. Duplicating any of it per category is the disease this overhaul exists to remove \u2014 ONE copy is the point. Decrease-only from here; T7 re-derives it once the split is finished."
    },
    {
      "path": "packages/server/src/agent/tools/cat/media.ts",
      "from": 0,
      "to": 896,
      "by": "PHASE-5 T4 sitting 2 (2026-08-02) \u2014 new-file-cap PIN, not a raise",
      "why": "PHASE-5 T4, the toolbox split. Relocated handler bodies for the five Media Generation tools; agent/tools.ts lost 874 lines of switch in the same commit. Not new code. image_create ALONE is ~450 lines \u2014 model choice, the generation job's lifecycle, the synthetic ack, and a deferred delivery IIFE that waits for the agent to go idle, sets it back to working, writes the attachment and wakes the runtime \u2014 and its C13 capture of the turn-scoped iMessage recipient is positional, so the body cannot be cut in half without moving that capture. Not split, on the precedent set for cat/healer.ts. Decrease-only from here."
    },
    {
      "path": "packages/server/src/agent/tools/cat/tracker.ts",
      "from": 0,
      "to": 837,
      "by": "PHASE-5 T4 sitting 2 (2026-08-02) \u2014 new-file-cap PIN, not a raise",
      "why": "PHASE-5 T4, the toolbox split. 837 lines of RELOCATED handler bodies: the 23 work-verb dispatch keys lifted byte-faithful out of the switch in agent/tools.ts, which lost 692 lines of switch plus normalizeRepeatDaysOfWeek in the same commit. Not new code. The size is PHASE-2 T8V's own shape: 24 tool names collapsed into six verbs, so `work_update` alone reaches seven bodies and `work_validate` five, and the 23 operations are the category. It also carries ALL NINETEEN per-OPERATION checkRequired sites T3C deliberately preserved (the work verbs' input_schema.required is [] because the wire carries a union; a schema-compiled validator validates nothing for them). Splitting the file would split those nineteen across modules for a line count. Decrease-only from here."
    },
    {
      "path": "packages/server/src/agent/tools/cat/comms.ts",
      "from": 0,
      "to": 844,
      "by": "PHASE-5 T4 sitting 2 (2026-08-02) \u2014 new-file-cap PIN, not a raise",
      "why": "PHASE-5 T4, the toolbox split. 844 lines of RELOCATED handler bodies: the ten Communication dispatch keys lifted byte-faithful out of the switch in agent/tools.ts, which lost 772 lines of switch in the same commit. It is not new code. The size is the category's own: imessage_send alone is 236 lines (bridge-status refusal, recipient resolution, attachment assembly, the turn-scoped recipient, the receipt), show_to_user 138, add_safe_sender 188. Pinned at its measured wc -l rather than split, on the precedent this task already set for cat/healer.ts: splitting one category across two files to satisfy a line count is shaping code to fit a gate, which roadmap #7 forbids, and the disjointness test (`handler-table.test.ts`) is what actually holds one-mechanism-per-tool. Decrease-only from here; T7's demolition pass is where it should fall.",
      "$note": "The category ALSO carries one of the exactly two handler-body `checkPermission` sites \u00a7T0-PINS P6 enumerated (`show_to_user`'s file_read check) \u2014 preserved verbatim in the move, and named in T7's demolition list."
    },
    {
      "path": "packages/server/src/agent/tools/cat/healer.ts",
      "from": 0,
      "to": 288,
      "by": "PHASE-5 T4 (2026-08-02) \u2014 new-file-cap PIN, not a raise",
      "why": "PHASE-5 T4, the toolbox split. This file is 288 lines of RELOCATED handler bodies: the five Healer tools lifted byte-faithful out of the 267-case switch in agent/tools.ts, which shrank by the same content in the same commit. It is not new code \u2014 check-deletion-ratio's rename detection is what keeps that honest, and the growth detector cannot see the other side of a move, which is exactly why it asks. Pinned at its measured wc -l (288) rather than split further, because the five bodies ARE the category: healer_propose alone is ~90 lines of evidence gate + provenance capture + insert, and splitting one category across two files to satisfy a line count is the kind of shape-for-the-gate move roadmap #7 forbids. Decrease-only from here: T7's demolition pass is where it should fall."
    },
    {
      "path": "packages/server/src/agent/tools/definitions.ts",
      "from": 0,
      "to": 2632,
      "by": "PHASE-5 T4 (2026-08-02) \u2014 the wire array, pinned at its measured size",
      "why": "NEW ENTRY, and it is 2,474 lines of RELOCATED DATA plus a header that says so. `toolDefinitions` is the provider payload itself: `agent/model.ts` projects {name, description, input_schema} out of it onto the request and both prompt goldens hash exactly that projection, which is why the array moved BYTE-IDENTICAL (`sed -n '794,3267p'` of the old file and `sed -n '144,2617p'` of this one hash to the same 14eba6f7\u2026) and why `registry-order.test.ts`'s byte-identity clause plus both golden hashes are the proof rather than this sentence. SPLITTING IT TO FIT THE 400-LINE CAP WOULD BE THE DISEASE, NOT THE CURE, and the cache-prefix law is the reason rather than taste: Anthropic's prefix cache breakpoint sits on the LAST always-loaded tool, so the emission ORDER of this array is a cost guarantee, and spreading 320 definitions across seven files to satisfy a line count creates seven places that each believe they know where a tool goes \u2014 exactly the drift OR7 forbids and exactly what `getAllToolDefinitions()` exists to prevent by being the ONE declared order. The same precedent the split already set for `cat/agents.ts` (1,284) and `cat/media.ts` (896): shaping code to fit a gate is not what the gate is for. `agent/tools.ts` fell 3,942 \u2192 1,377 in the same commit, so the toolbox is net far NEGATIVE on this move. Decrease-only from here; T7's demolition pass is where any real compression of the definitions themselves belongs, and the honest note is that there is very little \u2014 these lines are tool descriptions the model reads."
    },
    {
      "path": "packages/server/src/agent/tools/surface.ts",
      "from": 0,
      "to": 657,
      "by": "PHASE-5 T4 (2026-08-02) \u2014 the advertised surface, pinned at its measured size",
      "why": "NEW ENTRY (589 relocated lines + the header that states the contract). It is ONE mechanism and splitting it is the thing this entry refuses: `parseToolsPolicy` is the SINGLE parser behind both the advertised-surface strip (`computeFilteredTools`) and the executor-side deny re-check (`getAgentDenySet`), and FU-4's whole requirement is that those two cannot drift \u2014 putting them in two files is precisely how they would, silently and in the permissive direction, because Architecture Rule 1 makes the strip advice and the executor gate the enforcement. Most of the body is `computeFilteredTools`'s per-family eligibility: Google and Microsoft each with agent/user slots, service-level flags, access levels, Plaud, Office packages, credentials, the FA-TS2 primary-only strip and the per-account description annotation \u2014 one long ordered filter whose ORDER is its correctness, not seven small functions. The two memos are a measurement (FA-TS1: a 20-tool-call turn rebuilt the list ~20 times, ~185 synchronous account scans per rebuild) and they must sit beside what they memoize. `agent/tools.ts` fell 1,377 \u2192 800 in the same commit and 18 orphaned imports were REMOVED rather than pinned (unused symbols 118 \u2192 113). Decrease-only from here; T7's demolition pass owns any real compression."
    },
    {
      "path": "packages/server/src/agent/tools.ts",
      "from": 10343,
      "to": 0,
      "by": "PHASE-5 T4 (2026-08-02) \u2014 DELETED; entry retired with its tombstone",
      "why": "\u26b0 TOMBSTONE \u2014 `packages/server/src/agent/tools.ts` IS DELETED, and its entry (pinned 10,343) is REMOVED here rather than left to rot into a lie. WHAT IT WAS: the toolbox. One file holding the 320-definition wire array, a 268-case dispatch switch, the advertised-surface machinery, the permission ladder, the exec doors and the executor \u2014 the single largest file the overhaul named, and the one research 05 \u00a7(a) exists to split. WHERE EVERY MECHANISM WENT (positive evidence, per non-negotiable #15 \u2014 nothing here rests on an absence): the 268 handler bodies \u2192 `agent/tools/cat/*.ts` (18 modules) and `agent/tools/provider/*.ts` (4), reconciled at 268 handler-table keys + 0 surviving case labels = the base census exactly, with zero duplicates and zero overlap, re-run at every commit of all three sittings; the wire array + `getAllToolDefinitions()` + the boundary lookups \u2192 `agent/tools/definitions.ts`, BYTE-IDENTICAL (`sed -n '794,3267p'` of the old file and `sed -n '144,2617p'` of the new hash to the same 14eba6f7\u2026); the advertised surface + the FU-4 deny memo \u2192 `agent/tools/surface.ts`, 589 lines hashing to 49727b9e\u2026 on both sides once `getAgentDenySet` gains `export`; the 15-branch permission ladder \u2192 `agent/tools/gates.ts` + `agent/tools/gate-eval.ts` (PHASE-5 T2); the 66 hand-rolled validators \u2192 `agent/tools/validate-args.ts` (PHASE-5 T3C, RULING P5-R8); the string-exec entry point \u2192 DELETED at PHASE-5 T3 and replaced by two doors in `agent/tools/cat/fs.ts` over `agent/tools/process-run.ts`; the audit writer, the canvas cluster and the share helpers \u2192 `agent/tools/util.ts`; the mailbox banner \u2192 `agent/tools/provider/mailbox-banner.ts` with the 15-clause test that holds its asymmetry; the executor itself \u2014 `executeTool`, `executeToolInCallContext`, `dispatchResolved`, `executeToolInner`, the three membership interceptors (RULING P5-R10, verbatim, still early-returning, still absent from the handler table) and `applyMaxResultTokensCap` \u2192 `agent/tools/index.ts`. requirement preserved: ONE door for every tool call, with the gate ORDER intact and provable by position \u2014 FU-4 deny \u2192 `pmMayCall` \u2192 malformed args \u2192 T2's gate loop \u2192 T3C's boundary (57 names) \u2192 the three interceptors \u2192 dispatch \u2192 the tail. IMPORTERS ENUMERATED BEFORE THE REMOVAL, not after: a specifier sweep over every tracked `.ts` resolving relative paths to `agent/tools.js` returned 25 hits, all followed; it returns ZERO live references now. Removing shas: the three sittings' product commits, the last of them the one this gate-side commit follows. Detail: `.superpowers/sdd/PHASE-5/task-T4-report.md`, `task-T4B-report.md`, `task-T4C-report.md`; annex row in `overhaul-plans/TOMBSTONE-ANNEX.md`."
    },
    {
      "path": "packages/server/src/agent/tools/index.ts",
      "from": 0,
      "to": 640,
      "by": "PHASE-5 T4 (2026-08-02) \u2014 the executor, pinned at its measured size",
      "why": "NEW ENTRY, and it is what is LEFT of a 10,343-line file: 640 lines of door, of which the executable part is one function. It is not a new god file, it is the opposite \u2014 the arithmetic is 10,343 \u2192 640 here + 2,632 (the relocated wire array) + 657 (the relocated surface) + the 6,600 already in `cat/`/`provider/`, with the 268 dispatch cases gone from the door entirely. SPLITTING IT WOULD BREAK THE ONE THING IT EXISTS TO GUARANTEE: every tool call in the platform goes through ONE function whose gates run in ONE order \u2014 FU-4 deny, `pmMayCall`, malformed args, T2's gate loop, T3C's boundary, the three interceptors (RULING P5-R10, which cannot be handlers because they bypass the tail by design), dispatch, then the tail. Every one of those positions was earned by an incident or a ruling, and 'the order is the contract' is unenforceable across two files. A large share of the body is the WHY of each position, carried verbatim from the tasks that earned it. Decrease-only from here; T7's demolition pass owns the residuals it names (`logOnly`'s staging branch, the `toolCall.__contentBlocks` side channel)."
    },
    {
      "path": "packages/server/src/agent/spawner.ts",
      "from": 1382,
      "to": 1425,
      "by": "PHASE-5 T5 (2026-08-02) \u2014 scope: validated, subset-of-parent, readable",
      "why": "+43, of which 39 are the reason block at the AUTHORITATIVE SPAWN GATE and at the child-scope resolution. RULING P5-R2 required T5 to DECLARE which of the two spawn gates is authoritative and to state the other's disposition at the site rather than in a report nobody opens next year; that declaration is 27 lines and it is the deliverable. The executable change is a NET SIMPLIFICATION: `permissions ?? getAgentPermissions(parentId)` \u2014 a child inheriting its parent verbatim, with anything the caller passed stored unread \u2014 becomes one `resolveChildScope` call that validates, bounds and defaults, with the logic itself in `agent/scope.ts`. 7 executable lines added, 3 removed."
    },
    {
      "path": "packages/server/src/gateway/routes/agents.ts",
      "from": 822,
      "to": 858,
      "by": "PHASE-5 T5 (2026-08-02) \u2014 scope: validated, subset-of-parent, readable",
      "why": "+36, of which 23 are prose. This route wrote `JSON.stringify(body.permissions ?? {})` straight into the row: no schema, no bound, no reader \u2014 the plan's Step 2 requires dashboard creation to go through the SAME validation path as a spawn, and it now calls the same function. The other addition is `effectiveScope` on `GET /:id`, the 'readable any time' half of the same step. 14 executable lines for two of Step 2's five clauses."
    },
    {
      "path": "packages/server/src/agent/brokers/deny.ts",
      "from": 365,
      "to": 385,
      "by": "PHASE-5 T5 (2026-08-02) \u2014 scope: validated, subset-of-parent, readable",
      "why": "+20, of which 19 are the reason block on `isGlobalDenyRule` and ONE is the predicate line itself. RULING P5-R6 is verbatim \u2014 'a global deny is NEVER staged' \u2014 and the staged set had widened to one member: the AppleScript door's sensitive-read refusal carried a rule id this function did not recognise, so for a sub-agent the sensitive-files block list was recorded instead of applied. The comment explains which three ids joined and why two of them change no answer, because the next person to touch this predicate needs to know it is about the sensitive TIER and not about basis strings."
    },
    {
      "path": "packages/server/src/agent/brokers/grants.ts",
      "from": 333,
      "to": 344,
      "by": "PHASE-5 T5 (2026-08-02) \u2014 scope: validated, subset-of-parent, readable",
      "why": "+11 net (+20/-9), of which 18 of the additions are prose. `system_control:'*'` stops meaning AppleScript (the plan's own direction at T3 Step 2, executed here). The BRANCH SHRANK \u2014 a two-arm if/else over the scalar and list spellings becomes one arm over the list \u2014 and the comment grew, because the preserving ORDER is the load-bearing fact: the explicit grants land first (PRIMARY_AGENT_PERMISSIONS in code, migration 155 in the stored rows) and only then does the blanket stop covering it. A reader who reverses that order strips AppleScript off live agents."
    },
    {
      "path": "packages/server/src/agent/tools/util.ts",
      "from": 338,
      "to": 359,
      "by": "PHASE-5 T6 (2026-08-03) \u2014 +21, a SECURITY CLOSURE found by driving the owner's own requirement",
      "why": "THE AUDIT ROW WAS THE ONE COPY THE CREDENTIAL SEAM DID NOT COVER. `auditLog` now runs the DECLARED-value scrub (`redactHandedCredentials`, from the leaf `credentials/secret-values.ts`) over both `target` and `detail` before the INSERT. MEASURED ON THE DEV BOX BEFORE THE FIX, not reasoned about: an agent that fetched a credential with `credential_get` and used it in three `exec` calls \u2014 the supported capability the owner's binding requirement demands \u2014 wrote the plaintext key into `audit_log.target` THREE TIMES, once per call, because `process-run.ts` audits the argv line verbatim and the argv line is exactly where the value HAS to be for the command to work at all. `audit_log` has no TTL and rides the diagnostics export, so it was the longest-lived plaintext copy the flow produced, and no clause anywhere gated the column. AFTER: the same drive writes `sh /tmp/t6-capture.sh <redacted-credential> solo1` while the SPAWNED PROCESS still received the real key \u2014 proven by comparing the sha256 of what the process was handed against the sha256 of the real secret, so neither value is ever printed. That is the twin obligation satisfied inside one row: refused at rest, still usable. THE KEY IS THE DECLARED VALUE SET, NEVER A SHAPE \u2014 an undeclared secret-shaped string is deliberately left alone, pinned by clause 4 of `agent/tools/__tests__/audit-credential-redaction.test.ts` (6 clauses, RED-first at 3 failed / 3 passed, the two negative controls green in both states by design). Most of the +21 is the two `why` blocks and the import note; the mechanism is two assignments and two bind-parameter swaps. Decrease-only from here; T7 re-derives it. Detail: `.superpowers/sdd/PHASE-5/task-T6-report.md`."
    },
    {
      "path": "packages/server/src/agent/tools/index.ts",
      "from": 632,
      "to": 642,
      "by": "PHASE-5 T8 Step 2 (2026-08-03) \u2014 the executor's gate loop mints the per-call effect capability",
      "why": "PHASE-5 T8 Step 2 (RULING P5-R14). +10 lines: ONE import and ONE call, plus the seven-line comment that names the ruling and points at the module. THE MECHANISM ITSELF IS NOT HERE, deliberately \u2014 the gate's own instruction is 'move the new code elsewhere', and that is what was done: the capability, the resolver from declaration to resource, the branch-(A) scope templates and the branch-(B) carried list all live in the new `agent/effects/` module, and the executor gains a STEP rather than a mechanism. THE ALTERNATIVES WERE MEASURED, NOT WAVED AWAY. (a) Mint inline (the first draft): +32, refused \u2014 the interpretation of what a declaration MEANS belongs with the declarations, not in the dispatcher. (b) Wrap the dispatch in a callback to close the capability's window in a `finally`: +4 more and REFUSED for a better reason than lines \u2014 the window now expires off the platform's OWN per-call identity (`turn-state.ts`), so 'per call' is derived rather than remembered, and a fire-and-forget continuation cannot hold a live capability even if somebody forgets a `finally`. (c) Put the call anywhere other than immediately after the gate loop: refused, because the capability must carry resources the gates just authorized, and a mint before the refusals would exist for a call the platform declined. WHAT THE TEN LINES BUY: the residual this phase's exit gate is un-ticked over \u2014 a handler acting on a resource the gate loop never authorized, seen by no check. Held permanently by `agent/effects/__tests__/facade-contract.test.ts`, planted-fault proven in both directions (the facade stopped asking \u2192 2 clauses fail; a second minting site \u2192 the unforgeability census fails naming the file)."
    },
    {
      "path": "packages/server/src/agent/tools/definitions.ts",
      "from": 2632,
      "to": 2640,
      "by": "PHASE-5 T8 Step 3 category 2 (2026-08-03) \u2014 open_browser DECLARES the directory it has always written",
      "why": "PHASE-5 T8 Step 3, RULING P5-R14. +8 lines: the corrected `effects[]` declaration (1 line \u2192 4) plus the five-line reason. THE DEFECT IT FIXES: `open_browser`'s screenshot fallback has always written a PNG into `~/.dojo/data/canvas-shots` and the tool declared NO fs effect at all, so the converted call site was REFUSED until the declaration was corrected \u2014 which is the branch-(A) loop working exactly as P5-R14 designed it (the refusal is the signal that a declaration is owed). It adds NO refusal of its own: gate rows are declared in `tools/gates.ts` and are never derived from `effects[]` (RULING P5-R5), so this widens what the facade will carry and gates nothing new. THE ALTERNATIVES WERE MEASURED, NOT WAVED AWAY. (a) Drop the reason comment and keep the declaration alone (+3): REFUSED \u2014 'we looked and this is what it does' and 'nobody looked' must never read the same, and the next reader has to know the correction adds no refusal or they will re-litigate it. (b) Reformat the effect onto fewer physical lines to hold the count flat: REFUSED outright \u2014 contorting a source layout to move a line count is the trade this project forbids, and no line count passes or fails anything (roadmap #7). (c) Move the declaration out of the registry: REFUSED \u2014 the definition IS the declaration's home (T1); a second home for what a tool does is the disease this overhaul removes. THE DIRECTION IS STILL ENFORCED: this file may only shrink from 2640, and T8's remaining categories each correct their own declarations at this same site."
    },
    {
      "path": "packages/server/src/agent/tools/definitions.ts",
      "from": 2640,
      "to": 2647,
      "by": "PHASE-5 T8 Step 3 category 3 (2026-08-03) \u2014 history_get DECLARES the large-file store it has always read",
      "why": "PHASE-5 T8 Step 3, RULING P5-R14. +7 lines: the corrected `effects[]` declaration (1 line -> 3) plus the five-line reason. THE DEFECT IT FIXES: `history_get` has always served a `file_*` id by reading the stored body off disk and it declared `effects: []`, so the converted recall door was REFUSED until the declaration was corrected at the site \u2014 branch (A) working as P5-R14 designed it, the refusal being the signal that a declaration is owed. It adds NO refusal of its own: gate rows are declared in `tools/gates.ts` and are never derived from `effects[]` (RULING P5-R5). THE SCOPE IS THE STORE TREE AND THE NARROWER ONE WAS REFUSED ON EVIDENCE, not preference: `<store>/<agentId>` would have made a cross-agent id fail the READ, and the ownership check that produces the message the owner sees (`File X does not belong to this agent`) runs AFTER the read in `memory/retrieval.ts` \u2014 the message would have silently become `Large file not found`. A behaviour change during a byte-faithful move is exactly what this step forbids. THE ALTERNATIVES WERE MEASURED, NOT WAVED AWAY. (a) Drop the reason comment and keep the declaration alone (+2): REFUSED for the reason category 2 recorded \u2014 `we looked and this is what it does` and `nobody looked` must never read the same. (b) Reformat onto fewer physical lines to hold the count flat: REFUSED outright \u2014 no line count passes or fails anything (roadmap #7) and contorting layout to move one is the trade this project forbids. (c) Move the declaration out of the registry: REFUSED \u2014 the definition IS the declarations home (T1). THE DIRECTION IS STILL ENFORCED: this file may only shrink from 2647, and T8s remaining categories each correct their own declarations at this same site."
    },
    {
      "path": "packages/server/src/agent/effects/capability.ts",
      "from": 286,
      "to": 308,
      "by": "PHASE-5 T8 Step 3 (2026-08-03, T8D) \u2014 the header stated the per-call window wrongly and the correction is the record",
      "why": "PHASE-5 T8 Step 3. +22 lines and every one of them is COMMENT \u2014 the module gained no code. The header claimed a fire-and-forget continuation outliving its dispatch holds a DEAD capability. It was TESTED and it is FALSE: work started INSIDE the dispatch inherits the call's async context and holds a LIVE capability while it runs; only work reached from ANOTHER context (a poller or watcher started at boot, an HTTP route, a closure invoked from outside the chain) holds nothing. THE CORRECTION IS LOAD-BEARING, NOT COSMETIC: 'can this module convert' was being read as 'does the I/O happen after the handler returned', and by that reading `image_create`'s delivery IIFE would be unconvertible when it is not. The real question is WHOSE async context the I/O happens in, which is a caller-chain question. THE ALTERNATIVES WERE MEASURED, NOT WAVED AWAY. (a) Fix the sentence and leave the rest (+2): REFUSED \u2014 a one-line correction of a claim that had already misdirected one classification pass tells the next reader nothing about which question to ask instead, and this is the module every remaining category is classified against. (b) Put the correction in the report only: REFUSED \u2014 a report is not what a worker reads at the call site, and a durable document that describes reality is the bible rule. (c) Move it to a separate design note: REFUSED \u2014 a second home for what this module guarantees is the disease this overhaul removes. THE FACTS ARE PINNED AS TESTS AS WELL AS PROSE, so they cannot rot silently: two clauses in `agent/effects/__tests__/facade-contract.test.ts`, planted-fault proven (the liveness read stubbed out -> 5 of 27 fail; reverted -> 27/27)."
    },
    {
      "path": "packages/server/src/agent/tools/definitions.ts",
      "from": 2647,
      "to": 2657,
      "by": "PHASE-5 T8 Step 3 category 5 (2026-08-03) \u2014 image_create DECLARES the generated-images directory the provider bytes land in",
      "why": "PHASE-5 T8 Step 3, RULING P5-R14. +10 lines: a second `effects[]` entry (the array grows 1 line -> 4) plus the seven-line reason. THE DEFECT IT FIXES IS A DECLARATION THAT WAS SHORT BY ONE, which is a different shape from the two before it and worth the reader's time: `image_create` declared `derived:the calling agent uploads directory`, which is TRUE of the delivery copy and never covered the generator's OWN write \u2014 the provider's bytes land in `~/.dojo/uploads/generated`, a SIBLING of the agent's uploads directory, not inside it. A declaration can be honest and still be incomplete, and the facade is what found it. It adds NO refusal: gate rows are declared in `tools/gates.ts` and are never derived from `effects[]` (RULING P5-R5). THE ALTERNATIVES WERE MEASURED, NOT WAVED AWAY. (a) Widen the existing entry's scope to `~/.dojo/uploads` so one line covers both (+0): REFUSED, and this is the important one \u2014 it would authorize every OTHER agent's uploads directory to satisfy a line count, which is a real widening of what the capability names and the exact trade this project forbids. (b) Drop the reason comment (+3): REFUSED \u2014 the next reader has to know why a declaration that reads as true was still incomplete, or the same class of miss returns. (c) Reformat onto fewer physical lines: REFUSED \u2014 no line count passes or fails anything (roadmap #7). THE DIRECTION IS STILL ENFORCED: this file may only shrink from 2657."
    },
    {
      "path": "packages/server/src/agent/tools/definitions.ts",
      "from": 2657,
      "to": 2667,
      "by": "PHASE-5 T8 Step 3 category 6 (2026-08-03) \u2014 show_to_user DECLARES the uploads directory it copies into",
      "why": "PHASE-5 T8 Step 3, RULING P5-R14. +10 lines: the corrected effects[] declaration (1 line -> 4) plus the six-line reason. THE DEFECT IT FIXES: show_to_user copies any file from outside the uploads directory INTO ~/.dojo/uploads/<agentId> so the dashboards serve route can find it \u2014 the tools OWN description ends with that sentence \u2014 and it declared only the READ of the source. The write has never been declared, so the converted call site was REFUSED until the declaration was corrected at the site, which is branch (A) working exactly as P5-R14 designed it. It adds NO refusal of its own: gate rows are declared in tools/gates.ts and are never derived from effects[] (RULING P5-R5). THE SCOPE IS THE CALLING AGENTS OWN UPLOADS TREE, and the wider one was refused on evidence rather than preference: ~/.dojo/uploads would have authorized every OTHER agents uploads directory, which is the widening category 5 already refused on this same file for the same reason. THE ALTERNATIVES WERE MEASURED, NOT WAVED AWAY. (a) Drop the reason comment and keep the declaration alone (+4): REFUSED for the reason categories 2, 3 and 5 each recorded \u2014 we looked and this is what it does and nobody looked must never read the same. (b) Reformat the effects array onto fewer physical lines to hold the count flat: REFUSED outright \u2014 no line count passes or fails anything (roadmap #7) and contorting layout to move one is the trade this project forbids. (c) Declare the write as fixed:~/.dojo/uploads/<agentId> instead of a scoped derived: (+0 net): REFUSED because it would be a LIE about the mechanism \u2014 the directory is derived inside the handler from the calling agents identity, and fixed: is for a constant the tool always touches. THE DIRECTION IS STILL ENFORCED: this file may only shrink from 2667."
    },
    {
      "path": "packages/server/src/agent/tools/types.ts",
      "from": 259,
      "to": 287,
      "by": "PHASE-5 T8 Step 3 category 7 (2026-08-03, T8E) \u2014 ToolEffect gains the `via` sibling for RULING P5-R15 ADDENDUM mechanic 5",
      "why": "PHASE-5 T8 Step 3, RULING P5-R15 ADDENDUM mechanic 5. +28 lines and 26 of them are COMMENT: the `via?: EffectIndirection` field, the `EffectIndirection` union with its one member, and the two headers that say WHY an indirection is a named list rather than a shape. THE THING IT EXPRESSES: some arguments are not a path but an IDENTIFIER the platform recorded a path against (`transcribe_audio`\u2019s `attachment_id` is the whole class today). There is no tree to declare for those \u2014 T8D measured the real body and the recorded paths sit under at least three distinct roots \u2014 and prose is not a scope, so the declaration names the INDIRECTION and the gate loop resolves it with the same reader the handler uses. THE ALTERNATIVES WERE MEASURED, NOT WAVED AWAY. (a) Let `from:` carry the indirection inline (an `attachment:` prefix beside the existing three) so no new field is needed, +0 on this file: REFUSED \u2014 `from` is parsed by prefix in three places and a fourth prefix meaning something structurally different from the other three is how a parser grows a dialect; a SIBLING is the shape `scope` already established for exactly this reason. (b) Make `via` a FUNCTION on the declaration so a definition can resolve its own argument, +0 to +2: REFUSED outright, and this is the important one \u2014 a declaration that can compute its own resource is a declaration that can name anything, which is the residual this whole task exists to close. (c) Drop the comments and keep the two type lines, +2: REFUSED \u2014 the next reader has to know why the table is closed, or the first new indirection arrives as a callback. THE SET CANNOT GROW BY DECLARATION: `via` is typed to the keys of `INDIRECT_RESOLVERS`, and a census clause reads that table out of the source. THE DIRECTION IS STILL ENFORCED: this file may only shrink from 287."
    },
    {
      "path": "packages/server/src/agent/tools/definitions.ts",
      "from": 2667,
      "to": 2686,
      "by": "PHASE-5 T8 Step 3 category 7 (2026-08-03, T8E) \u2014 transcribe_audio declares its THIRD source; image_create declares the delivery copy in both directions",
      "why": "PHASE-5 T8 Step 3, RULING P5-R14 and the P5-R15 ADDENDUM. +19 lines across two declarations. (1) `transcribe_audio` takes ONE of `attachment_id`, `path` or `url` and declared only two of the three; its array grows 1 line to 5 and carries an eight-line reason for the indirection. (2) `image_create`\u2019s delivery copy is a READ of the generated-images directory \u2014 whose effect declared only the WRITE \u2014 and a WRITE of the calling agent\u2019s uploads tree, which was prose with no machine-checkable scope; that array grows 4 lines to 10 with a six-line reason. Neither adds a refusal: gate rows are declared in `tools/gates.ts` and are never derived from `effects[]` (RULING P5-R5). THE ALTERNATIVES WERE MEASURED, NOT WAVED AWAY. (a) Declare image_create\u2019s read as one WIDER tree covering both directories, saving 3 lines: REFUSED \u2014 the wider tree is the whole uploads root, which covers every OTHER agent\u2019s uploads directory; that is the exact widening category 5 already refused on this same file. (b) Give transcribe_audio a tree scope instead of the indirection, +0: REFUSED ON MEASUREMENT \u2014 there is no truthful tree, and any tree narrow enough to be honest refuses working transcription, which is a capability loss (STOP condition 4). (c) Drop the two reasons and keep the declarations, 6 lines saved: REFUSED for the reason every category before this one recorded \u2014 \"we looked and this is what it does\" and \"nobody looked\" must never read the same. THE DIRECTION IS STILL ENFORCED: this file may only shrink from 2686."
    },
    {
      "path": "packages/server/src/agent/effects/scopes.ts",
      "from": 0,
      "to": 270,
      "by": "PHASE-5 T8 Step 3 category 7 (2026-08-03, T8E) \u2014 the growth detector asked, and this is the answer in writing",
      "why": "FIRST PIN, not a raise. The growth detector fired because this file crossed 60% of the 400-line new-file cap (270 of 400) and is new since the 2026-08-01 baseline. Its own instruction is \"split it, or pin it at its current count in a gate-side-only commit\" \u2014 pinning is what was chosen, and here is why splitting was refused. WHAT THE FILE IS: the ONE place that turns a tool\u2019s DECLARED effects plus the call\u2019s real arguments into the resource list the capability carries (RULING P5-R14). All of the interpretation lives here on purpose, upstream of the facade, which is what leaves the facade a pure matcher and keeps one owner per job. THE ALTERNATIVES WERE MEASURED, NOT WAVED AWAY. (a) Split the three `from` shapes into three modules: REFUSED \u2014 they are one decision read in one loop over one declaration array, and three files would need a fourth to sequence them; the mint and the resolver would then sit in different files, which is exactly the second-permission-system shape the facade census exists to catch. (b) Move the two named tables (`CARRIED_PROGRAMS` for branch (B), `INDIRECT_RESOLVERS` for ADDENDUM mechanic 5) out to their own module, about 40 lines: REFUSED \u2014 each table is meaningless apart from the resolver clause that reads it, and a table one import away from its only reader is how a list silently grows. (c) Cut the module header, about 30 comment lines: REFUSED \u2014 it is the record of what each `from` shape MEANS, which is the single thing every remaining category is written against. WHAT THE PIN BUYS: the file is decrease-only from 270 from now on, so the interpretation layer cannot drift toward a god file while the remaining categories land \u2014 and each new category that needs a resolver clause has to come here and argue for it in a gate-side commit."
    },
    {
      "path": "packages/server/src/agent/tools/definitions.ts",
      "from": 2686,
      "to": 2706,
      "by": "PHASE-5 T8 Step 3 category 8 (2026-08-03, T8E) \u2014 tts_create and music_create declare the WHOLE async delivery path",
      "why": "PHASE-5 T8 Step 3, RULING P5-R14. +20 lines: two declarations grow 1 line to 10 each, six of those ten being the shared reason. THE DEFECT IT FIXES: both tools declared ONE of their three writes, and that one was prose with no machine-checkable scope. The provider bytes land in the shared generated-assets directory, are READ BACK from there, and are copied into the CALLING agent's uploads dir; only the last was declared at all. A declaration can be honest and still be a third of the truth. Neither correction adds a refusal: gate rows are declared in tools/gates.ts and are never derived from effects[] (RULING P5-R5). THE ALTERNATIVES WERE MEASURED, NOT WAVED AWAY. (a) One tree covering both directories, saving 6 lines: REFUSED \u2014 the only tree containing both is the whole uploads root, which covers every OTHER agent's uploads directory; the same widening categories 5, 6 and 7 each refused on this same file. (b) Share one reason comment between the two tools by putting it above the pair, saving 6 lines: REFUSED \u2014 the two definitions are 24 lines apart in the array and a reason that is not beside its declaration is a reason the next editor will not see. (c) Drop the reasons entirely, saving 12: REFUSED for the reason every category before this one recorded. WHAT IT BUYS: two more files leave the agent-named class, and they leave it because a RE-READ under the ADDENDUM's sharpened criterion found them convertible after all \u2014 not because anything was moved between classes. THE DIRECTION IS STILL ENFORCED: this file may only shrink from 2706."
    },
    {
      "path": "packages/server/src/agent/tools/types.ts",
      "from": 287,
      "to": 303,
      "by": "PHASE-5 T8 Step 3 category 9 (2026-08-03, T8E) \u2014 EffectScope gains argTree, the file_list tree resolver",
      "why": "PHASE-5 T8 Step 3, RULING P5-R15 part 1. +16 lines, 15 of them COMMENT: one new EffectScope variant, `{ at: 'argTree' }`, meaning \"the argument names a DIRECTORY and the effect covers what is inside it\". THE DEFECT IT FIXES IS A SILENT NARROWING, which is the failure mode this whole task exists to prevent: file_list is handed a directory and stats every entry IN it, its grant was at:'path' on the directory alone, and the handler's own catch turns a per-entry refusal into a dash \u2014 so converting the call site without this would have made every size column read \"-\" with nothing failing anywhere. THE ALTERNATIVES WERE MEASURED, NOT WAVED AWAY. (a) Work around it at the call site by not stat-ing entries, or by treating a refusal as a dash deliberately: REFUSED \u2014 that IS the narrowing, written down; \u00a75d names a call-site workaround as the thing not to do. (b) Give file_list a derived: tree template instead: REFUSED \u2014 the directory is the AGENT ARGUMENT, so a template would have to interpolate it, and expandScopeTemplate deliberately refuses a hole carrying a path separator (that refusal is the traversal guard). (c) Widen every args. fs_read to a tree so no new variant is needed, +0: REFUSED outright \u2014 that would give file_read a tree over the directory of any file it is asked to read, which is a real widening of every read tool in the toolbox; a clause now asserts file_read does NOT get a tree from the identical from:. THE ROOT CAN NEVER BE WIDER THAN WHAT THE GATE ALLOWED: it is the argument the brokers just resolved and authorized. THE DIRECTION IS STILL ENFORCED: this file may only shrink from 303."
    },
    {
      "path": "packages/server/src/agent/effects/scopes.ts",
      "from": 270,
      "to": 285,
      "by": "PHASE-5 T8 Step 3 category 9 (2026-08-03, T8E) \u2014 the argTree resolver clause, and its fail-closed twin",
      "why": "PHASE-5 T8 Step 3, RULING P5-R15 part 1. +15 lines: the clause that turns `scope: { at: 'argTree' }` on an args. effect into a TREE grant rooted at the resolved argument, plus the four-line fail-closed twin that yields NO grant when the same variant is written on a derived: effect (it has no template, so it names nothing there). The file was pinned at 270 by this same task one commit earlier, and this is the first raise against that pin. THE ALTERNATIVES WERE MEASURED, NOT WAVED AWAY. (a) Handle argTree inside the derived: branch only, saving the fail-closed twin: REFUSED \u2014 the whole point is that it qualifies an ARGUMENT, and a variant that silently does nothing in one branch is how a declaration comes to mean two things. (b) Mint one tree grant on the lexical root only, saving 3 lines: REFUSED ON THE PROJECT'S OWN EVIDENCE \u2014 every fs grant in this layer carries BOTH spellings because a match on the lexical name alone is walked around with a symlink and a match on the real name alone refuses a legitimate path under a linked parent; a tree grant is no different, so both roots are minted when they differ. (c) Split the args. branch into its own module to keep this file at 270: REFUSED for the reason the FIRST PIN entry above already recorded \u2014 the three from shapes are one decision read in one loop. THE DIRECTION IS STILL ENFORCED: this file may only shrink from 285."
    },
    {
      "path": "packages/server/src/agent/tools/definitions.ts",
      "from": 2706,
      "to": 2714,
      "by": "PHASE-5 T8 Step 3 category 9 (2026-08-03, T8E) \u2014 file_list declares the TREE it lists",
      "why": "PHASE-5 T8 Step 3, RULING P5-R15 part 1. +8 lines: file_list's effect gains `scope: { at: 'argTree' }` and an eight-line reason. THE DEFECT IT FIXES: the tool stats every entry inside the directory it is given, and declared only the directory. Converting it without this would have silently blanked the size column for every entry \u2014 the handler's catch swallows the refusal \u2014 which is precisely the silent narrowing this task exists to prevent, and it would have been invisible in every gate. It adds NO refusal: gate rows are declared in tools/gates.ts and are never derived from effects[] (RULING P5-R5). THE ALTERNATIVES WERE MEASURED, NOT WAVED AWAY. (a) Drop the reason (+6 saved): REFUSED \u2014 the next reader has to know why file_list carries a scope that file_read, with the identical from:, deliberately does not, or the two will be \"made consistent\" and every read tool will quietly gain a tree. (b) Leave the declaration alone and accept the dashes: REFUSED \u2014 that is a capability narrowing, STOP condition 4, and it would have shipped invisibly. THE DIRECTION IS STILL ENFORCED: this file may only shrink from 2714."
    },
    {
      "path": "packages/server/src/agent/tools/definitions.ts",
      "from": 2714,
      "to": 2721,
      "by": "PHASE-5 T8 Step 3 SURFACE SPLIT 1 (2026-08-03, T8F) \u2014 load_tool_docs declares the docs directory it reads",
      "why": "PHASE-5 T8 Step 3, RULING P5-R15 part 2. +7 lines: load_tool_docs`s `effects: []` becomes one scoped fs_read, with a five-line reason. THE DEFECT IT FIXES: the tool has always read `~/.dojo/tools/<name>.md` off disk and declared NOTHING, so the surface split that moved the reader out of tools/index-generator.ts refused it until the declaration was corrected AT THE SITE (RULING P5-R14). It adds NO refusal: gate rows are declared in tools/gates.ts and are never derived from effects[] (RULING P5-R5), and the handler intersects every requested name with getFilteredTools(agentId) before the read, so the declared directory IS the whole reach. THE ALTERNATIVES WERE MEASURED, NOT WAVED AWAY. (a) Drop the reason (+5 saved): REFUSED \u2014 the next reader has to know why a scope this wide is honest here, or it will be narrowed to a single file and load_tool_docs will start refusing every doc but one. The first draft of this comment was EIGHT lines and was tightened to five against its sibling history_get`s, which is the same correction in the same file; that is the saving taken instead. (b) Put the effect on one physical line (+2 saved): REFUSED \u2014 open_browser and history_get, the two other T8 declaration corrections in this file, both use the multi-line form, and a third spelling of the same shape is how a reader stops seeing them as one class. (c) Leave the declaration alone and let the reader keep node:fs: REFUSED \u2014 that is the split not happening, which is the whole of P5-R15 part 2. THE DIRECTION IS STILL ENFORCED: this file may only shrink from 2721."
    },
    {
      "path": "packages/server/src/agent/pdf-tools.ts",
      "from": 1017,
      "to": 1055,
      "by": "PHASE-5 T8 Step 3 the PDF door (2026-08-03, T8F) \u2014 the conversion, mechanic 7, and the bare-filename correction",
      "why": "PHASE-5 T8 Step 3, RULING P5-R15 ADDENDUM 2. +38 lines, and 30 of them are COMMENT or nonEffects REASON. What they buy: (a) the module no longer holds node:fs \u2014 22 call sites reach the disk through the facade; (b) pdf_create declares the image blocks it embeds (args.content[].path, mechanic 7) and its content.items finally documents the `path` property it has always accepted; (c) eight BARE-NAME arguments stop being declared as write paths. THE DEFECT (c) FIXES: `filename` / `output_filename` were declared fs_write from args.<name>, so the declaration resolved to a file in the SERVER working directory that these tools never touch, while the real write \u2014 the agent uploads dir \u2014 went undeclared. The declaration both missed the real resource and named a false one. THE ALTERNATIVES WERE MEASURED, NOT WAVED AWAY. (a) Reuse one shared nonEffects reason constant instead of eight literals (minus 21): REFUSED \u2014 nonEffects is read by the T1 conformance walk PER DEFINITION and the reason IS the ruling; a shared constant makes eight rulings look like one and the next reader cannot tell which tool was actually judged. (b) Drop the declaration comments (minus 7): REFUSED \u2014 the next reader has to know why `filename` is inert while `path` is not, or the two will be made consistent and the false resolution comes back. (c) Leave the bare-name declarations alone and convert anyway: REFUSED \u2014 the write would then be authorized for a path the tool never touches and refused for the one it does, i.e. the conversion would break every pdf tool. THE DIRECTION IS STILL ENFORCED: this file may only shrink from 1055."
    },
    {
      "path": "packages/server/src/agent/effects/scopes.ts",
      "from": 285,
      "to": 299,
      "by": "PHASE-5 T8 Step 3 (2026-08-03, T8F) \u2014 MECHANIC 7, element-property resolution",
      "why": "PHASE-5 T8 Step 3, RULING P5-R15 ADDENDUM 2 mechanic 7. +14 lines, 10 of them COMMENT: the args.<name>[] branch generalises to args.<name>[].<prop>, so a path one level INSIDE an array element resolves. THE DEFECT IT FIXES is the same family as the array mechanic's and it fails closed in the same wrong place: pdf_create's content blocks carry image { path }, the bare mechanic resolved each ELEMENT as a path, an object element yielded NO grant, and the converted site would have refused a documented, live capability. AN ELEMENT WITHOUT THE PROPERTY GRANTS NOTHING FOR IT, which is what keeps a mixed list \u2014 the normal case, since most content blocks are text \u2014 narrowed to exactly the elements that name a file. THE ALTERNATIVES WERE MEASURED, NOT WAVED AWAY. (a) Add a second branch beside the bare-array one (plus ~8 more): REFUSED \u2014 two branches reading the same declaration shape is how one shape comes to mean two things; the bare case is now prop === '' and a clause asserts the bare mechanic is unchanged. (b) Resolve the whole element and let the facade match any property: REFUSED outright \u2014 that is the facade gaining a rule of its own (STOP condition 2) and it would grant every path an element happens to carry. (c) Declare the image read as a derived: effect to dodge the resolver: REFUSED \u2014 a derived: with no machine scope yields no grant, so it would refuse the capability, and one with a scope cannot express 'anywhere the agent points an image block'. THE DIRECTION IS STILL ENFORCED: this file may only shrink from 299."
    },
    {
      "path": "packages/server/src/agent/effects/scopes.ts",
      "from": 299,
      "to": 317,
      "by": "PHASE-5 T8 Step 3 the techniques door (2026-08-03, T8G) \u2014 ADDENDUM 3 application (a), indirect TREE resolution",
      "why": "PHASE-5 T8 Step 3, RULING P5-R15 ADDENDUM 3(1)(a). +18 lines, 13 of them COMMENT or the new resolver's own entry: the `via` branch learns to compose with `scope: { at: 'argTree' }`, so an argument resolved through a named indirection can name a DIRECTORY whose contents are the resource, and `technique_dir` joins INDIRECT_RESOLVERS as the second (and second only) named indirection. THE DEFECT IT FIXES: mechanic 5 resolves an identifier to ONE recorded path, and the techniques cluster's reference \u2014 an id, a slug OR a display name \u2014 resolves to a directory the tool works INSIDE. No scope template can name that directory, so before this the only honest declaration was prose, which yields no grant at all; converted on prose, `technique_read action=\"search\"` would have found no supporting file and its own catch would have swallowed the refusal into a logged warning. THE ALTERNATIVES WERE MEASURED, NOT WAVED AWAY. (a) Add a new EffectScope member (`viaTree`) instead of composing (about the same lines): REFUSED \u2014 it would name the argument twice, once in `from` and once in the scope, and two spellings of one fact is how they come to disagree. (b) Duplicate the tree/path push inside the via branch (+7 more): REFUSED and the opposite was taken \u2014 both branches now go through ONE `pushResolved`, so the two ways of naming a resource cannot drift apart; that refactor is why the raise is +18 rather than +21. (c) Let the facade match any path under whatever the reference resolved to, without a declared scope: REFUSED outright \u2014 that is the facade gaining a rule of its own (STOP condition 2), and `file_read` vs `file_list` already proves the tree has to be DECLARED. THE DIRECTION IS STILL ENFORCED: this file may only shrink from 317."
    },
    {
      "path": "packages/server/src/agent/tools/types.ts",
      "from": 303,
      "to": 306,
      "by": "PHASE-5 T8 Step 3 the techniques door (2026-08-03, T8G) \u2014 the second named indirection",
      "why": "PHASE-5 T8 Step 3, RULING P5-R15 ADDENDUM 3(1)(a). +3 lines: `EffectIndirection` gains `technique_dir` and the doc says what makes it different from `attachment_row` \u2014 one resolves to a file, the other to a directory whose contents are the resource. THE REASON IT IS A TYPE AND NOT A STRING: a declaration must not be able to invent a way to turn an argument into a resource; it may only point at one the platform already owns, so the set cannot grow by declaration and a new member has to be written here by hand to exist at all. THE ALTERNATIVES WERE MEASURED. (a) Drop the doc addition (+0): REFUSED \u2014 the next reader pairing `technique_dir` with no scope gets ONE path and no error, which is the silent-narrowing failure this task exists to prevent; the sentence that says `pair it with scope argTree` is the whole warning. (b) Widen the type to `string` and let the resolver table be the only list (minus 2): REFUSED \u2014 the compile-time half is what makes an unknown `via` unwritable rather than merely unresolvable. The addition was written INSIDE the existing comment budget rather than as a new paragraph, which is why it is +3 and not +9. THE DIRECTION IS STILL ENFORCED: this file may only shrink from 306."
    },
    {
      "path": "packages/server/src/agent/tools/definitions.ts",
      "from": 2721,
      "to": 2722,
      "by": "PHASE-5 T8 Step 3 the techniques door (2026-08-03, T8G) \u2014 two false declarations corrected",
      "why": "PHASE-5 T8 Step 3, RULING P5-R15 ADDENDUM 3(1)(a). +1 line net. What changed: `technique_read` and `update_technique` now declare the technique TREE their calls resolve to, and the two RELATIVE names they used to declare as paths \u2014 `file` and `files[].path` \u2014 are ruled INERT with their reason. THE DEFECT IT FIXES is the PDF door's bare filename again: both arguments are relative names joined onto the technique row's recorded `directory_path`, so resolved as paths of their own they named files relative to the SERVER working directory that neither tool touches, while the real read and the real write went undeclared. Converted as-is, `technique_read action=\"read_file\"` and `update_technique(files:[\u2026])` would have refused their own work. THE ALTERNATIVES WERE MEASURED. (a) Keep the old declarations alongside the new one (+0 lines, since nonEffects would not be needed): REFUSED \u2014 a false grant on a cwd-relative path is exactly the resource the tool never touches, and leaving it makes the declaration lie in the widening direction. (b) Put each nonEffects reason on several lines like this file's longest rulings (+4): REFUSED \u2014 one line per ruling is the form the other 22 nonEffects entries use. (c) Leave `technique_read`'s effects array multi-line (+2): REFUSED \u2014 every other single-effect technique tool in this file uses the one-line form, and a second spelling of one shape is how a reader stops seeing them as one class. THE DIRECTION IS STILL ENFORCED: this file may only shrink from 2722."
    },
    {
      "path": "packages/server/src/agent/tools/definitions.ts",
      "from": 2722,
      "to": 2736,
      "by": "PHASE-5 T8 Step 3 SURFACE SPLIT 2 (2026-08-03, T8G) \u2014 the two imported-technique setup declarations corrected",
      "why": "PHASE-5 T8 Step 3, RULING P5-R15 part 2 + ADDENDUM 3(1)(a). +14 lines, 11 of them COMMENT. What changed: technique_set_placeholder gains the fs_read it has always performed, technique_finalize loses an fs_write NO code path performs and gains the fs_read it does, and both resolve their technique reference through the named indirection instead of prose. THE DEFECTS THEY FIX. (a) technique_set_placeholder reads the staged IMPORT_MANIFEST.json and every file the placeholder appears in BEFORE it substitutes, and declared only the write \u2014 converted as-is, the very first read would have been refused and the tool would have reported 'not imported, no placeholders' for a technique that has them. (b) technique_finalize declared fs_write on the technique directory; its only paths read the files to confirm no placeholder remains and delete the staged manifest, and its state flip is a DATABASE update \u2014 so the entry named an effect that does not exist while the read went undeclared. THE ALTERNATIVES WERE MEASURED, NOT WAVED AWAY. (a) Drop the reason comments (minus 11): REFUSED \u2014 the fs_write REMOVAL is the one edit in this task that takes a declaration away, and a later reader who cannot see why will restore it; the comment is the record that it was measured rather than forgotten. (b) Keep the fs_write and give it the tree scope (minus 6): REFUSED \u2014 that mints a live write grant for a write the tool never performs, which is precisely the false-grant class this sitting corrected twice already. (c) Declare the delete on the one manifest file instead of the tree (+0): REFUSED as unexpressible rather than undesirable \u2014 no authorized mechanic names a fixed file INSIDE a resolved directory, so the tree is the tightest honest scope, and the comment records that rather than letting it look chosen. THE DIRECTION IS STILL ENFORCED: this file may only shrink from 2736."
    },
    {
      "path": "packages/server/src/agent/effects/scopes.ts",
      "from": 317,
      "to": 334,
      "by": "PHASE-5 T8 Step 3 the canvas 3-way split (2026-08-03, T8G) \u2014 ADDENDUM 3(1)(b), the call's own agent identity as the resolution key",
      "why": "PHASE-5 T8 Step 3, RULING P5-R15 ADDENDUM 3(1)(b). +17 lines, 9 of them COMMENT: the derived-scope branch learns `at: 'agentResolved'`, which hands the named resolver the CALLING AGENT'S ID instead of an argument value, and `agent_canvas_file` joins INDIRECT_RESOLVERS as the third named indirection. THE DEFECT IT FIXES: canvas_read's schema names no path and its one production call site passes none, because the file it reads is the one the agent put on its own canvas EARLIER \u2014 so every mechanic that starts from an argument is blind to it, and prose yields no grant, which means the converted viewer would have refused the tool's ONLY use. THE ALTERNATIVES WERE MEASURED, NOT WAVED AWAY. (a) Give canvas_read a `path` argument to declare (about the same lines): REFUSED \u2014 it would be a new tool surface invented to satisfy a declaration, and C27 deliberately REMOVED the path/url/html targets from this tool. (b) Grant the whole uploads tree instead (minus 10): REFUSED \u2014 the canvas can show a file anywhere the agent opened one, so the honest scope is not a tree at all, and a tree wide enough would authorize reads the tool never makes. (c) Let the facade look the canvas up itself when a read is refused: REFUSED outright \u2014 that is the facade gaining a rule of its own (STOP condition 2). THE DIRECTION IS STILL ENFORCED: this file may only shrink from 334."
    },
    {
      "path": "packages/server/src/agent/tools/types.ts",
      "from": 306,
      "to": 316,
      "by": "PHASE-5 T8 Step 3 the canvas 3-way split (2026-08-03, T8G) \u2014 the agentResolved scope and the third named indirection",
      "why": "PHASE-5 T8 Step 3, RULING P5-R15 ADDENDUM 3(1)(b). +10 lines, 8 of them the doc on the new EffectScope member. What it buys: a declaration can say `the resource is what THIS AGENT recorded earlier` in a form the executor can resolve, instead of prose that resolves to nothing. THE REASON IT IS A SCOPE AND NOT A NEW `from` PREFIX: `from` answers WHERE the resource is named and the honest answer here is `nowhere in this call`, so the prose `derived:` stays and the machine-checkable half rides beside it exactly as RULING P5-R14 branch (A) designed \u2014 the prose survives, and 'we looked at this' still never reads the same as 'nobody looked'. THE ALTERNATIVES WERE MEASURED. (a) Reuse `via` on the effect with an empty `from` (minus 8): REFUSED \u2014 `via` is documented as resolving an ARGUMENT, and overloading it to sometimes mean the agent id is how one field comes to mean two things. (b) Drop the doc (minus 8): REFUSED \u2014 the member is the only one in the union whose key is not in the call's arguments, and a reader who misses that will look for the argument it names. THE DIRECTION IS STILL ENFORCED: this file may only shrink from 316."
    },
    {
      "path": "packages/server/src/agent/tools/definitions.ts",
      "from": 2736,
      "to": 2741,
      "by": "PHASE-5 T8 Step 3 the canvas 3-way split (2026-08-03, T8G) \u2014 canvas_read declares what it reads",
      "why": "PHASE-5 T8 Step 3, RULING P5-R14 + P5-R15 ADDENDUM 3(1)(b). +5 lines, 4 of them the reason. THE DEFECT IT FIXES: canvas_read declared `effects: []` and has always probed, stat-ed and read the file on the agent's canvas \u2014 the third tool this task has caught declaring nothing for a read it has always performed (after history_get and load_tool_docs). Converted as-is the viewer would have refused its own only use. It adds NO refusal: gate rows are declared in tools/gates.ts and are never derived from effects[] (RULING P5-R5). THE ALTERNATIVES WERE MEASURED. (a) Drop the reason comment (minus 4): REFUSED \u2014 this is the first declaration in the file whose resource comes from the agent's identity rather than an argument, and without the reason a later reader will look for the argument it names and 'fix' it. (b) Leave effects: [] and convert anyway: REFUSED \u2014 that is the tool refusing every call it has ever served. THE DIRECTION IS STILL ENFORCED: this file may only shrink from 2741."
    },
    {
      "path": "packages/server/src/microsoft/tools-office.ts",
      "from": 3471,
      "to": 3496,
      "by": "PHASE-5 T8 Step 3 the office door (2026-08-03, T8G) \u2014 the conversion, the one predicate, and three bare-filename corrections",
      "why": "PHASE-5 T8 Step 3, RULING P5-R15 ADDENDUM 3(1)(c). +25 lines net, and it is a DELETION plus a set of declarations: the duplicated local-path predicate (7 lines) left for its own leaf, 14 fs call sites became facade calls at no line cost, and the declarations grew \u2014 11 edit tools gained the `args.file_id` effect their handlers have always honoured, and the three create tools gained the uploads-tree effect plus a one-line `filename` inert ruling each. THE DEFECTS THEY FIX. (a) The edit tools accept a local .docx/.xlsx in EITHER `path` or `file_id` and declared only the first, so a converted door would refuse every call where the model put the path in file_id \u2014 which is the common case the resolver was written for. (b) `filename` on the three create tools is a BARE NAME the handler sanitises before writing into ~/.dojo/uploads/<agentId>; declared as a path it named a file in the server working directory these tools never touch while the real write went undeclared, so converted as-is every create tool would have refused its own output. THE ALTERNATIVES WERE MEASURED, NOT WAVED AWAY. (a) Declare `args.file_id` as a plain fs effect with no indirection (minus 11): REFUSED \u2014 it mints a live grant resolving a genuine OneDrive id as a cwd-relative path, i.e. a false grant on every cloud call, which is the exact class this sitting corrected three times. (b) Put the predicate in this file and let the gate loop import it here (minus 55 in the new leaf, +0 here): REFUSED \u2014 the executor would then pull docx, ExcelJS, JSZip and the Graph client into every dispatch's module graph to ask a two-line question, the same refusal T8E recorded for the STT engines. (c) Share ONE nonEffects reason constant across the three create tools (minus 2): REFUSED \u2014 nonEffects is read PER DEFINITION by the T1 conformance walk and the reason IS the ruling; a shared constant makes three rulings look like one. THE DIRECTION IS STILL ENFORCED: this file may only shrink from 3496."
    },
    {
      "path": "packages/server/src/agent/effects/scopes.ts",
      "from": 334,
      "to": 340,
      "by": "PHASE-5 T8 Step 3 the office door (2026-08-03, T8G) \u2014 the fourth named indirection",
      "why": "PHASE-5 T8 Step 3, RULING P5-R15 ADDENDUM 3(1)(c). +6 lines, 4 of them COMMENT and 1 the resolver entry: `office_local_path` joins INDIRECT_RESOLVERS, pointing at the office door's OWN local-path predicate. No branch was added \u2014 it reuses the `via` path mechanic 5 already landed \u2014 which is the point: the third application of the same-reader principle needed one table row, not a new mechanism. THE COMMENT EARNS ITS PLACE because this entry is the only one whose resolver answers `null` for a WELL-FORMED argument (a genuine OneDrive id), and a reader who does not know that will read the null as a bug. THE ALTERNATIVE WAS MEASURED: teaching the resolver a `looksLikeAPath` rule of its own instead of calling the handler's (about the same lines) \u2014 REFUSED outright, because that is the facade layer growing a predicate the handler does not share, and the two would then be free to disagree about what a call means. THE DIRECTION IS STILL ENFORCED: this file may only shrink from 340."
    },
    {
      "path": "packages/server/src/google/tools-slides.ts",
      "from": 3011,
      "to": 3016,
      "by": "PHASE-5 T8 Step 3 the slides door (2026-08-03, T8G) \u2014 the schema documents the property it has always accepted",
      "why": "PHASE-5 T8 Step 3, RULING P5-R14 + ADDENDUM 2's precedent. +5 lines NET, and the arithmetic is worth reading: the style writer's mkdir + tmp + rename LEFT for the carrying layer (minus 3), 11 fs call sites became facade calls at no cost, 18 declarations were rewritten in place at no cost, and slides_build_slide's `elements` items grew from `{ type: 'object' }` to a four-line object documenting `file_path` (+5, +3 net after the writer's departure). THE DEFECT IT FIXES: every slides_* verb declared effects: [] while SIXTEEN read ~/.dojo/data/slides_styles.json and two write it, and slides_build_slide reads a LOCAL image whose path was documented only in description prose. T1's conformance walk refused args.elements[].file_path until the schema said what the tool has always accepted. THE ALTERNATIVES WERE MEASURED, NOT WAVED AWAY. (a) Leave the schema alone and drop the image declaration (minus 5): REFUSED \u2014 the read is real and the converted door would refuse it, i.e. kind:'image_local' stops working, and this is the exact defect T8F caught on pdf_create. (b) Document every element kind's properties, not just file_path (+60 or so): REFUSED as scope this task did not measure \u2014 the tripwire asks about the resource property, the description already carries the rest, and every added property changes the provider payload and owes another prefix re-blessing. (c) Rule `file_path` inert instead (minus 4): REFUSED \u2014 it is a real read of a real file the agent names, the opposite of inert. THE OR7 COST WAS PAID RATHER THAN ASSUMED: both golden gates were RUN on the box before the commit and both PASSED with both references byte-unchanged. THE DIRECTION IS STILL ENFORCED: this file may only shrink from 3016."
    },
    {
      "path": "packages/server/src/agent/effects/fs.ts",
      "from": 0,
      "to": 258,
      "by": "PHASE-5 T8 Step 3 (2026-08-03, T8G) \u2014 FIRST PIN, at the growth detector's own instruction",
      "why": "PHASE-5 T8 Step 3. FIRST PIN, not a raise: check-growth.mjs flagged this file at 258 lines for crossing 60% of the 400-line new-file cap, and its own remedy is to pin it here so the decision is visible and the file is decrease-only from now on. WHY IT GREW: it is the fs half of the effect facade, and it grows by ONE ENTRY PER CONVERTED CATEGORY on purpose \u2014 an operation nothing calls is unexercised code pretending to be a lock, so each category brings the operations it drives and a test that drives them. This sitting added a `withFileTypes` overload (the techniques search walk) and a SECOND temp-then-rename entry moved WHOLE out of the slides style writer. THE ALTERNATIVES WERE MEASURED. (a) Split the sync and promises surfaces into two modules (minus about 100 here): REFUSED \u2014 the shape is deliberately 1:1 with node:fs so a conversion stays a rename plus an await, and two modules would make a converted call site import two things to replace one. (b) Blend the new temp-rename entry into atomicWriteFile (minus 26): REFUSED \u2014 different temp naming, different sync/async shape; merging them would change one caller's behaviour to match the other's, which a relocation may never do. (c) Drop the entry comments (minus 60): REFUSED \u2014 every entry's comment says WHAT IS DECLARED versus what is the carry's own implementation detail, and that distinction is the whole difference between a lock and a rename. THE DIRECTION IS ENFORCED FROM HERE: this file may only shrink from 258."
    },
    {
      "path": "packages/server/src/agent/effects/scopes.ts",
      "from": 340,
      "to": 349,
      "by": "PHASE-5 T8 Step 3 the transcode carry (2026-08-03, T8H) \u2014 branch (B)'s first named member",
      "why": "PHASE-5 T8 Step 3, RULING P5-R15 ADDENDUM 4(1) / P5-R14 branch (B). +9 lines, ALL of them the `CARRIED_PROGRAMS` entry and its reason: `ffmpeg` is the first program the platform CARRIES rather than matches, because `transcribe_audio`'s transcode builds its argv out of two paths the platform generates in its own temp directory. NO BRANCH WAS ADDED \u2014 the `scope: { at: 'program' }` arm has been in this file since T8B and has been yielding nothing because the list was empty; what landed is one row in the list. THE REASON IS THE POINT AND IS WHY IT IS NOT ONE LINE: the census clause already refuses a bare name, so the entry has to say what the program is for and why the argv cannot be declared, which is exactly the fact a future reader needs before adding a second. THE ALTERNATIVES WERE MEASURED, NOT WAVED AWAY. (a) Declare the transcode as an `args.` proc effect instead (about the same lines): REFUSED \u2014 no argument of `transcribe_audio` names a program, so the declaration would be a fiction and the resolver would mint nothing. (b) Keep the reason in `transcode.ts` and leave a bare key here (minus 7): REFUSED \u2014 the census reads the reason out of THIS object, so a reason living elsewhere is a reason the gate cannot see. (c) Route the temp pair through the facade and drop branch (B) entirely (minus 9 here, plus N in declarations): REFUSED \u2014 it needs a declaration that can name `os.tmpdir()`, which no template can, and inventing one would be a scope the agent never chose. THE DIRECTION IS STILL ENFORCED: this file may only shrink from 349."
    },
    {
      "path": "packages/server/src/agent/tools/definitions.ts",
      "from": 2741,
      "to": 2749,
      "by": "PHASE-5 T8 Step 3 the transcode carry (2026-08-03, T8H) \u2014 the undeclared transcoder, declared",
      "why": "PHASE-5 T8 Step 3, RULING P5-R15 ADDENDUM 4(1). +8 lines: ONE effect line on `transcribe_audio` and 7 lines of comment saying why the declaration names a PROGRAM rather than a resource. THE DEFECT IT FIXES: every source this tool accepts \u2014 attachment, path or url \u2014 is decoded or demuxed through ffmpeg before any engine sees it, and the definition declared no `proc` effect at all, so the spawn was invisible to the gate loop and the converted carry would have refused the tool's own transcode. NO `input_schema` LINE IS TOUCHED, so the tools array does not move and no prefix re-blessing is owed. THE ALTERNATIVES WERE MEASURED. (a) Declare it with prose only, no `scope` (minus 1): REFUSED \u2014 a `derived:` with no scope yields NO grant by design, so the tool would still refuse its own transcode; the prose survives as the `from`, which is the point of the sibling. (b) Drop the comment (minus 7): REFUSED \u2014 this is the FIRST branch-(B) declaration in the tree and the next reader has to know why a program is declarable here and an argv is not, or the list grows by imitation. THE DIRECTION IS STILL ENFORCED: this file may only shrink from 2749."
    },
    {
      "path": "packages/server/src/agent/tools/util.ts",
      "from": 359,
      "to": 370,
      "by": "PHASE-5 T8 Step 3 the shared util probes (2026-08-03, T8H) \u2014 the LAST agent-named toolbox file converts",
      "why": "PHASE-5 T8 Step 3. +11 lines, ALL of them the import comment, and the three probes converted at zero line cost (`fs.statSync` -> `effectFs.statSync` twice, `fs.existsSync` -> `effectFs.existsSync` once). THE COMMENT IS THE POINT AND IS WHY IT IS NOT DELETED: this module is NOT a tool, its probes run on the CALLER's grant, and all three answer their own `catch` with null / { opened: false } \u2014 so a refusal here is indistinguishable from a missing file and the user simply stops getting a download link or an Open-in-canvas chip with no error anywhere. A reader who does not know that will convert the next such helper without an oracle. The comment names both artifacts that hold it: the before/after behaviour oracle and the call-site census. THE ALTERNATIVES WERE MEASURED, NOT WAVED AWAY. (a) Say it in the test files only (minus 11): REFUSED \u2014 the hazard is at the CALL, and the next person to add a probe here reads this file, not a test in another directory. (b) Convert without the comment and let the tests speak (minus 11): REFUSED for the same reason plus one more \u2014 the tests assert WHAT holds, not WHY the shape is dangerous, and the why is the part that stops the next silent narrowing. (c) Split the three probes into their own leaf so this file shrinks (minus about 40 here, plus about 60 there): REFUSED \u2014 the probes are used by five modules that already import this one, so a split adds an import to every caller and a module to the graph to move a number, which is the bookkeeping move P5-R12 forbids. THE DIRECTION IS STILL ENFORCED: this file may only shrink from 370."
    },
    {
      "path": "packages/server/src/work/tracker-store.ts",
      "from": 503,
      "to": 530,
      "by": "PHASE-6 T0D Step 2 (2026-08-04) \u2014 the attribute door gets its G1",
      "why": "PHASE-6 T0D. +27 lines: 15 comment / 19 code / 7 deleted. THE DEFECT IT FIXES: `patchWork` was a bare `UPDATE work SET ... WHERE id = ?` returning `number`, with the three absences the survey named \u2014 no existence check, no log, no outcome type \u2014 so a write against a task id from an earlier session did nothing and told nobody at 49 of its 51 call sites. `0` also meant BOTH \"you named no field\" and \"that row does not exist\", and `updateTask` asserted the second either way. The code half is the three-arm outcome, the empty-patch existence probe and `refuseNoSuchWork`. NO `input_schema` LINE IS TOUCHED and both reference files are byte-unmoved. THE ALTERNATIVES WERE MEASURED. (a) Move `refuseNoSuchWork` into `work/outcome.ts` (minus 4): REFUSED \u2014 it is the DOOR's own refusal, built beside the door that emits it, and moving four lines to another file to make a number smaller is the relocation roadmap non-negotiable #7 says a ratchet must not reward. (b) Drop the comment explaining why `changes === 0` needs no probing SELECT (minus 8): REFUSED \u2014 that is the fact that makes the gate free on the hot path, and the next reader who does not know it adds a SELECT to a function the scheduler calls twelve times a tick. THE DIRECTION IS STILL ENFORCED: this file may only shrink from 530."
    },
    {
      "path": "packages/server/src/tracker/schema.ts",
      "from": 1010,
      "to": 1056,
      "by": "PHASE-6 T0D Step 3 (2026-08-04) \u2014 the title fallback, decided in writing at the mechanism",
      "why": "PHASE-6 T0D. +46 lines: 42 comment / 19 code / 15 deleted. Step 3 owes a DECISION IN WRITING on the title fallback \u2014 the class's second open half, where a dead id that matches a live row's title resolves to that other row. It STANDS: it is the 2026-07-17 PM 189-call spin's fix, and the hazard MEASURES ZERO on the real body (340 tracker rows, 0 with an id-shaped title, 0 whose title equals another row's id; T0B's 37 id-titled ask rows are `kind='ask'` and outside the resolver's scope by construction). The code half is `triedTitle` on the not_found arm and the clause that renders it, plus `updateTask`'s branch reading the door's outcome instead of a number. THE ALTERNATIVES WERE MEASURED. (a) Put the decision in the task report only (minus about 28): REFUSED \u2014 this plan's own routing repair says a routed item is not routed until it is in the receiving file, and a decision that lives only in a report is how the last one got lost. The mechanism is where the next person deciding to delete it will be standing. (b) Cut the measurement and keep the incident (minus 8): REFUSED \u2014 non-negotiable #15: \"it is scoped so it is probably fine\" is the absence-as-evidence move, and the counts are what turn it into a verdict. (c) Bake the fact into the message instead of carrying `triedTitle` (minus 6): REFUSED \u2014 a message that claims a lookup happened when it did not is the same dishonesty, one layer up, that this task is closing at the doors, and the unscoped control proves it does not. THE DIRECTION IS STILL ENFORCED: this file may only shrink from 1056."
    },
    {
      "path": "packages/server/src/agent/v2/loop.ts",
      "from": 9774,
      "to": 9788,
      "by": "PHASE-6 T0D Step 2 (2026-08-04) \u2014 the turn stops carrying dead ids",
      "why": "PHASE-6 T0D. +14 lines: 12 comment / 33 code / 31 deleted, at three sites. THE DEFECT IT FIXES: the close-out list reclassified DELETED ids as live on-deck stragglers and then refused every tool call naming them, and the served-work map was published even when its row read came back empty. THE +14 IS THE PRUNE ITSELF \u2014 two `advance` calls with their log lines \u2014 and the three inline queries it replaced moved OUT to `agent/v2/stale-work-ids.ts`, which is the direction this phase is going in. THE ALTERNATIVES WERE MEASURED. (a) Keep the long-form reasoning here (plus 14 more): REFUSED and already acted on \u2014 the first draft of this change was +28 and the comments were cut to pointers, because the module is where the mechanism lives and where a reader will look. (b) Move the state mutation into the module too (minus about 10): REFUSED \u2014 `advance` is the turn's own reducer and moving state writes out of the loop is PHASE-6 T1's `TurnContext` work; doing it here would pre-empt that task and make its collapse harder to prove byte-faithful. THE DIRECTION IS STILL ENFORCED: this file may only shrink from 9788, and T12's tripwire still reads the driver's residual."
    },
    {
      "path": "packages/server/src/agent/tools/cat/tracker.ts",
      "from": 840,
      "to": 848,
      "by": "PHASE-6 T0D Step 2 (2026-08-04) \u2014 `work_update:reassign` stops saying a write landed when it did not",
      "why": "PHASE-6 T0D. +8 lines: 6 comment / 4 code / 2 deleted. THE DEFECT IT FIXES: both reassign branches discarded the door's answer and went on to tell the model \"Task X reassigned to Y\" about a row resolved and re-read moments earlier \u2014 so a row deleted in between produced a confident lie. These are the two sites in the whole sweep that get a BRANCH rather than the recorder, because a model is waiting on the answer. THE ALTERNATIVES WERE MEASURED. (a) Use `noteUnsettled` here like the other 47 sites (minus 4): REFUSED \u2014 recording a refusal in a log the model cannot read, then telling it the opposite, is the honesty defect this overhaul exists to remove. (b) Drop the comment (minus 6): REFUSED \u2014 it is the only place that says WHY these two differ from the other 47, and without it the next sweep flattens them back. THE DIRECTION IS STILL ENFORCED: this file may only shrink from 848."
    },
    {
      "path": "packages/server/src/agent/tools/cat/agents.ts",
      "from": 1284,
      "to": 1285,
      "by": "PHASE-6 T0D Step 2 (2026-08-04) \u2014 one import line",
      "why": "PHASE-6 T0D. +1 line: the `noteUnsettled` import, so this file's three `patchWork` sites can consume the door's refusal. NO ALTERNATIVE EXISTS that is not worse: the recorder is one name, in one module, and re-exporting it through a file this one already imports would hide the dependency to save a line. THE DIRECTION IS STILL ENFORCED: this file may only shrink from 1285."
    },
    {
      "path": "packages/server/src/agent/v2/loop.ts",
      "from": 9788,
      "to": 9808,
      "by": "PHASE-6 T1 (2026-08-04) \u2014 the turn owns its facts and clears them in its own finally",
      "why": "PHASE-6 T1. +20 lines net. THE DEFECT IT FIXES: ten ambient maps were deleted by one statement inside `setAgentStatus(agentId,'idle')`, and the ordinary end-of-turn idle write sits INSIDE the main `try` while the `finally` that finalizes the turn record opens after it \u2014 so on EVERY turn teardown read facts that had already been deleted, silently, through `?.`/`?? null`. Two of `stampTasksAtTurnFinalize`'s four tie predicates were matched against the empty string on every turn the engine has ever run, and two conversation-scoped lookups ran unscoped. WHAT THE +20 IS: ~10 lines of wrapper (`openTurnContext` / `try` / `runV2TurnBody(agentId, turnCtx)` / `finally` / `endTurnContext`, plus the renamed body signature), ~5 lines turning a bare `catch {}` into a bound-and-logged one, and the rest pointer comments; the idle-write clear and the two turn-entry clears were DELETED. THE ALTERNATIVES WERE MEASURED. (a) Put the clear at the END of the teardown `finally` instead of a wrapper (saves ~10): REFUSED \u2014 two exits `return` before the body's `try` opens (the pickup-claim-lost bails, one human and one engine) and would leak the bag on both, and a clear inside the block that READS the bag has to be hand-ordered after four reads forever, which is the exact fragility that produced the defect. (b) Re-indent the 8,800-line body into the new `try` instead of splitting the function (saves ~4): REFUSED \u2014 it would make an 8,800-line whitespace diff out of a change that must be provably byte-faithful, in the phase whose next nine tasks depend on that proof. (c) Keep the bare `catch {}` (saves 5): REFUSED \u2014 the task's own instruction is that the silent degrade becomes a loud, tested behaviour or is removed with its reason. THE DIRECTION IS STILL ENFORCED: this file may only shrink from 9808, and T12's tripwire reads the DRIVER'S RESIDUAL, which is `runV2TurnBody` and is now smaller by the wrapper."
    },
    {
      "path": "packages/server/src/memory/assembler.ts",
      "from": 2612,
      "to": 2615,
      "by": "PHASE-6 T1 (2026-08-04) \u2014 one aliased import and its reason",
      "why": "PHASE-6 T1. +3 lines: the turn-context import (which cannot share the turn-state line, being a different module) plus a 2-line note that it is ALIASED \u2014 `turnContext` already means `PromptTurnContext` throughout this file, an assembly's per-turn INPUTS, and the two turn-shaped things must not be confused at the eleven sites that use the parameter. NO ALTERNATIVE IS BETTER: dropping the note saves 2 and re-creates the ambiguity the typechecker caught here in the first place; renaming the module's export would spread the awkwardness to nineteen other files that read it perfectly. THE DIRECTION IS STILL ENFORCED: this file may only shrink from 2615."
    },
    {
      "path": "packages/server/src/agent/runtime.ts",
      "from": 1415,
      "to": 1416,
      "by": "PHASE-6 T1 (2026-08-04) \u2014 one import line",
      "why": "PHASE-6 T1. +1: `turnContext` now comes from `agent/turn-context.js` while `turnBoundary` and the A2A carriers stay in `agent/turn-state.js` \u2014 two modules, two import lines. NO ALTERNATIVE EXISTS that is not worse: re-exporting one through the other to save a line would hide the dependency, which is the shim habit this overhaul removes. THE DIRECTION IS STILL ENFORCED: this file may only shrink from 1416."
    },
    {
      "path": "packages/server/src/agent/tools/index.ts",
      "from": 675,
      "to": 676,
      "by": "PHASE-6 T1 (2026-08-04) \u2014 one import line",
      "why": "PHASE-6 T1. +1: same split as runtime.ts \u2014 `runWithToolCallId` stays in `turn-state.js` (its AsyncLocalStorage scope is one tool EXECUTION, narrower than a turn, and deliberately not moved), `turnContext` comes from `turn-context.js`. THE DIRECTION IS STILL ENFORCED: this file may only shrink from 676."
    },
    {
      "path": "packages/server/src/agent/tools/util.ts",
      "from": 370,
      "to": 371,
      "by": "PHASE-6 T1 (2026-08-04) \u2014 one import line",
      "why": "PHASE-6 T1. +1: same split, same reason \u2014 `getCurrentToolCallId` stays with the async-context mechanism, the turn's facts come from the bag. THE DIRECTION IS STILL ENFORCED: this file may only shrink from 371."
    },
    {
      "path": "packages/server/src/agent/turn-state.ts",
      "from": null,
      "to": 218,
      "by": "PHASE-6 T1 (2026-08-04) \u2014 NEW PIN (the plan's RE-DERIVED #8: neither ambient-state file was watched)",
      "why": "PHASE-6 T1. NOT A RAISE \u2014 a first pin, and the file SHRANK to earn it (338 -> 218 as the ten per-turn maps left for `turn-context.ts`). The plan's own instruction: 'Neither turn-state.ts nor shared-state.ts is in ratchets.json \u2014 T1 must add the pin or the collapse target can grow unwatched.' Both sat under `maxNewFileLines` (400) and were therefore invisible to the decrease-only rule while half the ambient surface lived in them. Pinned at their measured size, which is a far tighter watch than the 400 cap: what is left here is the CROSS-TURN state, and it may only shrink."
    },
    {
      "path": "packages/server/src/agent/shared-state.ts",
      "from": null,
      "to": 90,
      "by": "PHASE-6 T1 (2026-08-04) \u2014 NEW PIN (RE-DERIVED #8)",
      "why": "PHASE-6 T1. NOT A RAISE and NOT A CHANGE \u2014 this file is byte-untouched by T1, which is the finding: all twelve of its maps/sets are process-level and none belongs in a bag cleared in `finally`. It is pinned because the plan's exit gate needs a denominator ('a grep-zero over one file would pass while half the surface stood') and because an unpinned file under the 400 cap can grow 310 lines unwatched."
    },
    {
      "path": "packages/server/src/agent/turn-context.ts",
      "from": null,
      "to": 275,
      "by": "PHASE-6 T1 (2026-08-04) \u2014 NEW PIN (the collapse target itself)",
      "why": "PHASE-6 T1. NOT A RAISE \u2014 a new file at 275, well under `maxNewFileLines` (400) and therefore already legal; pinned anyway so the thing the ten maps collapsed INTO is watched at least as tightly as the two they came from. Its content is the ten fields with the comments that moved with them, plus the lifecycle (`openTurnContext` / `turnContext` / `endTurnContext`) and the reason the clear lives in `runV2Turn`'s wrapper rather than the teardown `finally`."
    },
    {
      "path": "packages/server/src/agent/v2/loop.ts",
      "from": 9808,
      "to": 9672,
      "by": "PHASE-6 T8 (2026-08-04) \u2014 A LOWERING, the first tranche of the decomposition",
      "why": "THE TIGHTENING THE PLAN'S per-tranche Step 3 OWES, paid at the tranche that earned it. The postExecution gates (`:8329`-`:8480` at `b391949`, 152 lines) left this file for `agent/v2/steps/post-execution/`; the call site that replaced them is 13 lines and its import 3, so the MEASURED cut is 136 (9808 -> 9672) and not the 152 that moved. The difference is stated rather than smoothed: a relocation pays for its own seam. From here this file may only shrink, and each of the eight remaining tranches lowers it again. TWO ALTERNATIVES MEASURED AND REFUSED: (a) `check-ratchets.mjs --tighten`, which would ALSO have lowered five unrelated files carrying pre-existing slack (`agent/tools/cat/fs.ts` -2, `services/transcription.ts` -84, `techniques/share-import.ts` -101, `techniques/store.ts` -75, `work/store.ts` -2) \u2014 real slack, and real, but someone else's to claim rather than this tranche's to bundle in; (b) leaving the pin at 9808 so the later tranches have headroom, which is the ratchet inverted \u2014 headroom is the precise thing the decrease-only rule exists to remove."
    },
    {
      "path": "packages/server/src/agent/v2/steps/post-execution/index.ts",
      "from": null,
      "to": 235,
      "by": "PHASE-6 T8 (2026-08-04) \u2014 NEW PIN (the first step package)",
      "why": "NOT A RAISE \u2014 a new file at 235, under `maxNewFileLines` (400) and under the growth gate's 240 crossing line, so it is already legal twice over. Pinned anyway for the reason T1 pinned `turn-context.ts`: the thing the driver decomposes INTO must be watched at least as tightly as the thing it came out of, or this phase moves 8,800 lines into nine unwatched files and reports a shrinking `loop.ts` as progress. 152 of the 235 are the relocated span, byte-faithful bar five control-flow statements and two comments; the remainder is the module header, the typed `(state, ctx)` inputs, and the measured note that replaced the two false ones."
    },
    {
      "path": "packages/server/src/agent/v2/steps/step-outcome.ts",
      "from": null,
      "to": 78,
      "by": "PHASE-6 T8 (2026-08-04) \u2014 NEW PIN (the step contract every tranche shares)",
      "why": "NOT A RAISE \u2014 a new file at 78. It carries the contract the nine step packages share: the `(state, ctx)` -> `StepOutcome` shape, and the exit-request channel (a step ASKS by returning; the driver honours it at the call site, where nothing downstream can overwrite the request; `exit` carries its reason by construction). Pinned because it is the one file every remaining tranche will touch, and a shared contract that grows unwatched is how nine tranches grow nine conventions \u2014 the failure RULING P6-R3(1) exists to prevent. 47 of its 78 lines are the doc stating the rule and citing the defect it replaces (`loop.ts`'s own head comment on mid-body `phase` writes)."
    },
    {
      "path": "packages/server/src/agent/v2/loop.ts",
      "from": 9672,
      "to": 9362,
      "by": "PHASE-6 T9b (2026-08-04) \u2014 A LOWERING, the second tranche of the decomposition (CUT 2)",
      "why": "The per-tranche Step 3 tightening, paid at the tranche that earned it. The turn's exit path \u2014 the function-level `catch` and `finally` of `runV2TurnBody` (`:9330`-`:9677` at `1cbe8bb`, 348 lines) \u2014 left this file for `agent/v2/steps/teardown/`. The MEASURED cut against the phase base is 310 (9672 -> 9362), not the 348 that moved and not the 348 that moved: nine imports the span was the only reader of went with it: the carrier commit before it ADDED 6 lines here (the comment recording why `startAckTimer` now lives on the turn's bag), the two call sites and their import cost 27 back, and a relocation pays for its own seam rather than pretending it does not. TWO ALTERNATIVES MEASURED AND REFUSED, the same two CUT 1 refused and for the same reasons \u2014 the measurement was RE-RUN rather than carried: (a) `check-ratchets.mjs --tighten`, which was run and reverted, and which would ALSO have swept FIVE unrelated files' pre-existing slack (`techniques/share-import.ts` -101, `services/transcription.ts` -84, `techniques/store.ts` -75, `agent/tools/cat/fs.ts` -2, `work/store.ts` -2 = 264 lines, the identical set CUT 1 enumerated and left claimable) into a tranche commit that did not earn it; (b) leaving the pin at 9672 for the seven remaining tranches' headroom, which is the decrease-only rule inverted."
    },
    {
      "path": "packages/server/src/agent/v2/state.ts",
      "from": 578,
      "to": 610,
      "by": "PHASE-6 T9b (2026-08-04) \u2014 a RAISE, +32, and all 32 are the decision's own reasoning",
      "why": "THE `TurnPhase` DECISION THE PLAN'S \u00a7A OWED, written at the union rather than in a report nobody will read beside the code. The union gained ONE member \u2014 `'teardown'`, one line \u2014 and 31 lines of why: that `'preflight'` already sat outside the loop so the ninth step was not an exception to any real rule; that `state.phase` has exactly ONE production read (the `while` head) against ten writes, so the new write can break nothing; that a phase-only `advance` cannot newly throw, which matters because this transition also runs on the throw path where a new throw would replace the error being handled; and \u2014 the part most likely to be misread later \u2014 that this does NOT close T13's gate, because nothing but the loop head reads the field. ALTERNATIVES MEASURED AND REFUSED: (a) putting the reasoning in the step package instead and leaving one bare member here, which is where the next editor of the union would NOT look; (b) recording the decision only in the task report, which is exactly how the plan's own \u00a7A note observed that routed items go missing \u2014 'a routed item is not routed until it is in the receiving file'; (c) shortening it by dropping the what-this-does-not-close paragraph, refused because an overclaim about `TurnPhase` being load-bearing is the specific error T13's gate will be read against."
    },
    {
      "path": "packages/server/src/agent/turn-context.ts",
      "from": 275,
      "to": 301,
      "by": "PHASE-6 T9b (2026-08-04) \u2014 a RAISE, +26, for RULING P6-R3(1)'s carrier",
      "why": "The bag becomes the carrier for mutable driver locals that cross a step boundary, which is what RULING P6-R3(1) names it as ('the carrier is the turn's bag \u2026 and no second mechanism'). +26 = the `startAckTimer` field and its doc (13), the interface header rewritten to declare TWO populations honestly rather than let a driver local sit silently among facts the outside reads (12), and the initialiser (1). ALTERNATIVES MEASURED AND REFUSED: (a) a second, driver-only state object \u2014 forbidden by the ruling in so many words, and it is the adding-without-deleting shape; (b) passing the handle by value into the step, which typechecks and would even clear the timer, but lets the `= null` die at the module boundary \u2014 harmless today only because nothing runs afterwards to look, which is the reasoning this project bans and precisely the class T1 found on the four teardown reads; (c) leaving the interface header saying 'everything code OUTSIDE the loop must be able to read', which would have made the file's own doc false the moment the field landed."
    },
    {
      "path": "packages/server/src/agent/v2/steps/teardown/index.ts",
      "from": null,
      "to": 219,
      "by": "PHASE-6 T9b (2026-08-04) \u2014 NEW PIN (the ninth step's entry point)",
      "why": "NOT A RAISE \u2014 a new file at 219, under `maxNewFileLines` (400) and under the growth gate's 240 crossing line. Pinned for T1's and CUT 1's stated reason: a phase that moves 8,800 lines into nine unwatched files has not shrunk anything. It carries the two arms (`runTurnRecovery` for the `catch`, `runTurnTeardown` for the `finally`), the `TeardownContext` inputs measured rather than guessed, and the `TurnPhase` decision's summary."
    },
    {
      "path": "packages/server/src/agent/v2/steps/teardown/finalize-record.ts",
      "from": null,
      "to": 228,
      "by": "PHASE-6 T9b (2026-08-04) \u2014 NEW PIN (the turn record)",
      "why": "NOT A RAISE \u2014 a new file at 228, legal twice over. It is the P4 turn-record finalize and everything hanging off it: the closeout disposition, the ONE ticket-stamping point, the strike-0 receipt close, the phone reply binding and the per-ask answer stamp. It exists as its own file because the span is 348 lines against a 400-line cap, so the entry point could not hold it \u2014 P6-R1's directory shape earning itself rather than being invoked."
    },
    {
      "path": "packages/server/src/agent/v2/steps/teardown/attachment-flush.ts",
      "from": null,
      "to": 80,
      "by": "PHASE-6 T9b (2026-08-04) \u2014 NEW PIN (FA-TS4's teardown net)",
      "why": "NOT A RAISE \u2014 a new file at 80. The stranded-`show_to_user` net that holds the v2.9.20 requirement (queued files are never silently lost) and the deliberate refusal to re-push to iMessage/voice from a thrown turn. Split on a seam the block already had."
    },
    {
      "path": "packages/server/src/agent/v2/steps/teardown/conversation-tagging.ts",
      "from": null,
      "to": 70,
      "by": "PHASE-6 T9b (2026-08-04) \u2014 NEW PIN (C15 + F9)",
      "why": "NOT A RAISE \u2014 a new file at 70. The every-exit-path conv_key tagging (C15) and the assembled-sibling claim with its abort-safety guard (F9). Split on a seam the block already had."
    },
    {
      "path": "packages/server/src/agent/v2/loop.ts",
      "from": 9362,
      "to": 8783,
      "by": "PHASE-6 T3 (2026-08-04) \u2014 A LOWERING, the third tranche of the decomposition (CUT 3)",
      "why": "The per-tranche Step 3 tightening, paid at the tranche that earned it. The pre-call gates (`:2568`-`:3164` at `c1ad4d5`, 597 lines) left this file for `agent/v2/steps/pre-call-gates/`. The MEASURED cut is 579 (9362 -> 8783), not the 597 that moved, and the difference is stated rather than smoothed: the call site and its import cost 12 back, the context closure and its note cost 17, and SIX imports the span was the only reader of went out WITH the code (`OWNER_ALERT_HEADS_UP_PREFIX`, `backgroundDrains`, `compactionGate`, `setTrackerStatus`/`upholdClaim`, and the whole `memory/compaction.js` line) along with the five constants the ladder and the budget own. A relocation pays for its own seam. TWO ALTERNATIVES MEASURED AND REFUSED \u2014 the same two CUT 1 and CUT 2 refused, and the measurement was RE-RUN rather than carried: (a) `check-ratchets.mjs --tighten`, which was run and reverted, and which would ALSO have swept the identical FIVE unrelated files' pre-existing slack (`techniques/share-import.ts` -101, `services/transcription.ts` -84, `techniques/store.ts` -75, `agent/tools/cat/fs.ts` -2, `work/store.ts` -2 = 264 lines, still claimable and still someone else's to claim); (b) leaving the pin at 9362 for the six remaining tranches' headroom, which is the decrease-only rule inverted."
    },
    {
      "path": "packages/server/src/agent/v2/steps/pre-call-gates/index.ts",
      "from": null,
      "to": 162,
      "by": "PHASE-6 T3 (2026-08-04) \u2014 NEW PIN (the first step's entry point)",
      "why": "NOT A RAISE \u2014 a new file at 162, under `maxNewFileLines` (400) and under the growth gate's 240 crossing line. Pinned for T1's and CUT 1's stated reason: a phase that moves 8,800 lines into nine unwatched files has not shrunk anything. It carries the stop/preempt checks, the ORDER the three sub-modules run in, and the twelve measured inputs \u2014 including the written record of why three mutable driver locals ride by value rather than migrating to the turn's bag."
    },
    {
      "path": "packages/server/src/agent/v2/steps/pre-call-gates/thrash-gate.ts",
      "from": null,
      "to": 392,
      "by": "PHASE-6 T3 (2026-08-04) \u2014 NEW PIN (the thrash ladder)",
      "why": "NOT A RAISE \u2014 a new file at 392, under the 400 cap. It is the LARGEST single seam this span had and it is not divisible without cutting through the ladder itself: the drift computation, the soft nudge, the terminal auto-block and the per-signature gate all read the same three thresholds and the same steer latch. Splitting the block from the gate would put THRASH_GATE_BREAKER_LIMIT in one file and its only other reader in another. It is 8 lines under the cap, which is deliberate rather than lucky: the ladder's three numbers came with it and its OR2 notes came with it, and both are the reason this file may not simply be trimmed to look comfortable. It is ALSO seeded into `deploy/checks/growth-baseline.json` in the same commit, because it is the first step file to cross the growth gate's 240-line visibility line and the detector cannot watch a file it has never met (GUARD-AUDIT F7)."
    },
    {
      "path": "packages/server/src/agent/v2/steps/pre-call-gates/context-gates.ts",
      "from": null,
      "to": 197,
      "by": "PHASE-6 T3 (2026-08-04) \u2014 NEW PIN (the compaction gate + the gap drain)",
      "why": "NOT A RAISE \u2014 a new file at 197. The pre-call compaction gate's four decisions (<90 noop / 90-96 warn / 96-99 emergency / >=99 block, all copied verbatim) and the routine gap-based drain that must stay fire-and-forget (v2.5.14: it was once awaited through a summarizer whose only bound was an SDK default). Split on a seam the block already had."
    },
    {
      "path": "packages/server/src/agent/v2/steps/pre-call-gates/turn-budget.ts",
      "from": null,
      "to": 152,
      "by": "PHASE-6 T3 (2026-08-04) \u2014 NEW PIN (the turn-time budget and its recap)",
      "why": "NOT A RAISE \u2014 a new file at 152. The 15-minute checkpoint, the 3-crossing ladder, and the mid-turn compaction recap that answers the 2026-07-23 seven-apologies incident. Its requirement landed as six clauses in `agent/v2/__tests__/integration.test.ts` BEFORE this file existed, green on the unmoved tree \u2014 the branch had no test anywhere in the suite."
    },
    {
      "path": "packages/server/src/agent/v2/loop.ts",
      "from": 8783,
      "to": 7884,
      "by": "PHASE-6 T9 (2026-08-04) \u2014 A LOWERING, the fourth tranche of the decomposition (CUT 4)",
      "why": "The per-tranche Step 3 tightening, paid at the tranche that earned it. The finalize phase (`:7856`-`:8782` at `0942fd9`, 927 lines) left this file for `agent/v2/steps/finalize/`. The MEASURED cut is 899 (8783 -> 7884), not the 927 that moved, and the difference is stated rather than smoothed: the call site and its four-line note cost 5 back, the context closure and its own note cost 11, and SIXTEEN orphaned imports went out WITH the code (`writeToolReceipt`, `pendingWakeups`, `recoveryRunStreak`, `recordAffinityPromotion`, `affinityPromotionRefusedNoBasis`, `recordHeld`, `recordedId`, the whole `outbound-root.js` line and the `Message` type), found by the lint-baseline gate counting +11 TS6133 / +2 TS6196 / +1 TS6192 and fixed at the cause rather than by raising a pin. A relocation pays for its own seam. TWO ALTERNATIVES MEASURED AND REFUSED \u2014 the same two CUT 1, CUT 2 and CUT 3 refused, and the measurement was RE-RUN rather than carried: (a) `check-ratchets.mjs --tighten`, which was run and reverted, and which would ALSO have swept the identical FIVE unrelated files pre-existing slack (`techniques/share-import.ts` -101, `services/transcription.ts` -84, `techniques/store.ts` -75, `agent/tools/cat/fs.ts` -2, `work/store.ts` -2 = 264 lines, still claimable and still someone else to claim); (b) leaving the pin at 8783 for the five remaining tranches headroom, which is the decrease-only rule inverted."
    },
    {
      "path": "packages/server/src/agent/turn-context.ts",
      "from": 301,
      "to": 380,
      "by": "PHASE-6 T9 (2026-08-04) \u2014 A RAISE, and the whole of it is RULING P6-R3(1) carriers",
      "why": "A RAISE, deliberate and counted. The finalize span crossed EIGHT mutable driver locals; six migrated to the turn bag family by family before anything moved (P6-R3(1): \"the carrier is the turn bag \u2026 and no second mechanism\"), and each field carries the reasoning that belongs at its own site \u2014 including, for the phone-stream pair, the MEASUREMENT that its by-value alternative is unsafe (written from the model onChunk callback, and the span writes the buffer), and for the other four the honest label that they migrate under the rule rather than under a hazard. +79 lines, of which the six field declarations are 6 and the rest is that reasoning plus the initialisers. THE ALTERNATIVE REFUSED: writing the reasons only into a report, which is how the next reader inherits a rule with no reason attached \u2014 the same failure this cut CORRECTED on `deferredUserReplyWithTools`, whose declaration comment defended its position with a reason that had stopped being true."
    },
    {
      "path": "packages/server/src/agent/v2/steps/finalize/index.ts",
      "from": null,
      "to": 171,
      "by": "PHASE-6 T9 (2026-08-04) \u2014 NEW PIN (the entry point, the measured context, the ORDER and the tail)",
      "why": "NOT A RAISE \u2014 a new file at 171, under `maxNewFileLines` (400). Pinned for T1 and CUT 1 stated reason: a phase that moves 8,800 lines into nine unwatched files has not shrunk anything."
    },
    {
      "path": "packages/server/src/agent/v2/steps/finalize/channel-push.ts",
      "from": null,
      "to": 391,
      "by": "PHASE-6 T9 (2026-08-04) \u2014 NEW PIN (the settled-context hold and the five channel branches)",
      "why": "NOT A RAISE \u2014 a new file at 391, under `maxNewFileLines` (400) though ABOVE the growth gate 240-line visibility line, which is why every file of this package is seeded into `growth-baseline.json` in this same commit. Pinned for T1 and CUT 1 stated reason: a phase that moves 8,800 lines into nine unwatched files has not shrunk anything."
    },
    {
      "path": "packages/server/src/agent/v2/steps/finalize/route-reply.ts",
      "from": null,
      "to": 211,
      "by": "PHASE-6 T9 (2026-08-04) \u2014 NEW PIN (the reply-destination resolver \u2014 and the kit instrument anchor)",
      "why": "NOT A RAISE \u2014 a new file at 211, under `maxNewFileLines` (400). Pinned for T1 and CUT 1 stated reason: a phase that moves 8,800 lines into nine unwatched files has not shrunk anything."
    },
    {
      "path": "packages/server/src/agent/v2/steps/finalize/completion-ack.ts",
      "from": null,
      "to": 147,
      "by": "PHASE-6 T9 (2026-08-04) \u2014 NEW PIN (the scaffolded-work completion detection)",
      "why": "NOT A RAISE \u2014 a new file at 147, under `maxNewFileLines` (400). Pinned for T1 and CUT 1 stated reason: a phase that moves 8,800 lines into nine unwatched files has not shrunk anything."
    },
    {
      "path": "packages/server/src/agent/v2/steps/finalize/stranded-attachments.ts",
      "from": null,
      "to": 122,
      "by": "PHASE-6 T9 (2026-08-04) \u2014 NEW PIN (the show_to_user end-of-turn safety net)",
      "why": "NOT A RAISE \u2014 a new file at 122, under `maxNewFileLines` (400). Pinned for T1 and CUT 1 stated reason: a phase that moves 8,800 lines into nine unwatched files has not shrunk anything."
    },
    {
      "path": "packages/server/src/agent/v2/steps/finalize/link-backstop.ts",
      "from": null,
      "to": 113,
      "by": "PHASE-6 T9 (2026-08-04) \u2014 NEW PIN (the file download-link backstop)",
      "why": "NOT A RAISE \u2014 a new file at 113, under `maxNewFileLines` (400). Pinned for T1 and CUT 1 stated reason: a phase that moves 8,800 lines into nine unwatched files has not shrunk anything."
    },
    {
      "path": "packages/server/src/agent/v2/steps/finalize/close-the-loop.ts",
      "from": null,
      "to": 94,
      "by": "PHASE-6 T9 (2026-08-04) \u2014 NEW PIN (the A2A ack-and-ghost completion report)",
      "why": "NOT A RAISE \u2014 a new file at 94, under `maxNewFileLines` (400). Pinned for T1 and CUT 1 stated reason: a phase that moves 8,800 lines into nine unwatched files has not shrunk anything."
    },
    {
      "path": "packages/server/src/agent/v2/steps/finalize/deferred-recovery.ts",
      "from": null,
      "to": 90,
      "by": "PHASE-6 T9 (2026-08-04) \u2014 NEW PIN (G-SUP-2 \u2014 the answer that rode with a tool call)",
      "why": "NOT A RAISE \u2014 a new file at 90, under `maxNewFileLines` (400). Pinned for T1 and CUT 1 stated reason: a phase that moves 8,800 lines into nine unwatched files has not shrunk anything."
    },
    {
      "path": "packages/server/src/agent/tools/definitions.ts",
      "from": 2786,
      "to": 2816,
      "by": "PHASE-6 T0C-W (2026-08-04) \u2014 the census wire-throughs + `overdue`, owner-decided",
      "why": "the five new DECLARED properties and their descriptions \u2014 `local_time`+`local_timezone` on `work_open`, those two plus `revert_to_original` on `work_update`. This is the owner-decided capability itself; there is no version of declaring a parameter that adds no lines to the file that declares parameters."
    },
    {
      "path": "packages/server/src/agent/tools/cat/tracker.ts",
      "from": 848,
      "to": 875,
      "by": "PHASE-6 T0C-W (2026-08-04) \u2014 the census wire-throughs + `overdue`, owner-decided",
      "why": "the three door forwards (`work_open:task`, `work_open:reminder`, `work_update:edit`) and the `filter:\"overdue\"` route. ALTERNATIVE MEASURED AND REFUSED: moving the forward lists into their own module would land the ratchet elsewhere and re-create the two-lists defect T0A closed \u2014 a forward list that lives away from the door it serves is exactly how `anchor_time` was advertised and dropped."
    },
    {
      "path": "packages/server/src/work/tracker-view.ts",
      "from": 509,
      "to": 533,
      "by": "PHASE-6 T0C-W (2026-08-04) \u2014 the census wire-throughs + `overdue`, owner-decided",
      "why": "`dueScope()` \u2014 the single declaration of what \"overdue\" means, with the two live meanings it reconciles recorded at the declaration. ALTERNATIVE MEASURED AND REFUSED: the reason could live only in the report, but a predicate whose justification is not at its site is the class of thing the next reader re-derives wrongly; the comment was cut from 30 lines to 17 instead."
    },
    {
      "path": "packages/server/src/tracker/tools.ts",
      "from": 4438,
      "to": 4455,
      "by": "PHASE-6 T0C-W (2026-08-04) \u2014 the census wire-throughs + `overdue`, owner-decided",
      "why": "the `overdue` render branch (which NAMES the filter, so an empty answer cannot read as an ignored filter) plus the three model-facing texts T0C had to strike and this task made true again."
    },
    {
      "path": "packages/server/src/tracker/schema.ts",
      "from": 1056,
      "to": 1064,
      "by": "PHASE-6 T0C-W (2026-08-04) \u2014 the census wire-throughs + `overdue`, owner-decided",
      "why": "`listTasks`'s `overdue` option, answering from `dueScope()` rather than from a second copy of the predicate."
    },
    {
      "path": "packages/server/src/scheduler/runner.ts",
      "from": 1701,
      "to": 1702,
      "by": "PHASE-6 T0C-W (2026-08-04) \u2014 the census wire-throughs + `overdue`, owner-decided",
      "why": "net +1: the due query's four-line WHERE became one `dueScope('w')` call, and three comment lines record that the scheduler and the list filter now answer from one declaration."
    },
    {
      "path": "packages/server/src/agent/turn-context.ts",
      "from": 380,
      "to": 430,
      "by": "PHASE-6 T5 (CUT 5, 2026-08-04) \u2014 the `callLLM` tranche's carrier families under RULING P6-R3(1)",
      "why": "+50, and all of it is the four carrier fields and the reasoning that belongs AT each site rather than only in a report: the phone-stream family's third local (the one CUT 4 named at its declaration and deferred to this tranche, with the honest label that by value it would be correct today), the spin-brake pair (the one family here whose by-value alternative is wrong in BOTH directions \u2014 `execute` latches the flag, `callLLM` reads it a whole iteration later, and `callLLM` writes the grace counter that must survive into the next one), and the in-flight flag, which carries a MEASUREMENT instead of a hazard: `git grep -nw modelCallInFlight` over `packages/server/src` and `packages/dashboard/src` returns three hit lines \u2014 the declaration and two writes \u2014 and NO reader, so the comment claiming the start-ack timer consults it was false and is replaced by the command's answer. THE ALTERNATIVES WERE MEASURED. (a) Migrate the four fields with one-line comments (+~12): REFUSED \u2014 CUT 4 established at this same file that a carrier which claims a hazard it does not have teaches the next reader to discount the ones that do, so each field states which of the two it is; three of these four would have been correct by value today and say so. (b) Leave `modelCallInFlight` in the driver because nothing reads it (+0): REFUSED \u2014 a relocation does not get to retire a flag, and non-negotiable #15 forbids resting that on an absence; the finding is handed up instead. (c) Write the reasons into the report only (+0): REFUSED for the reason T9b recorded when it raised this same file \u2014 a report is not where the next reader of this field looks. From here this file may only shrink."
    },
    {
      "path": "packages/server/src/agent/v2/steps/step-outcome.ts",
      "from": 78,
      "to": 100,
      "by": "PHASE-6 T5 (CUT 5, 2026-08-04) \u2014 the fourth directive, `abandon`",
      "why": "+22: twelve lines of header explaining `abandon` vs `exit` ONCE for all nine steps, eight lines of the function and its doc, two in the type union. THE REQUIREMENT: the `callLLM` span holds the engine's only two exits that leave the TURN rather than the loop \u2014 the stopped and preempted mid-call `return`s, for which finalize does not run \u2014 and CUT 4's finalize contract already pins them at exactly two from the driver's side. A module cannot `return` from its caller. THE ALTERNATIVES WERE MEASURED AND REFUSED. (a) Give the `callLLM` step a PRIVATE outcome type carrying its own abandon (+0 here, ~+15 there): refused by this file's own opening sentence \u2014 it is deliberately the only thing the nine step packages share, 'so nine tranches cannot grow nine conventions', and a turn-abandoning exit is engine vocabulary, not one step's. (b) Reuse `exit` and let the driver map two reason strings onto a `return`: refused \u2014 string-typed control flow, and `exit` means finalize STILL RUNS; the two are behaviourally different, and copying the neighbour's word would have routed a stopped turn through the reply-destination router. (c) Ship the directive without the header paragraph (+10): refused \u2014 the difference between leaving the loop and leaving the turn is exactly what a later tranche will get wrong, and this file is where it is written once. This is the FIRST change to the shared contract in five cuts; three of them reused it unchanged. From here this file may only shrink."
    },
    {
      "path": "packages/server/src/agent/turn-context.ts",
      "from": 430,
      "to": 512,
      "by": "PHASE-6 T4 (CUT 6, 2026-08-04) \u2014 the `assemble` tranche's three carrier families under RULING P6-R3(1)",
      "why": "+82, and all of it is the eight carrier fields and the reasoning that belongs AT each site rather than only in a report. TWO OF THE THREE FAMILIES CARRY A MEASURED HAZARD, not the ruling's rule: the assembler-bookkeeping trio is written BY the span and read outside it, so by value the F9 assembly stamp dies at the module boundary (every turn then claims no siblings, silently, because null is legal at both readers), the non-compressible overhead never reaches the NEXT iteration's pre-call gate (CUT 3 named this tranche as the owner of that migration in its own AS-BUILT), and the once-per-TURN CONTEXT_HIGH banner latch resets every round so a long turn shows the user the same 'memory is full' message on every iteration; and the F10 start-ack quartet includes a flag WRITTEN FROM A TIMER CALLBACK, which is CUT 3's by-value test's own disqualifier, with a cap of 2 that stops binding if it is reset per iteration. The third \u2014 the ghosted-work-ask verdict \u2014 is honestly labelled as rule-following with its consequence measured: lost, a ghosted work ask reads as chatter and the steer never fires. THE ALTERNATIVES WERE MEASURED. (a) Migrate the eight with one-line comments (+~20): REFUSED for the reason CUT 4 established at this same file \u2014 a carrier that claims a hazard it does not have teaches the next reader to discount the ones that do, and here five of the eight carry a real one, so the difference has to be readable. (b) Return the three assembler-bookkeeping values as OUTPUTS instead of fields (+0 here, +~15 in the step): REFUSED by measurement \u2014 the eviction latch is read by the step itself on a LATER iteration and the assembly stamp by the turn's teardown closure, so an output would have to be threaded back into the driver and then forward again, which is a second mechanism for what the bag already is. (c) Write the reasons into the report only (+0): REFUSED for the reason T9b and T5 both recorded when they raised this same file \u2014 a report is not where the next reader of this field looks. From here this file may only shrink."
    }
  ],
  "maxNewFileLines": 400,
  "files": {
    "packages/dashboard/src/components/AttachmentChips.tsx": 465,
    "packages/dashboard/src/components/CanvasView.tsx": 425,
    "packages/dashboard/src/components/ContactsPanel.tsx": 478,
    "packages/dashboard/src/components/Dojo3Composer.tsx": 723,
    "packages/dashboard/src/components/Dojo3Stage.tsx": 450,
    "packages/dashboard/src/components/ImportWizard.tsx": 712,
    "packages/dashboard/src/components/PermissionsEditor.tsx": 435,
    "packages/dashboard/src/components/RouterConfig.tsx": 545,
    "packages/dashboard/src/components/SetupDeps.tsx": 672,
    "packages/dashboard/src/components/TechniqueSessionProvider.tsx": 533,
    "packages/dashboard/src/components/TwilioSettings.tsx": 563,
    "packages/dashboard/src/components/orb/dojoOrbEngine.ts": 2298,
    "packages/dashboard/src/lib/api.ts": 2313,
    "packages/dashboard/src/lib/voice/voice-client.ts": 873,
    "packages/dashboard/src/pages/AgentConfigPanel.tsx": 438,
    "packages/dashboard/src/pages/Agents.tsx": 678,
    "packages/dashboard/src/pages/Chat.tsx": 1970,
    "packages/dashboard/src/pages/Costs.tsx": 686,
    "packages/dashboard/src/pages/Health.tsx": 650,
    "packages/dashboard/src/pages/Memory.tsx": 499,
    "packages/dashboard/src/pages/Settings.tsx": 6413,
    "packages/dashboard/src/pages/Setup.tsx": 1995,
    "packages/dashboard/src/pages/TechniqueDetail.tsx": 410,
    "packages/dashboard/src/pages/Tracker.tsx": 1381,
    "packages/server/src/agent/a2a-transport.ts": 2008,
    "packages/server/src/agent/brokers/deny.ts": 385,
    "packages/server/src/agent/brokers/grants.ts": 344,
    "packages/server/src/agent/brokers/proc.ts": 375,
    "packages/server/src/agent/browser.ts": 412,
    "packages/server/src/agent/destructive-gate.ts": 583,
    "packages/server/src/agent/effects/capability.ts": 308,
    "packages/server/src/agent/effects/fs.ts": 258,
    "packages/server/src/agent/effects/scopes.ts": 349,
    "packages/server/src/agent/model.ts": 2621,
    "packages/server/src/agent/pdf-tools.ts": 1055,
    "packages/server/src/agent/pending-attachments.ts": 267,
    "packages/server/src/agent/permissions.ts": 145,
    "packages/server/src/agent/provider-error.ts": 317,
    "packages/server/src/agent/runtime.ts": 1416,
    "packages/server/src/agent/shared-state.ts": 90,
    "packages/server/src/agent/spawner.ts": 1425,
    "packages/server/src/agent/tools/cat/agents.ts": 1285,
    "packages/server/src/agent/tools/cat/comms.ts": 843,
    "packages/server/src/agent/tools/cat/fs.ts": 848,
    "packages/server/src/agent/tools/cat/healer.ts": 288,
    "packages/server/src/agent/tools/cat/media.ts": 896,
    "packages/server/src/agent/tools/cat/session.ts": 241,
    "packages/server/src/agent/tools/cat/tracker.ts": 875,
    "packages/server/src/agent/tools/definitions.ts": 2816,
    "packages/server/src/agent/tools/effect-conformance.ts": 406,
    "packages/server/src/agent/tools/gate-eval.ts": 304,
    "packages/server/src/agent/tools/index.ts": 676,
    "packages/server/src/agent/tools/surface.ts": 657,
    "packages/server/src/agent/tools/types.ts": 338,
    "packages/server/src/agent/tools/util.ts": 371,
    "packages/server/src/agent/turn-context.ts": 512,
    "packages/server/src/agent/turn-state.ts": 218,
    "packages/server/src/agent/v2/answered-edge.ts": 524,
    "packages/server/src/agent/v2/classifiers/multistep.ts": 482,
    "packages/server/src/agent/v2/counterparty.ts": 921,
    "packages/server/src/agent/v2/loop.ts": 6693,
    "packages/server/src/agent/v2/outbound.ts": 462,
    "packages/server/src/agent/v2/receipt.ts": 433,
    "packages/server/src/agent/v2/recovery.ts": 605,
    "packages/server/src/agent/v2/state.ts": 610,
    "packages/server/src/agent/v2/steer-queue.ts": 275,
    "packages/server/src/agent/v2/steps/assemble/delegation-and-attachments.ts": 183,
    "packages/server/src/agent/v2/steps/assemble/index.ts": 302,
    "packages/server/src/agent/v2/steps/assemble/multistep-detection.ts": 180,
    "packages/server/src/agent/v2/steps/assemble/steer-checkpoint.ts": 112,
    "packages/server/src/agent/v2/steps/assemble/technique-hints.ts": 222,
    "packages/server/src/agent/v2/steps/call-llm/index.ts": 198,
    "packages/server/src/agent/v2/steps/call-llm/model-call.ts": 312,
    "packages/server/src/agent/v2/steps/call-llm/model-selection.ts": 109,
    "packages/server/src/agent/v2/steps/call-llm/pre-call-injections.ts": 229,
    "packages/server/src/agent/v2/steps/finalize/channel-push.ts": 391,
    "packages/server/src/agent/v2/steps/finalize/close-the-loop.ts": 94,
    "packages/server/src/agent/v2/steps/finalize/completion-ack.ts": 147,
    "packages/server/src/agent/v2/steps/finalize/deferred-recovery.ts": 90,
    "packages/server/src/agent/v2/steps/finalize/index.ts": 171,
    "packages/server/src/agent/v2/steps/finalize/link-backstop.ts": 113,
    "packages/server/src/agent/v2/steps/finalize/route-reply.ts": 211,
    "packages/server/src/agent/v2/steps/finalize/stranded-attachments.ts": 122,
    "packages/server/src/agent/v2/steps/post-execution/index.ts": 235,
    "packages/server/src/agent/v2/steps/pre-call-gates/context-gates.ts": 197,
    "packages/server/src/agent/v2/steps/pre-call-gates/index.ts": 162,
    "packages/server/src/agent/v2/steps/pre-call-gates/thrash-gate.ts": 392,
    "packages/server/src/agent/v2/steps/pre-call-gates/turn-budget.ts": 152,
    "packages/server/src/agent/v2/steps/step-outcome.ts": 100,
    "packages/server/src/agent/v2/steps/teardown/attachment-flush.ts": 80,
    "packages/server/src/agent/v2/steps/teardown/conversation-tagging.ts": 70,
    "packages/server/src/agent/v2/steps/teardown/finalize-record.ts": 228,
    "packages/server/src/agent/v2/steps/teardown/index.ts": 219,
    "packages/server/src/agent/v2/turn-record.ts": 250,
    "packages/server/src/costs/tracker.ts": 541,
    "packages/server/src/credentials/secret-fields.ts": 241,
    "packages/server/src/gateway/routes/agents.ts": 858,
    "packages/server/src/gateway/routes/chat.ts": 485,
    "packages/server/src/gateway/routes/config.ts": 2530,
    "packages/server/src/gateway/routes/memory.ts": 632,
    "packages/server/src/gateway/routes/setup-deps.ts": 513,
    "packages/server/src/gateway/routes/tracker.ts": 906,
    "packages/server/src/gateway/routes/twilio.ts": 501,
    "packages/server/src/gateway/routes/update.ts": 1097,
    "packages/server/src/gateway/routes/upload.ts": 608,
    "packages/server/src/google/auth.ts": 771,
    "packages/server/src/google/tools-forms.ts": 821,
    "packages/server/src/google/tools-read.ts": 944,
    "packages/server/src/google/tools-slides.ts": 3022,
    "packages/server/src/google/tools-write.ts": 1862,
    "packages/server/src/healer/approval-routing.ts": 496,
    "packages/server/src/healer/diagnostic.ts": 1036,
    "packages/server/src/healer/healer-agent.ts": 1170,
    "packages/server/src/healer/injury-recovery.ts": 822,
    "packages/server/src/index.ts": 1186,
    "packages/server/src/memory/assembler.ts": 2615,
    "packages/server/src/memory/assembly-validation-sink.ts": 146,
    "packages/server/src/memory/assembly-validation.ts": 610,
    "packages/server/src/memory/budget.ts": 308,
    "packages/server/src/memory/compaction.ts": 1335,
    "packages/server/src/memory/deliveries-lane.ts": 240,
    "packages/server/src/memory/lanes.ts": 711,
    "packages/server/src/memory/message-store.ts": 1012,
    "packages/server/src/memory/recall.ts": 510,
    "packages/server/src/memory/retrieval.ts": 591,
    "packages/server/src/memory/summary-clean-stock.ts": 69,
    "packages/server/src/memory/summary-rebuild.ts": 458,
    "packages/server/src/microsoft/auth.ts": 623,
    "packages/server/src/microsoft/tools-office.ts": 3508,
    "packages/server/src/microsoft/tools-read.ts": 1631,
    "packages/server/src/microsoft/tools-write.ts": 2131,
    "packages/server/src/migration/checks.ts": 537,
    "packages/server/src/migration/import.ts": 407,
    "packages/server/src/prompt/assembler.ts": 1405,
    "packages/server/src/prompt/registry/entries.ts": 524,
    "packages/server/src/router/scorer.ts": 505,
    "packages/server/src/scheduler/anchor-compensation.ts": 300,
    "packages/server/src/scheduler/runner.ts": 1702,
    "packages/server/src/services/capabilities.ts": 597,
    "packages/server/src/services/gmail-watcher.ts": 592,
    "packages/server/src/services/image-generation.ts": 483,
    "packages/server/src/services/imessage-bridge.ts": 2314,
    "packages/server/src/services/outlook-watcher.ts": 517,
    "packages/server/src/services/teams-watcher.ts": 461,
    "packages/server/src/services/transcription.ts": 443,
    "packages/server/src/services/tunnel.ts": 683,
    "packages/server/src/techniques/dependencies.ts": 586,
    "packages/server/src/techniques/share-export.ts": 464,
    "packages/server/src/techniques/share-import.ts": 412,
    "packages/server/src/techniques/store.ts": 621,
    "packages/server/src/techniques/tools.ts": 892,
    "packages/server/src/tools/tool-docs.ts": 550,
    "packages/server/src/tools/unified-read.ts": 761,
    "packages/server/src/tools/work-verbs.ts": 373,
    "packages/server/src/tracker/pm-agent.ts": 2086,
    "packages/server/src/tracker/schema.ts": 1064,
    "packages/server/src/tracker/tools.ts": 4455,
    "packages/server/src/twilio/call-session.ts": 884,
    "packages/server/src/twilio/sms-inbound.ts": 406,
    "packages/server/src/vault/archive.ts": 304,
    "packages/server/src/vault/maintenance.ts": 2162,
    "packages/server/src/vault/retrieval.ts": 450,
    "packages/server/src/vault/store.ts": 1205,
    "packages/server/src/vault/tools.ts": 609,
    "packages/server/src/voice/hume-engine.ts": 605,
    "packages/server/src/voice/model-manager.ts": 583,
    "packages/server/src/voice/stt-service.ts": 538,
    "packages/server/src/voice/voice-ws.ts": 2299,
    "packages/server/src/work/ask-title.ts": 297,
    "packages/server/src/work/occurrences.ts": 426,
    "packages/server/src/work/override-requests.ts": 247,
    "packages/server/src/work/store.ts": 1741,
    "packages/server/src/work/tracker-store.ts": 530,
    "packages/server/src/work/tracker-view.ts": 533,
    "packages/server/src/work/work-reaper.ts": 523,
    "packages/shared/src/markers.ts": 286,
    "packages/shared/src/origin.ts": 246,
    "packages/shared/src/outcome.ts": 270,
    "packages/shared/src/types.ts": 527,
    "packages/shared/src/visibility.ts": 881,
    "packages/shared/src/ws.ts": 1088,
    "watchdog/src/index.ts": 1198
  }
}
